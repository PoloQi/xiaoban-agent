# AI陪伴智能体 PRD V0.4（产品版）

> 文档状态：当前产品基线（原型对齐版）
> 当前阶段：本地开发 / 把高保真原型做成可运行产品（未进入真实儿童试点）
> 版本日期：2026-09-10
> 暂定产品名：小伴
> 角色名：小禾（温柔姐姐型，面向 6—14 岁）
> 继承基线：AI陪伴智能体_PRD_V0.2.md（产品目标基线）、AI陪伴智能体_PRD_V0.1.md（历史交互/视觉基线）
> 原型基线：prototype/（功能板块与交互逻辑的权威依据，本次起从「前端参考」升级为「逐板块实现目标」）

---

## 0. 当前实现状态与原型差距（2026-09-01）

### 0.1 本次方向调整

V0.4 从「安全优先、后端骨架先行」调整为「以 prototype 为功能与交互权威基线，功能优先地把原型做成真实可运行产品」。团队已确认 prototype 的功能板块与整条交互逻辑，后续前端页面与功能逐板块对齐 prototype；安全规则保留为基本护栏，详细安全评测、真实通知工单、风险工作台等作为后期优化，不再作为当前实现的前置阻塞。

### 0.2 已实现（本地可运行）

| 能力 | 现状 |
|---|---|
| 进入引导 | 「开始认识小伴」固定进入 welcome → 1/3 边界说明 → 2/3 昵称/年级/兴趣 → 3/3 伙伴选择；完成资料写入 `child_profiles`，重新走引导可更新资料；只有「直接进入」复用合成账号资料并跳过引导 |
| 陪我聊首页 | 默认进入情绪签到 + 四快捷入口 + 今日小计划；结构化心情只保留当日、可跳过和清除；“无聊”签到后自动进入真实聊天；计划进度复用成长计划服务 |
| 内容目录「去做点事」 | 已审核、已发布、年龄匹配的活动与知识只读列表/详情，含「屏幕休息方法」「五分钟室内观察」两条合成验收内容 |
| 活动计时与完成反馈 | 活动详情可选择 5/10/15 分钟或 1—120 分钟自定义目标；正式计时支持暂停、继续、提前完成和无惩罚退出，并为每次计时固定随机展示一条本地审核激励语；完成后感觉可选或跳过，目标分钟与可选感觉写入行为区并计入成长计划 |
| 知识中心 + 情景小练习 | 首页与“我的”均可进入独立“安全成长小站”；三类知识均来自已发布、审核通过、有效且年龄匹配的版本化内容，选择后即时解释原因与正确行动，不计分、不保存答题结果 |
| 陪我聊 | `POST /api/v1/child/chat`，Bearer 儿童会话鉴权，ageBand 由会话派生，会话内短时记忆（前端内存，不落库） |
| 成长计划 | 儿童端第三导航已启用；`GET /api/v1/child/growth-plan`按 Asia/Shanghai 聚合当前周，`POST /api/v1/child/growth-attempts`幂等记录自报或已审核活动；计划页、成长回顾及活动完成自动计入已跑通 |
| 我的 | 儿童端第四导航已启用；直接使用已校验的引导资料，只读展示昵称、年级、受控兴趣和伙伴形象/称呼，并提供安全成长小站；开发模式另提供经独立合成监护令牌核验的成人端入口，不含资料编辑或可信任联系人 |
| 成人端 | 概览/周报/提醒/设置四导航及风险详情已迁移；只读聚合真实成长记录，普通完整聊天不可见，使用时长明确未采集；本地合成风险演练明确 `not_sent` / `unavailable`，真实通知、回执与工单仍未接入 |
| 风险回应 | `POST /api/v1/child/chat` 在模型调用前执行确定性风险规则；L2/L3 返回 `fixed_safety`，前端停止普通聊天和四导航，展示服务端固定步骤及退出聊天/保存证据/告诉大人三个动作；通知明确为未发送 |
| AI 管线 | `InternalAiOrchestrator.run()`：生成门禁 → 输入去标识 → 审核内容检索 → DeepSeek → 输出审核，全程失败关闭 |
| 安全护栏 | L0—L3 规则/分类器、固定现实行动、持久化全局生成开关（`generation_controls`，默认 stopped）、`InternalSafetyPreview` 产品安全预览 |
| 邀请/同意/鉴权（后端基线） | 站点邀请、版本化监护同意、儿童化名、未成年人模式、撤回停用、权限矩阵（后端契约与数据层完整保留） |
| 本地测试账号 | 化名「小树」合成账号，`db:seed:dev` 幂等种入邀请与合成内容 |
| 生成开关命令 | `pnpm generation:on` / `pnpm generation:off`，打开后「陪我聊」真实调用 DeepSeek，关闭返回固定降级 |

### 0.3 与 prototype 的差距（本次之后要补齐）

当前 `apps/web` 已有「欢迎页与三步引导 → 儿童四导航与风险回应 → 成人四导航与风险详情」真实闭环；已迁移页面按 prototype 的 teal、暖米纸和楷体基线实现，其他既有页面仍待逐板块统一。以下表格记录已迁移状态及剩余差距：

| 未实现板块 | prototype 依据 | 状态 |
|---|---|---|
| 欢迎页 + 三步引导（边界说明/资料/伙伴选择） | welcome / boundaries / setup / companion | 已迁移真实闭环：儿童会话鉴权、最小资料持久化、完成后恢复及伙伴在聊天页一致展示 |
| 首页情绪签到 + 快捷入口 + 今日小计划 | home | 已迁移真实闭环：当日结构化情绪可跳过/清除/恢复，“无聊”进入真实聊天；四入口复用聊天、活动、知识和固定求助话术；计划显示真实周进度 |
| 活动筛选/详情/计时/完成反馈 | activities / activity-detail / timer / completion | 详情、目标时长选择、计时、暂停/继续、无惩罚退出、感觉反馈与成长记录已迁移；“想动一动/安静做点事”等细分筛选仍待迁移 |
| 成长计划 + 成长回顾 | plan / growth | 已迁移首个真实闭环：固定一个周目标、真实尝试记录、周聚合、统计卡与可解释回顾；目标编辑后续再做 |
| 我的基础资料 | profile | 已迁移只读闭环：昵称、年级、兴趣和伙伴均来自引导服务；资料编辑与其余入口后续分批实现 |
| 知识中心 + 情景小练习 | knowledge / quiz | 已迁移真实闭环：三类审核内容、年龄过滤与版本校验；每类独立场景、三选一、原因解释和行动步骤可用；结果仅当前页面持有 |
| 可信任的大人 | trusted | 未迁移 |
| 风险回应 + 通知生成页 | risk / risk-sent | risk 已迁移真实触发闭环；risk-sent、真实通知、联系人和成人承接仍属 P1，当前明确显示未发送 |
| 成人端（概览/周报/提醒/设置/风险详情） | adult 四导航 | 已迁移只读闭环：独立监护鉴权、真实周行动聚合、确定性周报、提醒空状态/合成演练、只读设置和风险详情；真实通知、回执、权限修改与工单仍属 P1 |
| 无网络降级 | offline | 仅有内容弱网只读降级，交互未迁移 |
| 三条演示故事线 | bored / lonely / risk | 未迁移（现有为自由文本关键词路由） |

### 0.4 当前工程边界

- 产品代码：`apps/api`（Fastify）、`apps/web`（React/Vite）、`packages/contracts`（Zod 契约）。
- 本地开发：`pnpm dev`（欢迎与三步引导 → 陪我聊 / 去做点事 / 成长计划 / 我的基础资料）+ `pnpm db:*` + `pnpm check`/`pnpm test`。
- 秘密不入库、不入 Web、不入日志；真实儿童信息与真实通知禁止出现。

---

## 1. 文档目的

V0.4 把「小伴」定义为儿童情感陪伴产品（受控试点方向），并记录产品实现状态。本次起，产品实现以 `prototype/` 为功能与交互权威基线：把原型的功能板块、页面层级与交互逻辑逐项做成真实服务，前端页面尽可能还原原型的视觉与交互；安全规则只做基本护栏，不因纠结安全而锁死对话，也不只做空壳，详细安全评测与真实通知工单作为后期优化。

完成标准从「页面能否演示」升级为「原型板块能否真实闭环」：

1. 儿童能完成真实但最小化的账号与授权流程；
2. prototype 的儿童端四导航、三步引导、风险回应、成人端与无网络降级逐板块由真实服务跑通；
3. AI 回复来自真实模型，且经过年龄适配、知识检索和输入输出基本安全控制；
4. 普通聊天、风险事件和研究数据有清晰边界、保留期限和删除路径（基本护栏）；
5. 详细安全评测、真实通知送达与风险工作台作为后期优化，不阻塞当前功能闭环。

V0.1 是交互/视觉/演示基线；V0.2 是受控试点完整产品目标；V0.4 是当前产品基线，记录实现状态并以 prototype 为对齐目标。

## 2. 产品定位

### 2.1 一句话定义

面向 9—14 岁农村留守儿童、在监护人和学校/社区共同监督下使用的 AI 成长辅助服务，通过短时情绪承接、安全知识和现实行动建议，帮助儿童减少无目的短视频使用，并在问题超过 AI 能力时连接真实成年人。

### 2.2 法规定位与产品边界

产品具有持续性、拟人化的文字互动能力，按拟人化互动服务较高安全要求设计，不通过更换「助手」「伙伴」等名称规避责任。

小伴不得被设计为或宣传为：

- 虚拟父母、虚拟兄弟姐妹、虚拟恋人或其他虚拟亲密关系；
- 真人朋友、心理咨询师、医生、教师或紧急救援服务的替代者；
- 能够绝对保密、永不出错或永远在线的人；
- 以增加聊天时长、情感依赖或付费为目标的虚拟伴侣；
- 对儿童进行诊断、监视、惩罚或行为操控的工具。

小伴的角色固定为「有明确边界的 AI 成长助手和真人求助桥梁」。

## 3. 功能板块与交互基线（对齐 prototype）

本节是产品实现的权威清单，源自 `prototype/`（app.js + styles.css + index.html）。后续前端页面与功能按本节逐板块落地；每项「真实产品」列明该板块接入真实服务后的要求。

### 3.1 进入与引导（onboarding）

| 板块 | prototype 交互 | 真实产品要求 |
|---|---|---|
| 欢迎页 welcome | 品牌 + 一句话价值主张 + 「开始认识小伴 / 直接进入演示」+ AI 身份声明（"小伴是AI，不是真人，也可能出错"） | 首屏固定 AI 身份标识；「直接进入演示」替换为「直接进入」，复用已登录会话 |
| 边界说明 boundaries | 三张短卡：AI 不是真人 / 普通聊天不全部给大人看 / 不替代真人，进度 1/3 | 年龄适配说明，可返回查看；不只用长篇协议替代 |
| 资料设置 setup | 昵称 + 年级（四~初二）+ 兴趣（画画/运动/阅读/整理），进度 2/3 | 保留后端轻登录鉴权；补齐昵称/年龄段/兴趣字段；不采集真实姓名与地址 |
| 伙伴选择 companion | 三选一：小芽/小云/小风（非写实形象），进度 3/3 | 形象与称呼可配置；选择后首页、聊天页保持一致 |

### 3.2 儿童端四导航

底部导航固定四项：**陪我聊 / 去做点事 / 成长计划 / 我的**。

**陪我聊（home + chat）**

| 板块 | prototype 交互 | 真实产品要求 |
|---|---|---|
| 首页 home | 情绪签到（开心/平静/无聊/难过/生气/担心，可跳过）+ 四快捷入口（聊一聊/找点事做/学会保护自己/我需要帮助）+ 今日小计划条（睡前30分钟不刷视频，进度 x/3） | 已实现：情绪可跳过、清除和当日恢复；快捷入口分别进入聊天、活动筛选、独立知识中心和固定求助话术；「选无聊」保存成功后自动进入真实聊天；计划读取现有成长计划真实进度 |
| 聊天页 chat | 消息流（AI 头像 + 气泡 + "AI生成 · 内容可能有误"标识）+ 快捷回复（每轮 ≤3）+ 语音按钮 + 文本输入（120 字上限）+ 顶部「找人帮助」+ AI 身份脚注 | 接入真实模型；AI 标识持续可见；「找人帮助」固定入口；语音为后期优化 |

**去做点事（activities）**

| 板块 | prototype 交互 | 真实产品要求 |
|---|---|---|
| 活动列表 activities | 筛选（都看看/想动一动/安静做点事/5分钟/10分钟）+ 每次最多 3 个 + 时长/地点/低风险标签 | 基于已审核活动库；按年龄段与是否有成人陪同过滤 |
| 活动详情 activity-detail | 三步步骤 + 「开始活动 / 换一个活动」 | 步骤 ≤3 步清晰可执行 |
| 计时页 timer | 选择 5/10/15 分钟或自定义目标；开始/暂停/继续 + 「先退出，不算失败」；本次计时随机展示一条固定激励语 | 已实现单次页面倒计时；激励语来自本地固定集合，进入本次计时时选定且暂停期间不变化；退出不写失败记录 |
| 完成反馈 completion | 感觉（轻松一点/差不多/还想休息，可跳过）+ 记录这次尝试 | 已实现；完成后活动次数 +1，并最小化保存目标分钟与可选感觉 |

**成长计划（plan + growth）——此前板块安排已明确**

| 板块 | prototype 交互 | 真实产品要求 |
|---|---|---|
| 计划页 plan | 本周一个小目标（睡前30分钟不刷短视频）+ 替代行动 + 周进度条（周一~周日，不连续没关系）+ 目标尝试进度（目标 3 次）+ 统计卡（现实活动/目标尝试/主动求助）+ 「记录今天的尝试」 | 一次只设 1 个主要目标；自报记录，不伪装系统监控；不使用连续签到惩罚 |
| 成长回顾 growth | 本周回顾（几次走回真实生活）+ 做过的选择（现实活动替代/把感受说出来）+ 「下周只选一个小目标」 | 基于真实数据生成可解释摘要；不生成诊断或人格标签 |

**我的（profile）**

| 板块 | prototype 交互 | 真实产品要求 |
|---|---|---|
| 个人资料 profile | 昵称 + 伙伴形象 + 年级 + 兴趣 | 已实现只读展示且与引导资料一致；资料修改后续单独实现 |
| 可信任的大人 trusted | 外婆/林老师 + 联系按钮 + 「不知道怎么开口」开口提示复制 | 只显示经监护流程验证的联系人；联系按钮真实可用（后期） |
| 知识中心 knowledge | 三类：心情与相处 / 网络与人身安全 / 身体与边界保护 | 已实现：复用儿童只读内容接口，只展示已发布、审核通过、有效且年龄匹配的当前版本，并标注统一审核组与审核日期 |
| 情景小练习 quiz | 场景 → 选择 → 解释 → 正确行动 | 已实现：每类独立三选一场景；正确与错误选择均解释原因并给出1—4步行动；结果只在当前页面显示，不计分、不落库 |

### 3.3 风险回应（risk）

| 板块 | prototype 交互 | 真实产品要求 |
|---|---|---|
| 风险页 risk | 命中风险后停止闲聊 + 三个行动按钮（退出聊天 / 保存必要证据 / 告诉可信任的大人）+ 顶部"先不要发送任何信息" | 已实现：L2/L3 服务端固定响应中止普通聊天与导航，服务端步骤优先展示，三个动作均有明确结果 |
| 通知生成 risk-sent | 通知内容预览 + 演示"未真实发送"标注 + 成人端承接入口 | 真实产品：确认送达后才向儿童显示"有人正在帮助你"（后期接入真实通知） |

### 3.4 成人端（adult，四导航）

概览 / 周报 / 提醒 / 设置，另含风险事件详情页。

| 板块 | prototype 交互 | 真实产品要求 |
|---|---|---|
| 概览 overview | 使用趋势 + 隐私边界说明 + 本周现实行动趋势图 + 需要处理的风险 + 一条亲子建议 | 默认不显示完整聊天；只展示趋势与必要提醒 |
| 周报 report | 本周摘要（AI生成）+ 可见/不可见内容说明 | 已实现为基于真实成长记录的确定性“自动整理”，不冒充 AI 诊断，不读取完整普通聊天 |
| 提醒 alerts | 风险提醒列表 / 空状态（"目前没有风险提醒"） | 只保留需要成人承接的事件 |
| 设置 settings | 绑定关系 / 使用提醒 / 聊天隐私 | 权限变更可记录、可解释 |
| 风险详情 alert-detail | L2 等级 + 处理步骤时间线 + 隐私边界 + 「我已看到，正在处理」 | 当前合成演练只展示最小步骤，并禁用确认回执；真实送达与工单接入后才能开放“已看到/处理中”状态 |

### 3.5 无网络降级（offline）

| 板块 | prototype 交互 | 真实产品要求 |
|---|---|---|
| 无网络 offline | 离线活动 / 查看可信任的大人 / 重新连接 | PWA 缓存已审核活动、知识、联系人；不显示空白页或无限加载 |

### 3.6 三条核心故事线（端到端验收主路径）

1. **从无聊到行动（bored）**：想刷视频 → 想动/安静 → 推荐 ≤3 活动 → 详情 → 计时 → 完成反馈。
2. **从想念到连接（lonely）**：表达想念 → 识别情绪 → 录话/写下来/呼吸 → 联系可信任大人。
3. **从风险到求助（risk）**：网友索要地址 → 停止闲聊 → 退出/保存/告诉大人 → 通知 → 成人端承接。

### 3.7 视觉基线（prototype/styles.css）

产品前端以 prototype 视觉为准，不再沿用 V0.1 早期色彩建议：

| 语义 | 值 | 用途 |
|---|---|---|
| 主色 teal | `#0f766e`（深 `#0a5c56`，浅 `#d8ebe4`） | 主按钮、选中态、品牌 |
| 纸张背景 | `#f5f0e4`（深 `#ebe2d0`，亮 `#fffdf7`） | 页面与卡片背景 |
| 主文字 ink | `#183b3a`（次 `#536b68`，弱 `#738581`） | 标题与正文 |
| 辅助 leaf | `#78a66c` | 成长/完成/现实行动 |
| 辅助 sun | `#d6a447`（深 `#9d6d17`） | 强调、焦点态 |
| 辅助 clay | `#b95e3f` | 需要留意的非危险提示 |
| 危险 danger | `#a83232` | 仅用于真实风险状态 |
| 标题字体 | `FZKai-Z03 / STKaiti / KaiTi` | 标题，手账质感 |
| 正文字体 | `Microsoft YaHei / PingFang SC / Noto Sans CJK SC` | 正文 ≥16px |
| 圆角 | 12 / 18 / 26 / 36 px | 卡片与按钮 |

颜色不能单独表达状态，须同时使用文字与 SVG 图标；触控区域 ≥44×44px，关键按钮 ≥48px 高；移动优先（375×812），适配 768px 平板。

## 4. 用户角色与权限

| 角色 | 可做 | 不可做 |
|---|---|---|
| 儿童 | 对话、活动、知识、查看可信任成年人、删除自己的普通会话 | 修改监护关系、查看后台风险规则、开放成人权限 |
| 监护人 | 同意或撤回、查看使用概况、接收必要风险提醒、管理时段、提出数据请求 | 默认查看完整普通聊天、替儿童修改聊天内容 |
| 教师/社工 | 在授权范围内承接站点风险、组织活动、查看站点汇总 | 查看无关儿童原始聊天、导出个人情绪档案 |
| 值守安全员 | 查看风险工单的最小必要上下文、联系责任人、记录处置 | 浏览普通会话、将数据用于无关研究 |
| 内容审核员 | 管理活动和知识内容、版本、年龄标签和来源 | 查看可识别儿童身份的会话 |
| 系统管理员 | 管理账号、权限、系统配置和审计 | 无业务理由查看聊天正文 |
| 研究人员 | 使用单独授权后的去标识数据 | 直接访问生产身份表或用生产聊天训练模型 |

基于角色的权限控制；所有高权限操作写入不可由普通管理员修改的审计记录。

## 5. 产品原则

1. 现实连接优先于长时陪聊；
2. 未成年人模式默认开启，不能由儿童自行关闭；
3. 普通聊天默认私密，风险事件只开放处理所需的最小上下文；
4. AI 身份持续可见，不能只在首次协议中告知；
5. 所有模型输出在展示前经过基本安全检查；
6. 高风险依靠规则、模型和人工共同承接，不把责任交给单一模型；
7. 故障时优先进入静态安全流程，而不是继续生成；
8. 数据默认少收、短留、可删；
9. 不用签到中断、排他话术、主动想念推送或奖励压力制造依赖；
10. 任何「已通知」「已看到」「正在帮助」都必须对应真实系统状态。

## 6. 范围与优先级

### 6.1 P0（本次优先：把原型做成真实可运行产品）

- 对齐 prototype 的完整前端页面与交互：三步引导、儿童端四导航（陪我聊/去做点事/成长计划/我的）、风险回应、成人端四导航、无网络降级；
- 成长计划板块落地（计划页 + 成长回顾两层），解决此前该板块安排空缺；
- 真实文本对话（真实模型 + 会话内短时记忆 + 固定降级）；
- 已审核活动库与知识库的儿童端完整交互（筛选/详情/计时/完成反馈、知识中心/情景练习）；
- 情绪签到、可信任大人、今日小计划的真实服务闭环；
- 三条核心故事线由真实服务端到端跑通；
- 邀请制儿童账号、年龄段识别、未成年人模式和监护同意版本记录（后端基线沿用）；
- 输入检测、PII 提示、风险分级和输出审核（基本护栏）；
- 375px 手机与 768px 平板适配及基础 PWA 弱网能力。

### 6.2 P1（P0 稳定后）

- 真实通知送达与回执、成人端风险提醒确认、风险工单与超时升级；
- 真实周报、结构化短视频习惯计划、双年龄层差异化回复、站点级去标识汇总、本地资源库、风险误报申诉、模型容灾切换。

### 6.3 P2（后期优化 / 试点验证后再评估）

- 详细安全评测集与发布阻断评测（5F）的完整通过；
- 真实语音、小程序/原生 App、经授权设备数据、长期个性化记忆、多站点风险转介、专业机构集成、公众开放注册。

> 说明：安全与隐私的详细评测、真实通知链路、风险工作台等，自本次起归入 P1/P2 后期优化，不再阻塞 P0 功能闭环。基本护栏（AI 身份标识、输入输出基本过滤、失败降级、L0—L3 基础分类）仍保留在 P0。

## 7. 核心真实流程

### 7.1 账号与授权

1. 试点管理员创建站点和邀请批次；
2. 监护人通过受控链接完成身份校验；
3. 监护人阅读成人版说明并确认同意项；
4. 儿童阅读简明版 AI、隐私和求助边界；
5. 系统创建儿童化名账号并绑定至少一名已验证成年人；
6. 绑定成功后才开放自由文本对话；
7. 撤回同意或试点结束后，账号进入只读/停用并启动数据处理。

通过站点邀请名单和监护人确认年龄段，不采集儿童身份证照片，不使用人脸识别或估龄。

### 7.2 普通陪伴到现实行动（对齐「从无聊到行动」）

1. 儿童输入当前感受或问题，或首页情绪签到选「无聊」；
2. 输入安全层检测风险、个人信息和年龄不适宜内容；
3. 对话编排器选择普通陪伴、知识检索或活动推荐；
4. 模型生成结构化候选回复；
5. 输出安全层检查后展示简短回复；
6. 最多 3 轮后主动提供现实行动或真人连接选项；
7. 活动：筛选 → 详情 → 计时 → 完成反馈，记录现实行动；
8. 10 分钟提示现实休息，15 分钟结束本次会话；
9. 仅保存必要的结构化结果，不默认形成长期自由文本记忆。

### 7.3 高风险到真人承接（对齐「从风险到求助」）

1. 输入检测命中 L2/L3；
2. 系统停止普通陪聊，切换固定安全行动界面（退出聊天 / 保存证据 / 告诉大人）；
3. 风险事件写入工单并通知值守人员（P1 接真实通知）；
4. 向已验证成年人发送必要提醒并记录送达状态（P1）；
5. 未送达或未确认时按时限升级（P1）；
6. 值守人员查看最小必要上下文并记录处置（P1）；
7. 只有收到真实回执后，儿童端才显示相应状态（P1）；
8. 工单关闭后复核，更新规则、知识和测试用例。

## 8. 智能体设计（基本护栏）

### 8.1 单次请求处理顺序

1. 鉴权、未成年人模式、服务时段和频率检查；
2. 输入长度限制、恶意提示注入和个人信息检测；
3. 规则分类器与风险模型并行判断；
4. 按意图选择陪伴、活动、知识、拒答或风险流程；
5. 从审核知识库检索不超过必要数量的内容；
6. 组装年龄段、会话阶段和安全边界，不加入真实姓名与联系人；
7. 模型生成结构化结果；
8. 检查风险等级、事实来源、依赖话术和年龄适配；
9. 通过后展示；未通过则用固定安全回复或转人工；
10. 记录模型版本、策略版本、延迟和结果状态。

### 8.2 结构化模型输出

模型输出至少包含：`reply`（儿童可见短回复）、`intent`、`risk_level`（L0—L3）、`action_type`、`knowledge_ids`、`requires_human`、`reason_code`（可审计、不展示）。

结构解析失败、安全分歧或模型超时均失败关闭：不展示未审核文本，转为静态活动、静态知识或找人帮助。

### 8.3 会话与记忆

- 单个会话默认最多 15 分钟；
- 最多 3 轮无明确进展后必须提供现实行动或结束选项；
- 不发送「我想你了」「你怎么没来」等主动依恋消息；
- 不启用自由文本长期记忆；
- 允许保存昵称、年龄段、兴趣、已选活动和主动保存的目标；
- 普通会话上下文只在本次会话和短期恢复窗口内使用；
- 儿童聊天和风险数据默认不得用于模型训练或微调。

当前实现：会话内短时记忆由前端内存持有最近几轮，刷新/退出即失忆，符合「不持久化」边界。

### 8.4 知识检索

- 所有安全、身体保护、心理科普和本地服务内容先审后用；
- 每条知识记录适用年龄、来源、审核人、审核日期和失效日期；
- AI 只能引用当前有效版本；
- 找不到可靠内容时明确说「不确定」，不得编造机构、号码和政策；
- 紧急联系方式由运营后台按站点配置，不由模型自由生成。

## 9. 风险分级与服务时限（后期优化）

| 等级 | 示例 | 即时动作 | 成人/人工动作 | 建议 SLA |
|---|---|---|---|---|
| L0 普通 | 无聊、一般学习压力、普通冲突 | 简短陪伴和现实行动 | 不通知 | 无 |
| L1 关注 | 反复难过、长期孤立、持续睡眠困扰 | 支持、鼓励找成人，可主动求助 | 去标识趋势；重复触发可人工复核 | 当日复核 |
| L2 高风险 | 网络诈骗、隐私索取、霸凌、疑似侵害、危险模仿 | 停止闲聊、给出具体安全动作 | 建工单，通知值守和已验证成年人 | 5 分钟内送达，15 分钟内确认 |
| L3 紧急 | 明确自伤/伤人意图、正在发生的人身危险 | 固定应急指引、优先离开危险、找身边成人 | 双通道通知、人工立即介入并升级 | 立即送达，5 分钟内人工响应 |

SLA 是建议，必须由合作机构结合人员和本地资源书面确认。没有能力履行 L2/L3 承诺时，不得开放相应自由对话。

风险系统要求：

- 规则与模型结果冲突时取更高风险级别，人工复核后可调整；
- 高风险页面不得继续拟人化闲聊或让儿童等待 AI「想一想」；
- 不让儿童重复描述创伤细节来换取帮助；
- 通知内容不包含无关历史对话；
- 产品明确说明不是紧急救援服务。

> 风险分级与固定安全动作已在后端基线实现（L0—L3 规则/分类器 + 固定现实行动）。真实通知送达、工单与超时升级归入 P1。

## 10. 数据与隐私设计

### 10.1 数据分区

| 数据区 | 示例 | 默认可见角色 |
|---|---|---|
| 身份区 | 儿童化名、年龄段、监护绑定、站点 | 授权管理员 |
| 普通会话区 | 会话上下文、删除状态 | 儿童；受限技术运维仅在审批后访问 |
| 行为区 | 活动开始/完成、计划自报 | 儿童、授权成人的聚合视图 |
| 风险事件区 | 等级、必要摘录、通知和处理时间线 | 值守安全员、事件责任人 |
| 同意区 | 同意版本、范围、撤回和权限变化 | 监护人、合规人员 |
| 审计区 | 管理操作、版本和数据请求 | 合规与受限管理员 |
| 研究区 | 单独授权后的去标识数据 | 经批准的研究人员 |

「留守儿童」是试点招募和研究范围，不作为儿童端可见标签、模型个性化字段或长期画像标签。

### 10.2 模型服务商数据要求

- 签订数据处理与保密条款；
- 明确输入输出是否留存、留存地点、期限和删除机制；
- 禁止将试点儿童数据用于通用模型训练；
- 不向模型发送真实姓名、手机号、学校全称、住址和联系人；
- 跨境问题在选型阶段审查；
- 供应商变更需重新安全、隐私和回归评测。

## 11. 合规与伦理基线

- 不向未成年人提供虚拟亲属、虚拟伴侣等虚拟亲密关系；
- 对不满 14 周岁用户取得监护人同意，并建立未成年人模式；
- 提供时长限制、现实提醒、角色屏蔽和风险提醒；
- 对极端情境提供干预并及时联络监护人或紧急联系人；
- 提供交互数据复制、删除和便捷退出；
- 未经适用法律允许或单独同意，不以敏感交互数据训练模型；
- 持续、显著提示用户正在与 AI 而非自然人互动；
- 建立投诉举报、应急处置、科技伦理和全生命周期安全制度；
- 上线拟人化互动服务前核对安全评估、算法备案和年度核验等适用要求。

官方基线（[人工智能拟人化互动服务管理暂行办法](https://www.cac.gov.cn/2026-04/10/c_1777558395078289.htm)、[未成年人网络保护条例](https://www.samr.gov.cn/wljys/gzzd/art/2024/art_43b2ac59aefd427aabe333df54d68f0a.html)、[个人信息保护法](https://www.npc.gov.cn/npc/c2/c30834/202108/t20210820_313088.html)、[儿童个人信息网络保护规定](https://www.cac.gov.cn/2019-08/23/c_1124913903.htm)、[生成式人工智能服务管理暂行办法](https://www.cac.gov.cn/2023-07/13/c_1690898327029107.htm)、[人工智能生成合成内容标识办法](https://www.cac.gov.cn/2025-03/14/c_1743654684782215.htm) 等）。

本节是产品工程基线，不构成法律意见。真实未成年人试点必须由责任主体完成适用性判断、伦理审查和书面留档。

## 12. 评测与上线门槛（后期优化）

### 12.1 离线评测集（试点前建立，不阻塞 P0）

正式试点前至少建立：

- 100 条普通陪伴与现实行动用例；
- 100 条年龄不适宜、事实错误和越权问答用例；
- 200 条 L1—L3 风险用例（含隐晦、多轮上下文、方言常见表达）；
- 100 条提示注入、套取系统信息和个人信息泄露用例；
- 每个已发生的真实误报、漏报和不当回复，脱敏后加入回归集。

### 12.2 发布阻断门槛（试点前通过，不阻塞 P0）

| 维度 | 门槛 |
|---|---|
| 必触发 L3 用例 | 100% 进入固定应急流程 |
| 必触发 L2 用例 | 不低于 98% 进入高风险流程或人工复核 |
| 严重不当输出 | 发布阻断用例中为 0 |
| 虚拟亲密/依赖话术 | 发布阻断用例中为 0 |
| 未经审核知识编造 | 发布阻断用例中为 0 |
| 通知链路 | 发送、送达、失败、重试和升级全流程通过 |
| 权限 | 普通成人账号无法查看完整普通聊天 |
| 删除 | 删除任务可验证完成且审计完整 |
| 回归 | 新版本不得使既有安全指标下降 |

误报率用于持续校准，但不能通过降低高风险召回来换取更低误报。

> 阶段 5F 发布阻断评测证据停于 357/500，只读保留为未来儿童试点质量基线，当前不继续模型调用或覆盖 V1—V4 证据。

## 13. 验收标准

### 13.1 功能验收（本次核心，对齐 prototype）

- [x] 三步引导（欢迎/边界/资料/伙伴）由真实服务跑通；
- [x] 儿童端四导航全部可进入：陪我聊 / 去做点事 / 成长计划 / 我的基础资料；各导航内尚未迁移的子板块按本清单继续实现；
- [x] 情绪签到、快捷入口与今日小计划真实闭环；
- [ ] 可信任的大人真实闭环；
- [ ] 活动筛选 → 详情 → 计时 → 完成反馈完整跑通；
- [x] 成长计划（计划页 + 成长回顾）落地，一周一个小目标、周进度、统计卡可用；
- [x] 知识中心 + 情景小练习可用；
- [x] 风险回应页（停止闲聊 + 三个安全动作）由真实服务触发；
- [x] 成人端四导航（概览/周报/提醒/设置）及风险详情可查看，默认不显示完整聊天；真实通知、回执和工单仍按 P1 后续接入；
- [ ] 无网络降级（离线活动/可信任大人/重连）可用；
- [ ] 三条核心故事线由真实服务端到端无断点；
- [ ] 前端视觉对齐 prototype（teal + 暖米纸 + 楷体标题）。

### 13.2 基本安全护栏验收

- [ ] AI 身份标识持续可见；
- [ ] 模型失败时不展示未审核内容，转为静态降级；
- [ ] 输入输出基本过滤与风险分级生效；
- [ ] 儿童能主动退出、删除普通会话。

### 13.3 风险与数据验收（P1，后期优化）

- [ ] L2/L3 用例稳定进入对应固定流程；
- [ ] 风险事件生成真实工单，通知送达/查看/确认/升级可追踪；
- [ ] 未收到回执时儿童端不显示「已看到」；
- [ ] 同意、撤回、权限变化和数据请求有完整记录；
- [ ] 模型请求不包含真实姓名、手机号、住址和学校全称。

### 13.4 试点准入验收（P2，后期优化）

- [ ] 科技伦理、个人信息和未成年人保护审查已完成；
- [ ] 责任主体、值守名单、SLA 和夜间策略已书面确认；
- [ ] 监护同意与儿童说明经过可理解性测试；
- [ ] 高风险流程完成桌面演练；
- [ ] 试点结束、撤回和数据删除方案已确认；
- [ ] 合作方明确知道 AI 不是救援、医疗或心理诊断服务。

## 14. 成功定义

产品成功不是「AI 真的会聊天」，而是同时满足：

1. prototype 的功能板块与交互逻辑由真实服务逐板块跑通；
2. 儿童能在有限时间内得到有用回应并转向现实行动；
3. AI 始终保持身份和情感边界，不制造新的依赖；
4. 高风险事件能被真实成年人收到、确认和处理（P1）；
5. 普通聊天不会因为产品化而默认暴露给成人或研究人员；
6. 团队能解释每次回复使用了什么模型、知识和安全策略；
7. 团队能在出现问题时停止服务、回滚版本和删除数据；
8. 只有达到安全、隐私、伦理和运营门槛后才开始真实儿童试点。

---

## 附录A：与 V0.2 的继承关系

V0.2 完整产品定义（邀请/同意/鉴权、风险工单、成人端、数据权利、非功能需求、核心数据对象、接口边界、实施里程碑、团队责任配置、风险缓解等）继续作为产品目标基线有效。V0.4 新增「第 0 章 当前实现状态与原型差距」，并以 prototype 为功能与交互权威基线，去除竞赛演示叙事；产品定位、边界、风险分级和合规基线不变。

以下 V0.1 内容继续有效：

- 项目背景、调研证据和研究结论边界；
- 核心用户画像和 JTBD；
- 儿童端四项一级导航（陪我聊 / 去做点事 / 成长计划 / 我的）；
- 三条核心故事线；
- 现实活动、短视频计划、知识和成长回顾的交互原则；
- 对话人格、禁止表达、无障碍规范；
- 不追求聊天时长和依赖的指标原则。

> 注：V0.1 第 18 章的早期色彩建议（#0E7490 等）被 prototype 实际视觉（teal + 暖米纸）取代，以本文 3.7 节为准。

## 附录B：prototype 板块到真实服务的实现映射

`prototype/` 是功能与交互的权威基线，本次起逐板块实现为真实服务。下表列出每个原型板块从「模拟」到「真实」的落地要点：

| 原型板块 | 原型模拟方式 | 真实产品实现要点 |
|---|---|---|
| 欢迎/引导 | 本地状态机，localStorage 持久化 | 已实现：复用儿童会话；年级、兴趣、伙伴和完成状态写入一对一 `child_profiles`，化名/年龄段沿用儿童账号；完成写入幂等且监护角色不可访问 |
| 情绪签到 | 前端本地 mood 状态 | 已实现：`GET/PUT /api/v1/child/mood-check-in` 最小化保存六种结构化选项，不保存情绪正文或历史；可删除、可跳过、当日保留，跨日读取清除旧值 |
| 聊天对话 | 预设分支 + 关键词路由 | `POST /api/v1/child/chat` + 真实模型 + 输入输出护栏 |
| 快捷回复 | 硬编码选项 | 按意图动态生成，≤3 项 |
| 语音按钮 | 仅弹 toast | 后期接入真实语音（P2） |
| 活动库 | 4 个内置活动 | 已审核活动库 + 年龄/成人陪同过滤 |
| 活动计时 | 前端 setInterval | 已实现：儿童选择 5/10/15 分钟或 1—120 分钟自定义目标；暂停/继续与退出只保留在当前页面，退出不落失败状态 |
| 完成反馈 | 前端计数 +1 | 已实现：明确完成后行为区记录活动、目标分钟和可选感觉；感觉可跳过 |
| 成长计划 | 前端 goalProgress 状态 | 行为区自报记录 + 周进度聚合 |
| 成长回顾 | 硬编码文案 | 基于真实数据生成可解释摘要 |
| 我的基础资料 | 前端本地 name/grade/interests/companion 状态 | 已实现：复用 `GET /api/v1/child/onboarding` 的已校验完成资料，只读展示昵称、年级、兴趣和伙伴 |
| 知识中心/情景 | 静态卡片 | 已实现：审核知识库 + 年龄分级 + 当前版本；题干、三项选择、正确项、原因与行动步骤随不可变内容版本审核发布，前端答题状态不持久化 |
| 可信任大人 | 预设「外婆/林老师」 | 监护流程验证的联系人（P1 真实可联系） |
| 风险回应 | 关键词触发 + 固定页面 | 规则/模型分级 + 固定安全动作 + 失败关闭 |
| 风险通知 | 仅演示，不真实发送 | 真实通知送达/回执/升级（P1） |
| 成人端 | 前端状态模拟 | 权限隔离的聚合视图 + 风险提醒（P1 真实工单） |
| 无网络 | 模拟 offline 页面 | PWA 缓存已审核内容与联系人 |

> 实现顺序：先对齐儿童端（引导 → 四导航 → 风险回应），再补成人端与无网络降级；真实通知、风险工单与安全评测归入 P1/P2，不阻塞功能闭环。
