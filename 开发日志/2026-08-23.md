# 开发日志 2026-08-23

> 项目：小伴AI陪伴智能体
> 当前阶段：参见docs/07_EXECUTION_ROADMAP.md

## 记录

<!-- dev-log:2026-08-23:manual -->
### 00:10

**摘要**

- 删除竞赛演示功能，只做产品：移除成人竞赛聊天/内容入口、离线/在线演示服务器、故障演练脚本、竞赛契约与竞赛文档；生成开关命令与合成内容种子去「demo」命名保留为开发验收能力；新建 V0.4 产品版 PRD，重写 README 与交接/索引/路线文档。

**已完成**

- 后端删除 `apps/api/src/demo/` 下竞赛聊天/内容服务、路由、运行时、在线演示事务、模型故障演练与冒烟脚本，及根目录临时诊断脚本；从 app.ts/server.ts 摘除竞赛依赖，并把本地测试账号从竞赛运行时判断中解耦为独立「非生产回环」条件。
- 前端删除 `CompetitionDemoChat.tsx`、`competition-demo-chat.ts/css/test.ts` 与「进入竞赛演示」入口及样式；保留产品安全预览 `InternalSafetyPreview` 与轻登录。
- 删除 `packages/contracts` 竞赛契约（competition demo chat/content schema 与类型），保留产品契约（childChat、chatTurn、GENERATION_STOP_FIXED_REPLY 等）。
- 去 demo 命名：生成开关 CLI 移入 `safety/generation-control-cli.ts`，命令 `demo:generation:on/off` → `generation:on/off`；合成内容种子 `demo-content-seed.ts` → `dev-content-seed.ts`、`seed-demo.ts` → `seed-dev.ts`，导出符号与文案去「竞赛」，命令 `db:seed:demo` → `db:seed:dev`。
- 删除 `scripts/demo/` 整目录与根/API `package.json` 的 `demo:start/verify/rehearse/smoke:chat/stop` 脚本。
- 删除 `docs/14_COMPETITION_DEMO_STANDARD.md`、`docs/15_COMPETITION_DEMO_CHECKLIST.md`、`AI陪伴智能体_PRD_V0.3_竞赛演示版.md`。
- 新建 `AI陪伴智能体_PRD_V0.4_产品版.md`（含第 0 章「当前实现状态」对齐）；重写 README、docs/00_INDEX、docs/16_AGENT_HANDOVER；更新 agent.md、CLAUDE.md、docs/05_DEVELOPMENT_WORKFLOW、docs/07_EXECUTION_ROADMAP（去竞赛轨道与 D0—D4）。

**修改文件**

- apps/api/src/app.ts、apps/api/src/server.ts
- apps/api/src/safety/generation-control-cli.ts（新建，自 demo/ 移入）
- apps/api/src/database/dev-content-seed.ts、seed-dev.ts、dev-content-seed.integration.test.ts（自 demo-content-seed 改名）
- apps/api/package.json、package.json
- packages/contracts/src/index.ts、index.test.ts
- apps/web/src/main.tsx、App.tsx、styles.css
- AI陪伴智能体_PRD_V0.4_产品版.md（新建）
- README.md、agent.md、CLAUDE.md
- docs/00_INDEX.md、docs/05_DEVELOPMENT_WORKFLOW.md、docs/07_EXECUTION_ROADMAP.md、docs/16_AGENT_HANDOVER.md
- 开发日志/2026-08-23.md

**验证**

- pnpm typecheck 三包通过；pnpm test（contracts/api/web）通过；pnpm test:integration 通过；pnpm build 生产构建通过。
- grep 复核：业务代码与文档中无 `竞赛演示`、`competitionDemo`、`demo:start`、`demo:verify` 残留（历史开发日志与 V0.2 历史段落除外）。

**待办**

- 浏览器冒烟：`pnpm dev` 打开首页无「进入竞赛演示」入口；轻登录 → 去做点事 → 陪我聊正常；`pnpm generation:on` 后真实回复，结束 `pnpm generation:off` 恢复关闭。
- 开发库旧合成内容 `source_label`/`review reason` 文案已去「竞赛」，与库内旧数据不匹配，重跑 `db:seed:dev` 会触发冲突；需重建开发库合成数据（回滚后迁移+种子，或手动删旧合成内容再种）。

**阻塞与风险**

- 工作区不是 Git 仓库，删除操作不可逆；本次以文件清单复核、类型检查、测试、构建与 grep 残留扫描作为证据。
- slug `demo-screen-break-guide` / `demo-five-minute-indoor-observation` 与常量 `DEMO_INVITATION_CODE`（值 `XIAOBAN-DEMO-2026-02`）保留不变，属内部标识，不构成竞赛演示功能。
