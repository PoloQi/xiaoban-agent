# 开发日志 2026-09-17

> 项目：小伴AI陪伴智能体  
> 当前阶段：产品开发轨道 / 阶段6A（通知、成人端与风险工作台）；5F/儿童试点安全评测按2026-09-16决定暂停而非通过  

## 记录

<!-- dev-log:2026-09-17:manual-1 -->
### 10:44

**摘要**

- 阶段6A.6完成：在无网络、仅合成数据边界内打通本地模拟成功回执链`attempted→delivered→viewed→acknowledged`，并实现站内/站外双通道确认聚合——单通道确认只显示等待，双通道均确认工单才为`acknowledged`；不代表阶段6退出门槛达成。

**已完成**

- 新增`RiskLocalReceiptRunner`（apps/api/src/tickets/risk-local-receipt-runner.ts）：短事务内读取合成工单，按通道当前状态推进单条成功回执（attempted→record_delivered、delivered→record_viewed、viewed→record_acknowledged），转换复用6A.1状态机与`RiskTicketStore.applyEvent`的行锁、事件哈希幂等和outbox更新。
- 每个送达/查看/确认回执使用独立requestId，同requestId重放返回同一回执并标记`replayed:true`；返回固定`simulated:true`、`networkCallMade:false`。
- 双通道隔离：in_app的回执不推进off_site_backup；单通道确认工单为`waiting_for_acknowledgement`，两通道均`acknowledged`才聚合为`acknowledged`。
- 通道已确认后的新requestId安全空转（`recorded:false`、`reason:channel_already_acknowledged`）；`not_sent/failed/timed_out`通道拒绝成功回执，保持“未attempted不得delivered、未viewed不得acknowledged”的顺序门槛；未知工单/非法通道/非法ID/多余字段统一拒绝且不追加事件。
- 不新增迁移、不新增业务表、不注册API、不启动常驻worker或调度器；事件仍追加式、触发器禁止UPDATE/DELETE。
- 同步docs/00、docs/07、docs/02、docs/03的6A.6范围、验证与安全边界（未勾选阶段6退出门槛）。

**修改文件**

- apps/api/src/tickets/risk-local-receipt-runner.ts：新增本地模拟成功回执记录器。
- apps/api/src/tickets/risk-local-receipt-runner.integration.test.ts：新增4项MySQL集成测试（双通道全链路+聚合+幂等、通道隔离、终态/未尝试拒绝、非法请求不写事件）。
- docs/00_INDEX.md、docs/07_EXECUTION_ROADMAP.md、docs/02_TECHNICAL_ARCHITECTURE.md、docs/03_SAFETY_PRIVACY_STANDARD.md：同步6A.6。
- 开发日志/2026-09-17.md：新增本记录。

**验证**

- `node --env-file=../../.local/mysql-secrets/database.env ./node_modules/vitest/vitest.mjs run --config vitest.integration.config.ts src/tickets/risk-local-receipt-runner.integration.test.ts`（在apps/api）：1个文件、4/4通过。
- 6A.6+6A.5聚焦回归（risk-local-receipt-runner + risk-notification-attempt-runner）：2个文件、8/8通过。
- `pnpm test:integration`：20个文件、81/81通过（6A.5为19文件77/77，本切片新增4项）。
- `pnpm --filter @xiaoban/contracts test`：1个文件、78/78通过。
- `pnpm --filter @xiaoban/api test`：19个文件、119/119通过（离线单测数量未变，新逻辑仅集成覆盖）。
- `pnpm typecheck`：contracts、api、web三工作区通过（修复一处notification.status索引收窄）。
- `pnpm --filter @xiaoban/api build`：通过。
- 未发起外部网络请求；未调用模型；未覆盖V1—V7评测证据；本切片无新增迁移、无契约schema变更（回执事件与状态枚举6A.1已定义）。

**待办**

- 下一最小切片建议：通知故障的用户侧降级提示（仍无外部网络），或真实渠道适配前的渠道凭据/真实回执/幂等/回滚设计评审（须用户单独确认，不属于自动推进）。
- 退避调度器、SLA桌面演练、风险工作台API/UI、数据权利请求和阶段6总体验收仍未完成。

**阻塞与风险**

- delivered/viewed/acknowledged仅为本地合成状态建模，固定`simulated:true`、`networkCallMade:false`，不代表真实短信/邮件/站外消息/推送已送达，不代表监护人或值守人员已查看、确认。
- 真实渠道接入前仍需渠道凭据、真实回执、告警/值守、监控、审计、回滚和单独用户确认；“未回执不显示成功”的阶段6退出门槛未因此达成。
- 5F仍暂停而非通过，本任务未恢复模型评测。

<!-- dev-log:2026-09-17:phase6-console-A1 -->
### A1 · 15:44 阶段6风险工作台 切片1：WORKBENCH 契约

**摘要**

- Agent A 风险工作台开工，仅合成工单、只读通知边界；新增 `risk-console-2026-09-v1` 契约区块（纯追加，未改既有 risk-ticket 枚举字面量）。

**已完成**

- `packages/contracts/src/index.ts` 追加 WORKBENCH 区块：`RISK_CONSOLE_SCHEMA_VERSION`、list/detail 响应、claim/通用 action、note、resolve 请求 schema；每条通知固定 `simulated:true` 与 `networkCallMade:false`，顶层同设边界；备注/处置说明强制 `虚构处置：` 前缀（10—240字），工单仅 L2/L3。
- 列表项与详情均不含普通聊天正文、供应商原文、秘密、提示词、推理、危险候选或联系方式；通知只暴露 channel/status/attempts。
- 新增 4 项契约测试：合法列表（含无网络边界）、接单+备注详情、拒绝 networkCallMade:true/非合成/无前缀备注/过短处置、拒绝未知字段与非 L2/L3。

**修改文件**

- packages/contracts/src/index.ts（追加 WORKBENCH 区块与导出）
- packages/contracts/src/index.test.ts（追加 import 与 4 项测试）

**验证**

- `pnpm --filter @xiaoban/contracts test`：1 个文件、82/82 通过（6A.6 为 78/78，本切片 +4）。
- 未发起外部网络请求；未新增迁移、未改状态机与通知 runner。

**待办**

- 切片2：迁移 019（risk_tickets 追加 child_id/assignee 列 + 独立 append-only console notes 表与触发器）+ database/types 追加。
- 后续：console service（监护鉴权、接单/备注/解决/关闭，复用 RiskTicketStore）、routes 与 app/server 追加注册、RiskConsole UI、docs 追加。

**阻塞与风险**

- 现有 risk_tickets（016）无儿童/责任人归属字段，既有事件枚举无“接单/处置备注”，故需启用任务书预授权的 019 迁移（仅追加列与独立备注表，不改既有枚举/状态机/触发器）；018 已让给 Agent B，不占用。
- 所有通知/回执仍为本地合成状态，不代表真实送达或确认。

<!-- dev-log:2026-09-17:phase6-console-A2 -->
### A2 · 15:53 阶段6风险工作台 切片2：迁移019与数据库类型

**摘要**

- 启用任务书预授权的 019 迁移：给 `risk_tickets` 纯追加儿童归属/责任人可空列，并新建独立 append-only 工作台备注表；已先在 TASK_BRIEF.md 追加 019 与装配层边界说明。

**已完成**

- `TASK_BRIEF.md` 追加“执行补充说明（2026-09-17 Agent A）”：说明启用 019 的原因（016 无儿童/责任人列、事件枚举无接单/备注）、仅追加可空列与独立备注表、复用既有状态机与 `risk_ticket_events`，以及 types.ts/server.ts 的纯追加装配边界。
- 新增 `apps/api/src/database/migrations/019_risk_console_assignment.ts`：
  - `risk_tickets` 追加 `child_id`、`claimed_by_guardian_id`、`claimed_at`（均可空），加索引 `idx_risk_ticket_console(child_id,status,created_at)`、`idx_risk_ticket_assignee(claimed_by_guardian_id,status)`；
  - 新表 `risk_ticket_console_notes`（id/ticket_id/request_id/guardian_id/kind/note/created_at，request_id 唯一），kind 限 `claimed|disposition_note`，note 空或≥10字符；
  - 建 `trg_risk_console_note_no_update/no_delete` 触发器禁止改删；down 删除触发器/表/索引/列。
- `apps/api/src/database/types.ts` 纯追加：`RiskTicketsTable` 三列、新 `RiskTicketConsoleNotesTable` 接口、`DatabaseSchema.risk_ticket_console_notes`；未改既有接口语义。

**修改文件**

- TASK_BRIEF.md（追加执行补充说明）
- apps/api/src/database/migrations/019_risk_console_assignment.ts（新增）
- apps/api/src/database/types.ts（纯追加）

**验证**

- 先 `pnpm --filter @xiaoban/contracts build`（worktree 内 dist 缺失会导致迁移 import 失败，已补齐）。
- 在 xiaoban_test：019 up `Success`；`migrateDown` `Success`；再 `migrateToLatest` 重放 `Success`（up→down→up 均通过）。
- `pnpm --filter @xiaoban/api typecheck`：通过。
- 未发起外部网络请求；未改 outbox 租约、attempt/receipt runner、LocalSyntheticNotificationAdapter 或 016/017 触发器。

**待办**

- 切片3：`risk-console-service.ts`（复用监护核验查询做严格权限；列表/详情/接单/备注/解决/关闭；复用 RiskTicketStore.applyEvent；019 列与 notes 表读写）。

**阻塞与风险**

- 019 为可空追加列，018 让给 Agent B；若 Agent B 同时新增迁移需协调编号。
- 本地 MySQL 凭据仅在主仓库 `D:\code\agent\.local`，worktree 集成命令需用该绝对 env 文件（仅 127.0.0.1:3307，无外网）。
<!-- dev-log:2026-09-17:phase6-console-A3 -->
### A3 · 16:10 阶段6风险工作台 切片3：console service 与 MySQL 集成

**摘要**

- 新增 `RiskConsoleService`：监护鉴权下的工单列表/详情/接单/追加处置备注/解决/关闭；复用 `GuardianDashboardService` 同款监护核验与 `RiskTicketStore`/状态机，不重写状态机、不触发任何发送。

**已完成**

- `apps/api/src/tickets/risk-console-service.ts`：
  - 复用监护会话核验（role=guardian、未撤销、enrollment/consent/link/child/guardian 全部 active、relationship verified），儿童角色 403、无效令牌 401；
  - 工单按 `risk_tickets.child_id` 严格过滤到本户；他户工单详情/接单一律 `NOT_FOUND 404`（不暴露存在性），他户列表为空；
  - 接单：`FOR UPDATE` 行锁，仅 `unclaimed` 可接，幂等 requestId 重放；写入 `claimed_by_guardian_id/claimed_at` 并追加 `claimed` 备注；
  - 追加处置备注：仅接单本人、非 closed，写 append-only `disposition_note`，幂等重放且异载荷冲突；
  - 解决/关闭：仅接单本人，委托 `RiskTicketStore.applyEvent`（resolve 需 acknowledged/escalated，close 需 resolved）；按 requestId 判重后再写，避免服务端 occurredAt 变化造成的伪幂等冲突；
  - 详情只暴露风险等级/类别/状态/时间/通知 channel/status/attempts/处置时间线，通知固定 `simulated:true`、`networkCallMade:false`、`readOnly:true`；不含聊天正文、供应商原文、秘密、提示词、推理、危险候选或联系方式；
  - permissions 按 assigneeState 与 status 派生 canClaim/canAddNote/canResolve/canClose。
- `apps/api/src/tickets/risk-console-service.integration.test.ts`：4 项 MySQL 集成（回滚事务，不对 append-only 表做 DELETE）：本户列表隔离+无聊天字段；接单→备注→解决→关闭全链路与幂等重放+通知无网络边界+console notes 触发器禁止 UPDATE/DELETE；closed 拒绝追加备注、未回执拒绝解决；儿童403、他户列表空/详情与接单404、无效令牌401。

**修改文件**

- apps/api/src/tickets/risk-console-service.ts（新增）
- apps/api/src/tickets/risk-console-service.integration.test.ts（新增 4 项）

**验证**

- `pnpm --filter @xiaoban/api typecheck`：通过。
- 在 xiaoban_test 运行 `risk-console-service.integration.test.ts`：1 个文件、4/4 通过（含 console notes UPDATE/DELETE 触发器拒绝回归）。
- 未发起外部网络请求；未改 outbox 租约、attempt/receipt runner、LocalSyntheticNotificationAdapter 或 016/017 触发器。

**待办**

- 切片4：`risk-console-routes.ts` + `app.ts`/`server.ts` 追加注册；路由级 401/403/404/409 与 JSON Schema 校验。

**阻塞与风险**

- 工单 `child_id` 由迁移 019 追加可空列；当前生产链路尚无造单入口给该列赋值，本切片通过集成造数设置；后续真实造单需在安全事件建单时写入 child_id（不在本分支范围）。
- 通知状态仍为本地合成，只读展示，不代表真实送达/查看/确认。
<!-- dev-log:2026-09-17:phase6-console-A4 -->
### A4 · 16:21 阶段6风险工作台 切片4：HTTP 路由与应用装配

**摘要**

- 新增 `risk-console-routes.ts`，在 `app.ts`/`server.ts` 纯追加注册；统一安全语义：无凭证/坏凭证/撤销/儿童角色/跨户访问他人工单一律 403（不泄露工单存在性），请求体非法 400，状态/幂等冲突 409。

**已完成**

- `apps/api/src/tickets/risk-console-routes.ts`：注册
  - `GET /api/v1/risk-console/tickets`（本户列表）
  - `GET /api/v1/risk-console/tickets/:ticketId`（详情）
  - `POST .../:ticketId/claim`、`.../notes`、`.../resolve`、`.../close`
  - Fastify JSON Schema 约束 params/headers/body/response（服务层仍以 Zod 二次解析，不信任线上 JSON Schema）；备注/处置说明 wire 层强制 `虚构处置：` 前缀与 10—240 字；响应 schema 固化 synthetic/networkCallMade:false/readOnly。
- 安全语义统一：service 的 authenticate 对缺/坏/撤销凭证返回 `FORBIDDEN 403`，跨户详情/接单返回 `FORBIDDEN 403`（原 NOT_FOUND 改为 403，按任务书“未授权/儿童/他户监护人一律403”）；路由 authorization 头不再 schema required，缺头由 bearerToken 抛 403（避免 Fastify 400）。
- `apps/api/src/app.ts`：仅追加 import、`riskConsoleService?` 依赖字段与一段注册区块；`apps/api/src/server.ts`：仅追加 import 与 `riskConsoleService: new RiskConsoleService(database)`；未改任何既有行语义。
- 集成测试：
  - service 测试 4 项期望更新为 403 安全语义；
  - 新增 routes 集成 3 项（Fastify inject，回滚事务）：无/坏凭证与儿童 403；监护全链路 claim→notes（非法体 400）→resolve→close，响应含无网络边界且无 conversation；他户与未知工单 404 用例改为 403（含跨户 claim 403）。

**修改文件**

- apps/api/src/tickets/risk-console-routes.ts（新增）
- apps/api/src/tickets/risk-console-routes.integration.test.ts（新增 3 项）
- apps/api/src/tickets/risk-console-service.ts（安全语义 401/404→403）
- apps/api/src/tickets/risk-console-service.integration.test.ts（期望同步）
- apps/api/src/app.ts、apps/api/src/server.ts（纯追加注册/装配）

**验证**

- `pnpm --filter @xiaoban/api typecheck`：通过。
- xiaoban_test 运行 console service + routes 集成：2 个文件、7/7 通过。
- 未发起外部网络请求；未注册任何发送动作；未改 outbox/runner/adapter/016/017 触发器。

**待办**

- 切片5：`apps/web/src/RiskConsole.tsx` + 独立 css，在成人端导航内挂载入口（App.tsx 追加 RiskConsole 区块）；375/768 双视口、设计令牌、只读通知边界、无聊天正文。
- 切片6：docs 00/02/07 追加工作台小节；全量验证（contracts/api 离线、全量 MySQL 集成、三工作区 typecheck、api/web build）。

**阻塞与风险**

- 401→403 是按任务书权限矩阵显式收敛；若后续需区分“未登录/已登录无权限”，应在另一任务评估，本分支不改。
- 通知仍只读本地合成状态，无重发入口，不代表真实送达。

<!-- dev-log:2026-09-17:phase6-console-A5 -->
### A5 · 19:43 阶段6风险工作台 切片5：成人端UI接入与独立样式

**摘要**

- 完成 A5 前端收尾并接入成人端：`App.tsx` 成人会话挂载 `RiskConsole`；监护主页提供“打开风险工单工作台”入口，列表/详情使用独立顶栏与 `risk-console.css`。

**已完成**

- `apps/web/src/App.tsx`：仅替换成人会话挂载，从 `GuardianDashboard` 改为 `RiskConsole`（其余儿童引导/内容路径不变）。
- `apps/web/src/RiskConsole.tsx`：dashboard 视图直接复用 `GuardianDashboard` 并叠加工作台入口，避免双顶栏；列表/详情使用独立工作台顶栏；新增 `categoryLabel` 中文类别、通知状态中文展示；接单/追加记录/解决/关闭严格按服务端 permissions 禁用；只读通知区持续展示 simulated · networkCallMade:false，无重发入口。
- `apps/web/src/risk-console.css`（新增，299行）：teal/暖米纸设计令牌、16–28px手账圆角、最小触控44px、加载/空态/错误/列表/详情/表单/焦点态/减少动态效果，375px 与 768px 双视口无横向溢出。

**修改文件**

- apps/web/src/App.tsx、apps/web/src/RiskConsole.tsx、apps/web/src/risk-console.css（新增）。

**验证**

- `pnpm --filter @xiaoban/web typecheck`：通过（初次因 contracts dist 未构建导致模块解析失败，构建 contracts 后通过；另有1处 `aria-selected` 类型收窄，已修正）。
- `pnpm --filter @xiaoban/web test`：5个文件、11/11通过。
- `pnpm --filter @xiaoban/web build`：通过（135 modules，dist 产物正常）。

**待办**

- 切片6：docs 00/02/07 追加工作台小节；全量验证。
- 未做手机/平板真实浏览器视口人工走查（当前以构建与响应式CSS为准，列入风险）。

**阻塞与风险**

- 前端仅消费合成工单；生产造单链路尚无 child_id 赋值入口（由后端集成造数），真实使用前需在安全事件建单处写入，属后续切片。
- 无真实发送/回执，工作台不触发重发。

<!-- dev-log:2026-09-17:phase6-console-A6 -->
### A6 · 19:43 阶段6风险工作台 切片6：文档同步与全量验证

**摘要**

- 同步 docs/00、docs/02、docs/07 风险工作台段落（追加式），并完成任务书要求的全量验证；修复019迁移后数据库基线表计数断言（27→28）。

**已完成**

- `apps/api/src/database/database.integration.test.ts`：019 新增 `risk_ticket_console_notes` 后业务表由27增至28，基线期望同步为28（A2遗漏项，全量集成首次运行暴露）。
- docs/00：在6A状态新增“阶段6A.7（Agent A风险工作台）”事实行，明确合成、403矩阵、只读通知边界、88/88等验证与未完成项；不勾选阶段6退出门槛。
- docs/02：在6A.6后追加“阶段6A.7风险工作台（synthetic-only workbench）”小节，覆盖契约/019迁移/服务鉴权/HTTP路径/前端/验证。
- docs/07：阶段6状态行更新为6A.7工作台API/UI完成；在6A.6后追加第7条6A.7（边界/能力/验证/未完成），阶段6退出门槛保持未勾选。

**修改文件**

- docs/00_INDEX.md、docs/02_TECHNICAL_ARCHITECTURE.md、docs/07_EXECUTION_ROADMAP.md；
- apps/api/src/database/database.integration.test.ts（基线计数）；
- 开发日志/2026-09-17.md（本记录）。

**验证**

- `pnpm --filter @xiaoban/contracts test`：1个文件、82/82通过。
- `pnpm --filter @xiaoban/api test`（离线）：19个文件、119/119通过。
- 全量MySQL集成（`node --env-file=D:\code\agent\.local\mysql-secrets\database.env ./node_modules/vitest/vitest.mjs run --config vitest.integration.config.ts`，apps/api）：修复基线计数后22个文件、88/88通过（首次运行87/88，唯一失败为表计数28 vs 27，已修复并复跑全绿）。
- `pnpm typecheck`：contracts/api/web 三工作区通过。
- `pnpm --filter @xiaoban/api build`：通过；`pnpm --filter @xiaoban/web build`：通过。
- 权限回归覆盖：无/坏凭证、儿童角色、他户监护人访问与操作均403（routes/service 集成）；append-only console notes 触发器拒绝 UPDATE/DELETE。
- 未发起外部网络请求、未调用模型、未改 outbox/attempt/receipt runner/LocalSyntheticNotificationAdapter、未覆盖 V1–V8 证据。

**待办**

- 与 Agent B 6A.7（通知故障降级）分支按顺序合并（B 先 A 后），冲突仅保留双方 app.ts/App.tsx/contracts 追加区块；合并后统一做 SLA 桌面演练。
- 生产造单链路写入 child_id；真实渠道/真实回执/退避调度器/数据权利请求仍需单独确认。

**阻塞与风险**

- 6A.7 仅证明合成工单的工作台闭环与权限边界，不代表真实通知送达或阶段6退出门槛达成；真实渠道、回执、值守SLA与数据权利链路未完成。
- 未做真实设备/浏览器 375/768 人工视觉走查；未推送/未发起合并，等待合并顺序确认。

<!-- dev-log:2026-09-17:B-1 -->
### B-1 19:28 · 6A.7 通知故障用户侧降级（切片1/3）

**摘要**

- 在无外部网络、仅合成数据边界内，新增独立通知状态降级纯函数与展示组件；儿童风险收尾页与成人风险详情对未回执/送达失败/超时/升级统一显示安全中性提示，未回执绝不显示成功，且不透传内部异常。

**已完成**

- 新增 `apps/web/src/notice-status.ts`：把工单/通道状态（failed/timed_out/escalated/确认窗口内未回执）映射为 delivery_unavailable / acknowledgement_pending / local_acknowledged 三种安全语气；双通道均 acknowledged 且工单已确认才允许“本地演练回执已记录”，并固定标注不代表真实渠道送达。
- 新增 `notice-status-banner.tsx`、`notice-status.css`：独立降级提示组件，不改动既有通知 runner/store/契约。
- `ChildRiskSent.tsx` 最小挂载儿童侧提示（当面告诉可信任大人）；`GuardianDashboard.tsx` 风险详情最小挂载监护侧提示（可稍后查看/当面联系）。
- 未接真实短信/邮件/站外/推送，未启动 worker，未改 6 个既有 runner/adapter/store，未改共享契约枚举。

**修改文件**

- apps/web/src/notice-status.ts、notice-status.test.ts、notice-status-banner.tsx、notice-status.css：新增。
- apps/web/src/ChildRiskSent.tsx、apps/web/src/GuardianDashboard.tsx：最小挂载。
- 开发日志/2026-09-17.md：追加本记录。

**验证**

- `pnpm --filter @xiaoban/web test`：6 个测试文件、14/14 通过（含新增 notice-status 4 类状态不出现成功文案用例）。
- `pnpm --filter @xiaoban/web typecheck`：通过。
- `pnpm --filter @xiaoban/web build`：通过（135 modules）。
- 未发起任何外部网络请求；仅前端纯逻辑与静态构建。

**待办**

- 6A.8 本地退避调度演练 runner（确定性、复用 RiskOutboxClaimWorker 门槛、不启动常驻 worker）。
- 6B.1 数据权利请求模块与迁移 018。

**阻塞与风险**

- 本次仅为用户侧降级文案与本地状态映射，不代表真实通知链路、真实送达/查看/确认达成；真实渠道接入仍需单独确认。
- 成人 dashboard 当前数据源仅含 not_sent 合成提醒，failed/timed_out/escalated 的页面分支由纯函数单测覆盖，尚未接入真实工单读取接口（属后续切片，不在本切片扩接口）。

<!-- dev-log:2026-09-17:B-2 -->
### B-2 19:49 · 6A.8 本地退避调度演练（切片2/3）

**摘要**

- 在无外部网络、不启动常驻 worker/定时器的前提下，新增确定性 `RiskBackoffScheduleDrillRunner`，复用 `RiskOutboxClaimWorker` 的领取门槛（不复制退避 SQL）演示“首次失败→退避窗口内不重领同一行→到期重领同一失败行→二次失败升级”的完整调度判断，全程固定 `networkCallMade:false`、不发送、不送达。

**已完成**

- 新增 `apps/api/src/tickets/risk-backoff-schedule-drill-runner.ts`：
  - 组合 `RiskOutboxClaimWorker` + `RiskNotificationAttemptRunner` + `LocalSyntheticNotificationAdapter`（强制本地模拟失败），不复制 claim 退避条件。
  - 先占用备份通道租约，确保演练针对“同一条失败行”而非切换到另一通道；首次失败后释放租约（沿用 6A.5），退避阈值前 `claimed:false`，到期（`failed_at <= now-1000ms`）重领同一 outbox，第二次失败经 `record_timed_out` 聚合为工单 `escalated`。
  - 固定返回 `simulated:true`、`networkCallMade:false`、`sent:false`、`delivered:false`，最终通道 `timed_out/attempts:2`、工单 `escalated`，并显式给出四个步骤布尔值。
  - 无到期行时返回 `scheduled:false/reason:no_due_row` 且不写事件；非法 requestId/workerId/leaseDurationMs 拒绝。
- 未改既有 6 个 runner/adapter/store 文件；不启动常驻 worker、定时器或调度器；不接外部渠道。
- 同步 docs/00、docs/02、docs/03、docs/07 的 6A.7 通知降级与 6A.8 退避演练小节（标注 2026-09-17），不勾选阶段6退出门槛。

**修改文件**

- apps/api/src/tickets/risk-backoff-schedule-drill-runner.ts、risk-backoff-schedule-drill-runner.integration.test.ts：新增（3 项 MySQL 集成测试）。
- docs/00_INDEX.md、docs/02_TECHNICAL_ARCHITECTURE.md、docs/03_SAFETY_PRIVACY_STANDARD.md、docs/07_EXECUTION_ROADMAP.md：追加 6A.7/6A.8 小节。
- 开发日志/2026-09-17.md：追加本记录。

**验证**

- 6A.8 聚焦 MySQL 集成：1 个文件、3/3 通过。
- 退避+6A.5+6A.3 聚焦回归（backoff drill + notification attempt + outbox claim）：3 个文件、12/12 通过。
- `pnpm --filter @xiaoban/api typecheck`：通过。
- `pnpm --filter @xiaoban/api build`：通过。
- 未发起任何外部网络请求；未启动常驻 worker/定时器；无新增迁移、无契约变更。

**待办**

- 6B.1 数据权利请求：datarights 模块、迁移 018（应用→回滚→重放、审计禁改禁删触发器）、删除后儿童自由功能按权限停止。
- 完成后跑全量契约/API 离线/MySQL 集成/三工作区 typecheck/API 与 Web 构建。

**阻塞与风险**

- 本 runner 是可重放的本地演练，不代表存在真实调度器或真实重试；`timed_out/escalated` 仅为本地合成状态，不代表真实渠道失败或真实值守介入。
- 真实渠道适配、真实回执、告警/值守与 SLA 桌面演练仍不在本分支范围，须合并后另行确认。

<!-- dev-log:2026-09-17:B-3 -->
### B-3 20:50 · 6B.1 数据权利请求（切片3/3）

**摘要**

- 在无外部网络、仅本地合成数据边界内，新增监护人数据权利请求最小闭环：`POST /api/v1/guardian/data-rights/requests` 提交 delete/export；018 迁移新增请求表与追加式、禁改禁删的事件表。导出仅排队、删除为“功能性删除”（停用/撤回/撤销会话），不物理擦除历史与审计证据。

**已完成**

- 新增 `apps/api/src/datarights/data-rights-service.ts`：监护人 Bearer 鉴权；校验 active guardian/enrollment/consent/child 与 verified 且未停用绑定；requestId+请求哈希幂等，异载荷返回 `IDEMPOTENCY_CONFLICT`(409)、儿童令牌 403、未确认/非法请求 400。
- export：请求置 `queued`、儿童保持 active，写 `request_received`、`export_queued` 事件与 `data_rights.export_queued` 审计，不生成任何导出文件。delete：请求置 `completed`，撤回监护同意（记录 reason/requestId）与 enrollment、停用 child 账号与监护绑定、撤销未撤销的 child 会话，写 `functional_deletion_completed` 事件及 `data_rights.functional_deletion_completed`、`child_account.deactivated` 两条审计。
- 新增迁移 `018_datarights_requests.ts`：`data_rights_requests`、`data_rights_request_events`；事件表 `trg_data_rights_event_no_update`、`trg_data_rights_event_no_delete` 触发器禁改禁删，固定 `synthetic=TRUE` 检查；事件以 `(data_rights_request_id,sequence_no)` 与 `(data_rights_request_id,action)` 唯一约束，`sequence_no` 稳定表达同事务内追加顺序；down 先删触发器再删表。
- 共享契约 `packages/contracts` 追加 `DATA_RIGHTS_SCHEMA_VERSION` 与 request/response schema（仅追加，不改既有枚举）；`app.ts` 注册路由、`server.ts` 装配服务、`database/types.ts` 追加两表类型。
- 集成测试 4 项：export 排队+幂等重放、delete 功能性停用/撤回/撤销会话+事件禁改禁删+审计、未确认/儿童/异载荷冲突、Fastify inject 的 HTTP 201 响应契约与儿童 403。
- 同步 docs/00、02、03、07、08 的 6B.1 段落与本地 MySQL 说明。

**修改文件**

- apps/api/src/datarights/data-rights-service.ts、data-rights-routes.ts、data-rights-service.integration.test.ts：新增。
- apps/api/src/database/migrations/018_datarights_requests.ts：新增。
- apps/api/src/database/types.ts、apps/api/src/app.ts、apps/api/src/server.ts、apps/api/src/database/database.integration.test.ts：最小追加/基线断言调整。
- packages/contracts/src/index.ts：追加数据权利契约区块。
- docs/00_INDEX.md、02_TECHNICAL_ARCHITECTURE.md、03_SAFETY_PRIVACY_STANDARD.md、07_EXECUTION_ROADMAP.md、08_LOCAL_MYSQL_DEVELOPMENT.md：追加 6B.1 说明。
- 开发日志/2026-09-17.md：追加本记录。

**验证**

- 018 迁移在 xiaoban_test（迁移账号）直接调用同一份迁移函数完成 应用→回滚（断言两表与触发器消失）→重放（断言重建）：rollback ok / replay ok。
- 6B.1 聚焦 MySQL 集成：1 个文件、4/4 通过（含 HTTP 201/403）。
- 全量 MySQL 集成：22 个文件、88/88 通过。
- `pnpm --filter @xiaoban/contracts test`：1 个文件、78/78 通过；contracts build 通过。
- `pnpm --filter @xiaoban/api test`：19 个文件、119/119 通过（离线）。
- `pnpm typecheck`：contracts/api/web 三工作区通过。
- `pnpm --filter @xiaoban/api build`、`pnpm --filter @xiaoban/web build`（135 modules）：通过。
- 未发起外部网络请求；未接短信/邮件/微信/推送；未存真实联系方式；未启动 worker/定时器；临时迁移脚本均已删除，未提交。

**待办**

- 真实导出文件生成与交付、真实渠道与回执、物理删除/数据保留策略评审、SLA 桌面演练、风险工作台（Agent A）合并后做阶段 6 总体验收。
- 待两分支合并或协调干净 xiaoban_test 后，重跑标准迁移器命令做完整迁移验证。

**阻塞与风险**

- 标准迁移器（migrate.ts，Kysely Migrator）在共享 xiaoban_test 库报 “previously executed migration 019_risk_console_assignment is missing”：该库已存在并行 Agent A 分支的 019 执行记录/对象，而本分支文件系统只到 018，Migrator 拒绝运行；这不是 018 DDL 失败。本分支未复制 019、未删除并行对象；018 已用直接调用同一份迁移函数完成应用→回滚→重放。不得把标准迁移命令记为通过。
- 当前 delete 为“功能性删除”（撤回同意/报名、停用儿童与绑定、撤销儿童会话以阻止继续自由功能），不物理擦除历史合成数据与不可变审计证据；export 仅排队、不生成真实导出文件。若后续合规要求物理擦除全部历史，需先做数据保留/审计冲突评审并单独确认。
- 本切片为本地合成演练，不代表真实通知链路、真实数据权利履约或阶段 6 退出门槛达成；5F 仍暂停而非通过，本任务未恢复模型评测、未进入阶段 7。

---

## 21:36 阶段6 分支合并：main(Agent A 风险工作台) × notice-resilience(6A.7/6A.8/6B.1)

**背景**：PR #1 先把 Agent A 的 `codex/phase6-risk-console` 合入 `origin/main`（merge 1479b30）。本次在隔离 worktree `D:\code\agent\.worktrees\phase6-integrate-20260917`、临时分支 `codex/phase6-integrate-20260917`（起于 origin/main 1479b30）合并真正的 B 分支 `origin/codex/phase6-notice-resilience`@56e62a7（6A.7 通知降级 3468e8e、6A.8 退避演练 ac6bf0b、6B.1 数据权利 56e62a7）。主工作区有未提交的 5F 改动，全程未触碰。

**编号冲突裁决**：两分支都占用了“6A.7”。保留 B 已推送编号 6A.7=通知故障用户侧降级、6A.8=退避调度演练、6B.1=数据权利；把 Agent A 风险工作台在合并后文档顺延为 **6A.9**（仅改文档阶段切片号；契约 schema 版本 `risk-console-2026-09-v1` 不变；历史日志原文保留）。

**冲突解决（双方追加区块均保留）**：

- `apps/api/src/app.ts`：保留双方 import 与两个路由注册块（risk-console、data-rights），各自正确闭合。
- `apps/api/src/server.ts`：保留 RiskConsoleService 与 DataRightsService 的 import 及两个 `new ...Service(database)` 装配。
- `apps/api/src/database/database.integration.test.ts`：SELECT 返回 tableName，基线断言统一为精确 **30** 张表，并断言含 018 两表 `data_rights_requests`、`data_rights_request_events` 与 019 `risk_ticket_console_notes`。
- `packages/contracts/src/index.ts`：RISK_CONSOLE 与 DATA_RIGHTS 两个仅追加区块都保留。
- `docs/07_EXECUTION_ROADMAP.md`：§8 状态行理顺；分批进度重排为 7=6A.7 通知、8=6A.8 退避、9=6B.1 数据权利、10=6A.9 风险工作台；把 B 误置于文件末尾(§11 之后)的三条移入 §8 并删除游离段落。
- `docs/00_INDEX.md`、`docs/02_TECHNICAL_ARCHITECTURE.md`：Agent A 风险工作台“6A.7”改注为 6A.9（保留“原 Agent A 6A.7、合并后顺延”溯源），B 的 6A.7/6A.8/6B.1 小节保持不变。
- `docs/08_LOCAL_MYSQL_DEVELOPMENT.md`：表计数由“本分支 001—018 共29、至少29”更新为合并后“001—019 共30、精确断言含 018 两表 + 019 一表”；迁移器风险段更新为合并后真实状态（见下）。
- `开发日志/2026-09-17.md`：A1—A6 与 B 各切片历史记录全部保留，仅追加本合并小节。
- 无冲突标记残留；顺手 trim 了 B 分支带入的 5 处 EOF 多余空行（drill runner ×2、ChildRiskSent、GuardianDashboard、00_INDEX），`git diff --check` 干净（仅 LF→CRLF 的 autocrlf 提示）。

**前端共存确认**：成人会话挂 `RiskConsole`，其 dashboard 视图内复用被 B 改过的 `GuardianDashboard`（含 `NoticeStatusBanner`），`ChildRiskSent.tsx` 含 child banner；两者已共存，未重复挂载。

**验证（合并后全量）**：

- `pnpm typecheck`：contracts/api/web 三工作区通过。
- contracts：build 通过；`test` 82/82 通过。
- api 离线：19 个文件、119/119 通过；`api build` 通过。
- web：`test` 6 文件、14/14 通过；`web build`（138 modules）通过。
- 全量 MySQL 集成：24 个文件、95/95 通过（`vitest run --config vitest.integration.config.ts`，凭据用绝对路径 `D:\code\agent\.local\mysql-secrets\database.env`，未打印凭据）。

**阻塞与风险（如实记录，未解决）**：

- 标准迁移器在共享 `xiaoban_test` 仍无法干净跑通：该库历史上被两并行分支分别“直接调用迁移函数 up”污染，`kysely_migration` 已登记 000—017 与 **019**，018 两表对象实际存在但**缺少 018 登记行**。曾尝试 INSERT 一行 018 登记，触发 Kysely 顺序校验（expected 019 at index 18 but 018 found），已回滚该 INSERT（DELETE 固定 timestamp 行），库恢复诊断前状态；未 DROP 任何对象/记录。
- 业务集成测试在事务内造数、依赖对象而非迁移登记，故 95/95 全过；但**不得**把“标准迁移命令通过”写入结论。彻底修复需协调/重建干净 xiaoban_test（破坏性，须用户单独授权）或由迁移负责人统一补齐 018 登记顺序。诊断临时脚本（tmp-*.mts）均已删除。
- 本合并仍为本地合成边界，不代表真实通知/数据权利履约或阶段 6 退出门槛达成；5F 维持搁置，未恢复模型评测、未进入阶段 7。

**待办**：向用户确认后再决定推送/更新 main 的方式（仅授权合并，尚未授权 push）。
