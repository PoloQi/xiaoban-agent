# 开发日志 2026-09-17

> 项目：小伴AI陪伴智能体  
> 当前阶段：产品开发轨道 / 阶段6A（通知、成人端与风险工作台）；5F/儿童试点安全评测按2026-09-16决定暂停而非通过  

## 记录

<!-- dev-log:2026-09-17:manual-1 -->
### 10:44

**摘要**

- 阶段6A.6完成：在无网络、仅合成数据边界内打通本地模拟成功回执链`attempted→delivered→viewed→acknowledged`，并实现站内/站外双通道确认聚合——单通道确认只显示等待，双通道均确认工单才为`acknowledged`；不代表阶段6退出门槛达成。

**已完成**

- 新增`RiskLocalReceiptRunner`（apps/api/src/tickets/risk-local-receipt-runner.ts）：短事务内读取合成工单，按通道当前状态推进单条成功回执（attempted→record_delivered、delivered→record_viewed、viewed→record_acknowledged），转换复用6A.1状态机与`RiskTicketStore.applyEvent`的行锁、事件哈希幂等和outbox更新。
- 每个送达/查看/确认回执使用独立requestId，同requestId重放返回同一回执并标记`replayed:true`；返回固定`simulated:true`、`networkCallMade:false`。
- 双通道隔离：in_app的回执不推进off_site_backup；单通道确认工单为`waiting_for_acknowledgement`，两通道均`acknowledged`才聚合为`acknowledged`。
- 通道已确认后的新requestId安全空转（`recorded:false`、`reason:channel_already_acknowledged`）；`not_sent/failed/timed_out`通道拒绝成功回执，保持“未attempted不得delivered、未viewed不得acknowledged”的顺序门槛；未知工单/非法通道/非法ID/多余字段统一拒绝且不追加事件。
- 不新增迁移、不新增业务表、不注册API、不启动常驻worker或调度器；事件仍追加式、触发器禁止UPDATE/DELETE。
- 同步docs/00、docs/07、docs/02、docs/03的6A.6范围、验证与安全边界（未勾选阶段6退出门槛）。

**修改文件**

- apps/api/src/tickets/risk-local-receipt-runner.ts：新增本地模拟成功回执记录器。
- apps/api/src/tickets/risk-local-receipt-runner.integration.test.ts：新增4项MySQL集成测试（双通道全链路+聚合+幂等、通道隔离、终态/未尝试拒绝、非法请求不写事件）。
- docs/00_INDEX.md、docs/07_EXECUTION_ROADMAP.md、docs/02_TECHNICAL_ARCHITECTURE.md、docs/03_SAFETY_PRIVACY_STANDARD.md：同步6A.6。
- 开发日志/2026-09-17.md：新增本记录。

**验证**

- `node --env-file=../../.local/mysql-secrets/database.env ./node_modules/vitest/vitest.mjs run --config vitest.integration.config.ts src/tickets/risk-local-receipt-runner.integration.test.ts`（在apps/api）：1个文件、4/4通过。
- 6A.6+6A.5聚焦回归（risk-local-receipt-runner + risk-notification-attempt-runner）：2个文件、8/8通过。
- `pnpm test:integration`：20个文件、81/81通过（6A.5为19文件77/77，本切片新增4项）。
- `pnpm --filter @xiaoban/contracts test`：1个文件、78/78通过。
- `pnpm --filter @xiaoban/api test`：19个文件、119/119通过（离线单测数量未变，新逻辑仅集成覆盖）。
- `pnpm typecheck`：contracts、api、web三工作区通过（修复一处notification.status索引收窄）。
- `pnpm --filter @xiaoban/api build`：通过。
- 未发起外部网络请求；未调用模型；未覆盖V1—V7评测证据；本切片无新增迁移、无契约schema变更（回执事件与状态枚举6A.1已定义）。

**待办**

- 下一最小切片建议：通知故障的用户侧降级提示（仍无外部网络），或真实渠道适配前的渠道凭据/真实回执/幂等/回滚设计评审（须用户单独确认，不属于自动推进）。
- 退避调度器、SLA桌面演练、风险工作台API/UI、数据权利请求和阶段6总体验收仍未完成。

**阻塞与风险**

- delivered/viewed/acknowledged仅为本地合成状态建模，固定`simulated:true`、`networkCallMade:false`，不代表真实短信/邮件/站外消息/推送已送达，不代表监护人或值守人员已查看、确认。
- 真实渠道接入前仍需渠道凭据、真实回执、告警/值守、监控、审计、回滚和单独用户确认；“未回执不显示成功”的阶段6退出门槛未因此达成。
- 5F仍暂停而非通过，本任务未恢复模型评测。

<!-- dev-log:2026-09-17:B-1 -->
### B-1 19:28 · 6A.7 通知故障用户侧降级（切片1/3）

**摘要**

- 在无外部网络、仅合成数据边界内，新增独立通知状态降级纯函数与展示组件；儿童风险收尾页与成人风险详情对未回执/送达失败/超时/升级统一显示安全中性提示，未回执绝不显示成功，且不透传内部异常。

**已完成**

- 新增 `apps/web/src/notice-status.ts`：把工单/通道状态（failed/timed_out/escalated/确认窗口内未回执）映射为 delivery_unavailable / acknowledgement_pending / local_acknowledged 三种安全语气；双通道均 acknowledged 且工单已确认才允许“本地演练回执已记录”，并固定标注不代表真实渠道送达。
- 新增 `notice-status-banner.tsx`、`notice-status.css`：独立降级提示组件，不改动既有通知 runner/store/契约。
- `ChildRiskSent.tsx` 最小挂载儿童侧提示（当面告诉可信任大人）；`GuardianDashboard.tsx` 风险详情最小挂载监护侧提示（可稍后查看/当面联系）。
- 未接真实短信/邮件/站外/推送，未启动 worker，未改 6 个既有 runner/adapter/store，未改共享契约枚举。

**修改文件**

- apps/web/src/notice-status.ts、notice-status.test.ts、notice-status-banner.tsx、notice-status.css：新增。
- apps/web/src/ChildRiskSent.tsx、apps/web/src/GuardianDashboard.tsx：最小挂载。
- 开发日志/2026-09-17.md：追加本记录。

**验证**

- `pnpm --filter @xiaoban/web test`：6 个测试文件、14/14 通过（含新增 notice-status 4 类状态不出现成功文案用例）。
- `pnpm --filter @xiaoban/web typecheck`：通过。
- `pnpm --filter @xiaoban/web build`：通过（135 modules）。
- 未发起任何外部网络请求；仅前端纯逻辑与静态构建。

**待办**

- 6A.8 本地退避调度演练 runner（确定性、复用 RiskOutboxClaimWorker 门槛、不启动常驻 worker）。
- 6B.1 数据权利请求模块与迁移 018。

**阻塞与风险**

- 本次仅为用户侧降级文案与本地状态映射，不代表真实通知链路、真实送达/查看/确认达成；真实渠道接入仍需单独确认。
- 成人 dashboard 当前数据源仅含 not_sent 合成提醒，failed/timed_out/escalated 的页面分支由纯函数单测覆盖，尚未接入真实工单读取接口（属后续切片，不在本切片扩接口）。

<!-- dev-log:2026-09-17:B-2 -->
### B-2 19:49 · 6A.8 本地退避调度演练（切片2/3）

**摘要**

- 在无外部网络、不启动常驻 worker/定时器的前提下，新增确定性 `RiskBackoffScheduleDrillRunner`，复用 `RiskOutboxClaimWorker` 的领取门槛（不复制退避 SQL）演示“首次失败→退避窗口内不重领同一行→到期重领同一失败行→二次失败升级”的完整调度判断，全程固定 `networkCallMade:false`、不发送、不送达。

**已完成**

- 新增 `apps/api/src/tickets/risk-backoff-schedule-drill-runner.ts`：
  - 组合 `RiskOutboxClaimWorker` + `RiskNotificationAttemptRunner` + `LocalSyntheticNotificationAdapter`（强制本地模拟失败），不复制 claim 退避条件。
  - 先占用备份通道租约，确保演练针对“同一条失败行”而非切换到另一通道；首次失败后释放租约（沿用 6A.5），退避阈值前 `claimed:false`，到期（`failed_at <= now-1000ms`）重领同一 outbox，第二次失败经 `record_timed_out` 聚合为工单 `escalated`。
  - 固定返回 `simulated:true`、`networkCallMade:false`、`sent:false`、`delivered:false`，最终通道 `timed_out/attempts:2`、工单 `escalated`，并显式给出四个步骤布尔值。
  - 无到期行时返回 `scheduled:false/reason:no_due_row` 且不写事件；非法 requestId/workerId/leaseDurationMs 拒绝。
- 未改既有 6 个 runner/adapter/store 文件；不启动常驻 worker、定时器或调度器；不接外部渠道。
- 同步 docs/00、docs/02、docs/03、docs/07 的 6A.7 通知降级与 6A.8 退避演练小节（标注 2026-09-17），不勾选阶段6退出门槛。

**修改文件**

- apps/api/src/tickets/risk-backoff-schedule-drill-runner.ts、risk-backoff-schedule-drill-runner.integration.test.ts：新增（3 项 MySQL 集成测试）。
- docs/00_INDEX.md、docs/02_TECHNICAL_ARCHITECTURE.md、docs/03_SAFETY_PRIVACY_STANDARD.md、docs/07_EXECUTION_ROADMAP.md：追加 6A.7/6A.8 小节。
- 开发日志/2026-09-17.md：追加本记录。

**验证**

- 6A.8 聚焦 MySQL 集成：1 个文件、3/3 通过。
- 退避+6A.5+6A.3 聚焦回归（backoff drill + notification attempt + outbox claim）：3 个文件、12/12 通过。
- `pnpm --filter @xiaoban/api typecheck`：通过。
- `pnpm --filter @xiaoban/api build`：通过。
- 未发起任何外部网络请求；未启动常驻 worker/定时器；无新增迁移、无契约变更。

**待办**

- 6B.1 数据权利请求：datarights 模块、迁移 018（应用→回滚→重放、审计禁改禁删触发器）、删除后儿童自由功能按权限停止。
- 完成后跑全量契约/API 离线/MySQL 集成/三工作区 typecheck/API 与 Web 构建。

**阻塞与风险**

- 本 runner 是可重放的本地演练，不代表存在真实调度器或真实重试；`timed_out/escalated` 仅为本地合成状态，不代表真实渠道失败或真实值守介入。
- 真实渠道适配、真实回执、告警/值守与 SLA 桌面演练仍不在本分支范围，须合并后另行确认。

<!-- dev-log:2026-09-17:B-3 -->
### B-3 20:50 · 6B.1 数据权利请求（切片3/3）

**摘要**

- 在无外部网络、仅本地合成数据边界内，新增监护人数据权利请求最小闭环：`POST /api/v1/guardian/data-rights/requests` 提交 delete/export；018 迁移新增请求表与追加式、禁改禁删的事件表。导出仅排队、删除为“功能性删除”（停用/撤回/撤销会话），不物理擦除历史与审计证据。

**已完成**

- 新增 `apps/api/src/datarights/data-rights-service.ts`：监护人 Bearer 鉴权；校验 active guardian/enrollment/consent/child 与 verified 且未停用绑定；requestId+请求哈希幂等，异载荷返回 `IDEMPOTENCY_CONFLICT`(409)、儿童令牌 403、未确认/非法请求 400。
- export：请求置 `queued`、儿童保持 active，写 `request_received`、`export_queued` 事件与 `data_rights.export_queued` 审计，不生成任何导出文件。delete：请求置 `completed`，撤回监护同意（记录 reason/requestId）与 enrollment、停用 child 账号与监护绑定、撤销未撤销的 child 会话，写 `functional_deletion_completed` 事件及 `data_rights.functional_deletion_completed`、`child_account.deactivated` 两条审计。
- 新增迁移 `018_datarights_requests.ts`：`data_rights_requests`、`data_rights_request_events`；事件表 `trg_data_rights_event_no_update`、`trg_data_rights_event_no_delete` 触发器禁改禁删，固定 `synthetic=TRUE` 检查；事件以 `(data_rights_request_id,sequence_no)` 与 `(data_rights_request_id,action)` 唯一约束，`sequence_no` 稳定表达同事务内追加顺序；down 先删触发器再删表。
- 共享契约 `packages/contracts` 追加 `DATA_RIGHTS_SCHEMA_VERSION` 与 request/response schema（仅追加，不改既有枚举）；`app.ts` 注册路由、`server.ts` 装配服务、`database/types.ts` 追加两表类型。
- 集成测试 4 项：export 排队+幂等重放、delete 功能性停用/撤回/撤销会话+事件禁改禁删+审计、未确认/儿童/异载荷冲突、Fastify inject 的 HTTP 201 响应契约与儿童 403。
- 同步 docs/00、02、03、07、08 的 6B.1 段落与本地 MySQL 说明。

**修改文件**

- apps/api/src/datarights/data-rights-service.ts、data-rights-routes.ts、data-rights-service.integration.test.ts：新增。
- apps/api/src/database/migrations/018_datarights_requests.ts：新增。
- apps/api/src/database/types.ts、apps/api/src/app.ts、apps/api/src/server.ts、apps/api/src/database/database.integration.test.ts：最小追加/基线断言调整。
- packages/contracts/src/index.ts：追加数据权利契约区块。
- docs/00_INDEX.md、02_TECHNICAL_ARCHITECTURE.md、03_SAFETY_PRIVACY_STANDARD.md、07_EXECUTION_ROADMAP.md、08_LOCAL_MYSQL_DEVELOPMENT.md：追加 6B.1 说明。
- 开发日志/2026-09-17.md：追加本记录。

**验证**

- 018 迁移在 xiaoban_test（迁移账号）直接调用同一份迁移函数完成 应用→回滚（断言两表与触发器消失）→重放（断言重建）：rollback ok / replay ok。
- 6B.1 聚焦 MySQL 集成：1 个文件、4/4 通过（含 HTTP 201/403）。
- 全量 MySQL 集成：22 个文件、88/88 通过。
- `pnpm --filter @xiaoban/contracts test`：1 个文件、78/78 通过；contracts build 通过。
- `pnpm --filter @xiaoban/api test`：19 个文件、119/119 通过（离线）。
- `pnpm typecheck`：contracts/api/web 三工作区通过。
- `pnpm --filter @xiaoban/api build`、`pnpm --filter @xiaoban/web build`（135 modules）：通过。
- 未发起外部网络请求；未接短信/邮件/微信/推送；未存真实联系方式；未启动 worker/定时器；临时迁移脚本均已删除，未提交。

**待办**

- 真实导出文件生成与交付、真实渠道与回执、物理删除/数据保留策略评审、SLA 桌面演练、风险工作台（Agent A）合并后做阶段 6 总体验收。
- 待两分支合并或协调干净 xiaoban_test 后，重跑标准迁移器命令做完整迁移验证。

**阻塞与风险**

- 标准迁移器（migrate.ts，Kysely Migrator）在共享 xiaoban_test 库报 “previously executed migration 019_risk_console_assignment is missing”：该库已存在并行 Agent A 分支的 019 执行记录/对象，而本分支文件系统只到 018，Migrator 拒绝运行；这不是 018 DDL 失败。本分支未复制 019、未删除并行对象；018 已用直接调用同一份迁移函数完成应用→回滚→重放。不得把标准迁移命令记为通过。
- 当前 delete 为“功能性删除”（撤回同意/报名、停用儿童与绑定、撤销儿童会话以阻止继续自由功能），不物理擦除历史合成数据与不可变审计证据；export 仅排队、不生成真实导出文件。若后续合规要求物理擦除全部历史，需先做数据保留/审计冲突评审并单独确认。
- 本切片为本地合成演练，不代表真实通知链路、真实数据权利履约或阶段 6 退出门槛达成；5F 仍暂停而非通过，本任务未恢复模型评测、未进入阶段 7。
