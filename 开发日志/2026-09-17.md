# 开发日志 2026-09-17

> 项目：小伴AI陪伴智能体  
> 当前阶段：产品开发轨道 / 阶段6A（通知、成人端与风险工作台）；5F/儿童试点安全评测按2026-09-16决定暂停而非通过  

## 记录

<!-- dev-log:2026-09-17:manual-1 -->
### 10:44

**摘要**

- 阶段6A.6完成：在无网络、仅合成数据边界内打通本地模拟成功回执链`attempted→delivered→viewed→acknowledged`，并实现站内/站外双通道确认聚合——单通道确认只显示等待，双通道均确认工单才为`acknowledged`；不代表阶段6退出门槛达成。

**已完成**

- 新增`RiskLocalReceiptRunner`（apps/api/src/tickets/risk-local-receipt-runner.ts）：短事务内读取合成工单，按通道当前状态推进单条成功回执（attempted→record_delivered、delivered→record_viewed、viewed→record_acknowledged），转换复用6A.1状态机与`RiskTicketStore.applyEvent`的行锁、事件哈希幂等和outbox更新。
- 每个送达/查看/确认回执使用独立requestId，同requestId重放返回同一回执并标记`replayed:true`；返回固定`simulated:true`、`networkCallMade:false`。
- 双通道隔离：in_app的回执不推进off_site_backup；单通道确认工单为`waiting_for_acknowledgement`，两通道均`acknowledged`才聚合为`acknowledged`。
- 通道已确认后的新requestId安全空转（`recorded:false`、`reason:channel_already_acknowledged`）；`not_sent/failed/timed_out`通道拒绝成功回执，保持“未attempted不得delivered、未viewed不得acknowledged”的顺序门槛；未知工单/非法通道/非法ID/多余字段统一拒绝且不追加事件。
- 不新增迁移、不新增业务表、不注册API、不启动常驻worker或调度器；事件仍追加式、触发器禁止UPDATE/DELETE。
- 同步docs/00、docs/07、docs/02、docs/03的6A.6范围、验证与安全边界（未勾选阶段6退出门槛）。

**修改文件**

- apps/api/src/tickets/risk-local-receipt-runner.ts：新增本地模拟成功回执记录器。
- apps/api/src/tickets/risk-local-receipt-runner.integration.test.ts：新增4项MySQL集成测试（双通道全链路+聚合+幂等、通道隔离、终态/未尝试拒绝、非法请求不写事件）。
- docs/00_INDEX.md、docs/07_EXECUTION_ROADMAP.md、docs/02_TECHNICAL_ARCHITECTURE.md、docs/03_SAFETY_PRIVACY_STANDARD.md：同步6A.6。
- 开发日志/2026-09-17.md：新增本记录。

**验证**

- `node --env-file=../../.local/mysql-secrets/database.env ./node_modules/vitest/vitest.mjs run --config vitest.integration.config.ts src/tickets/risk-local-receipt-runner.integration.test.ts`（在apps/api）：1个文件、4/4通过。
- 6A.6+6A.5聚焦回归（risk-local-receipt-runner + risk-notification-attempt-runner）：2个文件、8/8通过。
- `pnpm test:integration`：20个文件、81/81通过（6A.5为19文件77/77，本切片新增4项）。
- `pnpm --filter @xiaoban/contracts test`：1个文件、78/78通过。
- `pnpm --filter @xiaoban/api test`：19个文件、119/119通过（离线单测数量未变，新逻辑仅集成覆盖）。
- `pnpm typecheck`：contracts、api、web三工作区通过（修复一处notification.status索引收窄）。
- `pnpm --filter @xiaoban/api build`：通过。
- 未发起外部网络请求；未调用模型；未覆盖V1—V7评测证据；本切片无新增迁移、无契约schema变更（回执事件与状态枚举6A.1已定义）。

**待办**

- 下一最小切片建议：通知故障的用户侧降级提示（仍无外部网络），或真实渠道适配前的渠道凭据/真实回执/幂等/回滚设计评审（须用户单独确认，不属于自动推进）。
- 退避调度器、SLA桌面演练、风险工作台API/UI、数据权利请求和阶段6总体验收仍未完成。

**阻塞与风险**

- delivered/viewed/acknowledged仅为本地合成状态建模，固定`simulated:true`、`networkCallMade:false`，不代表真实短信/邮件/站外消息/推送已送达，不代表监护人或值守人员已查看、确认。
- 真实渠道接入前仍需渠道凭据、真实回执、告警/值守、监控、审计、回滚和单独用户确认；“未回执不显示成功”的阶段6退出门槛未因此达成。
- 5F仍暂停而非通过，本任务未恢复模型评测。

<!-- dev-log:2026-09-17:phase6-console-A1 -->
### A1 · 15:44 阶段6风险工作台 切片1：WORKBENCH 契约

**摘要**

- Agent A 风险工作台开工，仅合成工单、只读通知边界；新增 `risk-console-2026-09-v1` 契约区块（纯追加，未改既有 risk-ticket 枚举字面量）。

**已完成**

- `packages/contracts/src/index.ts` 追加 WORKBENCH 区块：`RISK_CONSOLE_SCHEMA_VERSION`、list/detail 响应、claim/通用 action、note、resolve 请求 schema；每条通知固定 `simulated:true` 与 `networkCallMade:false`，顶层同设边界；备注/处置说明强制 `虚构处置：` 前缀（10—240字），工单仅 L2/L3。
- 列表项与详情均不含普通聊天正文、供应商原文、秘密、提示词、推理、危险候选或联系方式；通知只暴露 channel/status/attempts。
- 新增 4 项契约测试：合法列表（含无网络边界）、接单+备注详情、拒绝 networkCallMade:true/非合成/无前缀备注/过短处置、拒绝未知字段与非 L2/L3。

**修改文件**

- packages/contracts/src/index.ts（追加 WORKBENCH 区块与导出）
- packages/contracts/src/index.test.ts（追加 import 与 4 项测试）

**验证**

- `pnpm --filter @xiaoban/contracts test`：1 个文件、82/82 通过（6A.6 为 78/78，本切片 +4）。
- 未发起外部网络请求；未新增迁移、未改状态机与通知 runner。

**待办**

- 切片2：迁移 019（risk_tickets 追加 child_id/assignee 列 + 独立 append-only console notes 表与触发器）+ database/types 追加。
- 后续：console service（监护鉴权、接单/备注/解决/关闭，复用 RiskTicketStore）、routes 与 app/server 追加注册、RiskConsole UI、docs 追加。

**阻塞与风险**

- 现有 risk_tickets（016）无儿童/责任人归属字段，既有事件枚举无“接单/处置备注”，故需启用任务书预授权的 019 迁移（仅追加列与独立备注表，不改既有枚举/状态机/触发器）；018 已让给 Agent B，不占用。
- 所有通知/回执仍为本地合成状态，不代表真实送达或确认。

<!-- dev-log:2026-09-17:phase6-console-A2 -->
### A2 · 15:53 阶段6风险工作台 切片2：迁移019与数据库类型

**摘要**

- 启用任务书预授权的 019 迁移：给 `risk_tickets` 纯追加儿童归属/责任人可空列，并新建独立 append-only 工作台备注表；已先在 TASK_BRIEF.md 追加 019 与装配层边界说明。

**已完成**

- `TASK_BRIEF.md` 追加“执行补充说明（2026-09-17 Agent A）”：说明启用 019 的原因（016 无儿童/责任人列、事件枚举无接单/备注）、仅追加可空列与独立备注表、复用既有状态机与 `risk_ticket_events`，以及 types.ts/server.ts 的纯追加装配边界。
- 新增 `apps/api/src/database/migrations/019_risk_console_assignment.ts`：
  - `risk_tickets` 追加 `child_id`、`claimed_by_guardian_id`、`claimed_at`（均可空），加索引 `idx_risk_ticket_console(child_id,status,created_at)`、`idx_risk_ticket_assignee(claimed_by_guardian_id,status)`；
  - 新表 `risk_ticket_console_notes`（id/ticket_id/request_id/guardian_id/kind/note/created_at，request_id 唯一），kind 限 `claimed|disposition_note`，note 空或≥10字符；
  - 建 `trg_risk_console_note_no_update/no_delete` 触发器禁止改删；down 删除触发器/表/索引/列。
- `apps/api/src/database/types.ts` 纯追加：`RiskTicketsTable` 三列、新 `RiskTicketConsoleNotesTable` 接口、`DatabaseSchema.risk_ticket_console_notes`；未改既有接口语义。

**修改文件**

- TASK_BRIEF.md（追加执行补充说明）
- apps/api/src/database/migrations/019_risk_console_assignment.ts（新增）
- apps/api/src/database/types.ts（纯追加）

**验证**

- 先 `pnpm --filter @xiaoban/contracts build`（worktree 内 dist 缺失会导致迁移 import 失败，已补齐）。
- 在 xiaoban_test：019 up `Success`；`migrateDown` `Success`；再 `migrateToLatest` 重放 `Success`（up→down→up 均通过）。
- `pnpm --filter @xiaoban/api typecheck`：通过。
- 未发起外部网络请求；未改 outbox 租约、attempt/receipt runner、LocalSyntheticNotificationAdapter 或 016/017 触发器。

**待办**

- 切片3：`risk-console-service.ts`（复用监护核验查询做严格权限；列表/详情/接单/备注/解决/关闭；复用 RiskTicketStore.applyEvent；019 列与 notes 表读写）。

**阻塞与风险**

- 019 为可空追加列，018 让给 Agent B；若 Agent B 同时新增迁移需协调编号。
- 本地 MySQL 凭据仅在主仓库 `D:\code\agent\.local`，worktree 集成命令需用该绝对 env 文件（仅 127.0.0.1:3307，无外网）。
<!-- dev-log:2026-09-17:phase6-console-A3 -->
### A3 · 16:10 阶段6风险工作台 切片3：console service 与 MySQL 集成

**摘要**

- 新增 `RiskConsoleService`：监护鉴权下的工单列表/详情/接单/追加处置备注/解决/关闭；复用 `GuardianDashboardService` 同款监护核验与 `RiskTicketStore`/状态机，不重写状态机、不触发任何发送。

**已完成**

- `apps/api/src/tickets/risk-console-service.ts`：
  - 复用监护会话核验（role=guardian、未撤销、enrollment/consent/link/child/guardian 全部 active、relationship verified），儿童角色 403、无效令牌 401；
  - 工单按 `risk_tickets.child_id` 严格过滤到本户；他户工单详情/接单一律 `NOT_FOUND 404`（不暴露存在性），他户列表为空；
  - 接单：`FOR UPDATE` 行锁，仅 `unclaimed` 可接，幂等 requestId 重放；写入 `claimed_by_guardian_id/claimed_at` 并追加 `claimed` 备注；
  - 追加处置备注：仅接单本人、非 closed，写 append-only `disposition_note`，幂等重放且异载荷冲突；
  - 解决/关闭：仅接单本人，委托 `RiskTicketStore.applyEvent`（resolve 需 acknowledged/escalated，close 需 resolved）；按 requestId 判重后再写，避免服务端 occurredAt 变化造成的伪幂等冲突；
  - 详情只暴露风险等级/类别/状态/时间/通知 channel/status/attempts/处置时间线，通知固定 `simulated:true`、`networkCallMade:false`、`readOnly:true`；不含聊天正文、供应商原文、秘密、提示词、推理、危险候选或联系方式；
  - permissions 按 assigneeState 与 status 派生 canClaim/canAddNote/canResolve/canClose。
- `apps/api/src/tickets/risk-console-service.integration.test.ts`：4 项 MySQL 集成（回滚事务，不对 append-only 表做 DELETE）：本户列表隔离+无聊天字段；接单→备注→解决→关闭全链路与幂等重放+通知无网络边界+console notes 触发器禁止 UPDATE/DELETE；closed 拒绝追加备注、未回执拒绝解决；儿童403、他户列表空/详情与接单404、无效令牌401。

**修改文件**

- apps/api/src/tickets/risk-console-service.ts（新增）
- apps/api/src/tickets/risk-console-service.integration.test.ts（新增 4 项）

**验证**

- `pnpm --filter @xiaoban/api typecheck`：通过。
- 在 xiaoban_test 运行 `risk-console-service.integration.test.ts`：1 个文件、4/4 通过（含 console notes UPDATE/DELETE 触发器拒绝回归）。
- 未发起外部网络请求；未改 outbox 租约、attempt/receipt runner、LocalSyntheticNotificationAdapter 或 016/017 触发器。

**待办**

- 切片4：`risk-console-routes.ts` + `app.ts`/`server.ts` 追加注册；路由级 401/403/404/409 与 JSON Schema 校验。

**阻塞与风险**

- 工单 `child_id` 由迁移 019 追加可空列；当前生产链路尚无造单入口给该列赋值，本切片通过集成造数设置；后续真实造单需在安全事件建单时写入 child_id（不在本分支范围）。
- 通知状态仍为本地合成，只读展示，不代表真实送达/查看/确认。