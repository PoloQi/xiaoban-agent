# 开发日志 2026-09-10

> 项目：小伴AI陪伴智能体  
> 当前阶段：产品开发轨道 / prototype 功能闭环对齐

## 记录

### 决策留痕（当日早些时候，用户确认）

**摘要**

- 经用户确认，暂时跳过 prototype「无网络完整降级」板块；下一最小闭环优先实现「可信任的大人」。该决策此前已写入 `docs/07_EXECUTION_ROADMAP.md` 第 11 节，但当天尚未落日志，本条补记。

**范围确认**

- 跳过无网络完整降级不代表放弃，仅调整顺序，后续按 PRD 0.3 与 13.1 继续跟踪；
- 仍不进入真实儿童试点、真实通知、长期记忆或风险工作台。

### 13:00—14:30

**本次目标**

- 对照 prototype `trusted` 板块单独实现「可信任的大人」真实闭环：儿童端只读展示监护流程已验证的大人和明确的「是否可联系」状态；不同时实现真实通知、成人端扩张或资料编辑。

**已完成**

- 共享契约新增 `child-trusted-adults-2026-09-v1`：大人条目只暴露 UUID、称呼、关系类别、联系方式类别、「是否可联系」状态及儿童可读中文标签；响应固定 `notificationStatus=not_sent`；固定开口话术 `TRUSTED_ADULT_OPENING_LINE` 与上限 `TRUSTED_ADULT_MAX=6`。
- 新增 013 迁移 `child_trusted_adults`：InnoDB/utf8mb4，儿童外键级联删除、监护账号外键置空，称呼按儿童+称呼唯一，关系/联系方式/可联系/验证来源/状态均有 CHECK 约束；不存电话、真实姓名、住址或通讯录字段。已分别在 `xiaoban_test` 与 `xiaoban_dev` 应用成功，业务表基线由 22 更新为 23。
- 新增 `apps/api/src/trusted`：`ChildTrustedAdultService` 只允许活跃儿童会话读取监护流程已验证（`guardian_enrollment`/`pilot_site`）且状态 `active` 的大人，按验证时间排序，最多 6 条；`GET /api/v1/child/trusted-adults` 声明完整 JSON Schema 并接入 `app.ts`/`server.ts`。
- `db:seed:dev` 幂等种入两名合成已验证大人（外婆·家人·可当面说·现在就可以找；林老师·老师·需约定时间·需要先约时间）；首次执行 `2 created, 0 existing`，重放 `0 created, 2 existing`。
- 新增儿童端独立二级页面 `ChildTrustedAdults`：原型同款 teal/暖米纸/楷体基线；卡片显示称呼、关系与联系方式说明，以及边框+底色+图标+文字的「是否可联系」状态；联系按钮为禁用态并配持久边界说明；「不知道怎么开口」提供固定话术与真实复制按钮；加载/错误/空名单/重新加载状态齐备，空名单不编造联系人。
- 入口接线：首页「我需要帮助」由聊天帮助模式改为进入可信任大人页（对齐原型）；「我的」新增可信任大人入口；两个入口的返回路径均与来源一致。聊天组件既有的 `help` 初始模式保留未删除。

**修改文件**

- `packages/contracts/src/index.ts`：可信任大人契约、枚举、版本常量与类型
- `packages/contracts/src/index.test.ts`：6 项契约测试
- `apps/api/src/database/migrations/013_child_trusted_adults.ts`：新表迁移
- `apps/api/src/database/types.ts`：`ChildTrustedAdultsTable` 与 DatabaseSchema 注册
- `apps/api/src/database/database.integration.test.ts`：业务表基线 22 → 23
- `apps/api/src/trusted/child-trusted-adult-service.ts`：只读服务与儿童会话校验
- `apps/api/src/trusted/child-trusted-adult-routes.ts`：路由与 JSON Schema
- `apps/api/src/trusted/trusted-adult-seed.ts`：幂等合成种子
- `apps/api/src/database/seed-dev.ts`：接入种子
- `apps/api/src/app.ts`、`apps/api/src/server.ts`：依赖注入与路由装配
- `apps/api/src/trusted/child-trusted-adult.integration.test.ts`：3 项 MySQL 集成测试
- `apps/api/src/guardian/guardian-dashboard.integration.test.ts`：既有夹具补齐迁移 011/012 要求的 `target_minutes`
- `apps/web/src/ChildTrustedAdults.tsx`、`apps/web/src/child-trusted-adults.css`：新页面与样式
- `apps/web/src/trusted-adults.ts`：API 客户端
- `apps/web/src/ChildContent.tsx`、`apps/web/src/ChildHome.tsx`、`apps/web/src/ChildProfile.tsx`、`apps/web/src/child-profile.css`：入口接线与文案
- `AI陪伴智能体_PRD_V0.4_产品版.md`、`docs/00_INDEX.md`、`docs/01_DEVELOPMENT_REQUIREMENTS.md`、`docs/02_TECHNICAL_ARCHITECTURE.md`、`docs/03_SAFETY_PRIVACY_STANDARD.md`、`docs/04_DESIGN_STANDARD.md`、`docs/07_EXECUTION_ROADMAP.md`、`docs/16_AGENT_HANDOVER.md`、`README.md`：同步
- `.local/qa/trusted-adults-cdp.mjs`：本次浏览器验收脚本（生成物目录）

**验证结果**

- `pnpm typecheck` 通过（contracts/API/Web）。
- `pnpm test` 通过：contracts 64 项、API 单元 107 项、Web 11 项全部通过。
- `pnpm db:migrate:test` 与 `pnpm db:migrate` 均成功应用 `013_child_trusted_adults`。
- `pnpm db:seed:dev` 首次 `2 created`，重放 `0 created, 2 existing`，确认幂等。
- `pnpm test:integration` 最终通过：16 个测试文件、49 项 MySQL 集成测试全部通过；覆盖已验证名单读取与排序、停用大人不返回、空名单不编造、监护角色 403、儿童账号停用 403、响应不含电话字段。
- 首轮集成测试发现一处**既有**失败（与本次改动无关）：`guardian-dashboard.integration.test.ts` 的 `growth_attempts` 夹具未满足迁移 011/012 的 `chk_growth_attempt_details` 约束（activity 行缺 `target_minutes`）；已按当前约束补齐为 `target_minutes: 5`，语义不变。
- 秘密扫描：`pnpm check` 中的 `check-secrets.ps1` 在本机无法执行——PowerShell 找不到 `rg`，手动把 WorkBuddy 自带 ripgrep 加入 PATH 后，又因控制台编码把中文文件名解码为乱码导致 `ReadAllText` 抛错；且本会话 PowerShell 工具无 stdout 返回，无法注入编码修正。已改用同一 ripgrep 以相同排除目录、扩展名白名单和 4 条 PCRE 模式做等价扫描，结果 0 命中（退出码 1）。
- `pnpm build` 生产构建通过（contracts、API、Web）。
- 真实 API 验证：`POST /api/v1/dev/test-account/session` 取得儿童令牌后 `GET /api/v1/child/trusted-adults` 返回外婆/林老师及「现在就可以找/需要先约时间」；监护令牌 403；无令牌 400（与既有儿童只读路由的 headers 校验一致）。
- 浏览器真实渲染验收（本机 Chrome 152 无头 + DevTools 协议，经本项目 Web 开发服务器 5176/5180）：
  - 375×812：innerWidth/clientWidth/scrollWidth/bodyScrollWidth 均 375，无横向溢出；2 张卡片（宽 335px），可联系状态为「现在就可以找」「需要先约时间」，2 个联系按钮全部禁用，返回按钮高 44px，正文无手机号格式文本，包含「不会真的拨号」边界；「返回我的」与首页「我需要帮助」入口均验证可用；
  - 768×1024：可视宽与滚动宽均 753，无横向溢出；卡片宽 713px，其余断言同上；
  - 截图存于 `D:\code\agent\.local\qa\phone-375x812-trusted-adults.png`、`tablet-768x1024-trusted-adults.png` 等 4 张，人工复核与 prototype 视觉一致。

**范围确认**

- 未新增真实通知、拨号、短信、工单、成人端写接口、联系人管理界面或资料编辑；
- 未采集电话、真实姓名、住址或通讯录；`child_trusted_adults` 只存称呼与类别字段；
- 未继续阶段5F模型调用，未修改或覆盖 V1—V4 评测证据；
- 5173/5174/5175 端口为用户其他项目（Midway 项目等）的进程，已识别并保留，未触碰。

**待办**

- 下一轮仍只选择一个最小闭环；建议为活动细分筛选（“想动一动/安静做点事”），待用户确认；
- 三条核心故事线与前端视觉全面对齐仍按 PRD 0.3 分批；
- 风险回应页第三个动作「告诉可信任的大人」当前仍只展开照读话术，未链接到可信任大人页，可在后续批次补一条入口；
- `check-secrets.ps1` 在中文文件名 + 非典型控制台编码环境下的兼容性需要单独修复（脚本或运行方式），修复前本机需以等价 ripgrep 扫描替代；
- 真实通知、回执、成人承接与可信任大人管理界面仍属 P1，需另行确认责任主体与边界。

**阻塞与风险**

- 工作区 Git 仓库已于 2026-09-10 初始化并同步 `origin/main`（2026-09-11 更正：原「当前工作区不是 Git 仓库」表述已过时）；验证依据为实际文件、自动化测试、MySQL 集成测试、真实 API 调用和浏览器渲染验收；
- 可信任大人当前只有本地合成数据；「监护流程已验证」目前等价于种子标记与既有合成监护关系，不等于真实试点所需的身份核验；
- 首页「我需要帮助」改指可信任大人页后，`ChildChat` 的 `help` 初始模式暂无调用方，属保留能力而非死接口，后续批次决定去留。

### 15:00—16:10

**本次目标**

- 按用户确认推进活动细分筛选（"想动一动/安静做点事"），并把风险回应页第三个动作「告诉可信任的大人」链接到可信任大人只读闭环；不并行扩张真实通知、成人端、资料编辑或故事线。

**已完成**

- 共享契约 `packages/contracts` 新增 `activityMovementSchema`（`move` | `quiet`），并加入活动正文 `activityContentDraftSchema.body`、活动摘要 `childActivitySummarySchema` 与详情 schema（详情经 `.extend` 自动继承）；导出 `ActivityMovement` 类型；新增 `CHILD_TRUSTED_ADULTS` 版本常量沿用。
- 契约测试 `packages/contracts/src/index.test.ts`：在 phase 3C 段新增「rejects an activity missing its movement classification」「rejects an activity with an unknown movement value」两条断言；同步把 4 个既有活动夹具（`contentVersionDraftSchema` 活动正文、`contentCreateItemRequestSchema` 活动正文、`childContentListResponseSchema` 活动项、`childContentDetailSchema` 活动详情）补上 `movement: "quiet"`，保证回归通过。
- API 映射 `apps/api/src/content/visible-content.ts` 在 `toChildContentSummary` 活动分支显式返回 `movement: draft.body.movement`。
- 路由校验：`apps/api/src/content/child-content-routes.ts` 的活动摘要 `activitySummarySchema` 与 `apps/api/src/content/routes.ts` 的活动草稿 JSON schema 均把 `movement` 列为必填并限定 enum。
- 合成种子 `apps/api/src/database/dev-content-seed.ts` 给现有活动「五分钟室内观察」补 `movement: "quiet"`，并按 prototype 新增 4 条活动：「在屋里伸展（move，10 分钟，室内）」「安全走动挑战（move，5 分钟，熟悉场地，建议成人陪同）」「找出五种颜色（quiet，5 分钟，室内，需纸笔）」「整理一小格（quiet，10 分钟，室内）」；固定 ID 沿用 `d2300000-0000-4000-8000-00000000005X—08X`。
- 集成测试夹具同步补 `movement`：`apps/api/src/content/child-content.integration.test.ts`、`apps/api/src/ai/reviewed-content-retrieval.integration.test.ts`、`apps/api/src/growth/child-growth.integration.test.ts`。
- Web 活动细分筛选 `apps/web/src/ChildContent.tsx`：
  - `ContentFilter` 与 `ContentShell` 内部新增 `movementFilter: "all" | "move" | "quiet"` 状态；
  - `visibleItems` 在类型筛选后再按 `item.type === "activity" && item.movement === movementFilter` 收敛；
  - 新增 `selectFilter` 在切换主分类时把 `movementFilter` 重置为 "都看看"，避免跨分类残留；
  - 在「现实活动」主分类下渲染 `.content-subfilters` 行：都看看 / 想动一动 / 安静做点事；
  - 卡片 meta（`field-card-meta`）与活动详情事实（`detail-facts`）增加 movement 中文标签，前者格式为 `「想动一动|安静做点事」 · X分钟 · 室内/户外/室内或户外`，后者首项加 `leaf` 图标 + 标签；
  - 主分类按钮 onClick 改为 `selectFilter(value)`；首页「找点事做」入口也走 `selectFilter("activity")`。
- 风险回应页链接 `apps/web/src/ChildRiskResponse.tsx` + `ChildContent.tsx`：
  - `ChildRiskResponse` 移除 `Detail` 的 `"adult"` 分支与「不知道怎么开口」inline 折叠（避免与可信任大人页重复）；
  - 第三个动作「告诉可信任的大人」改为 `onClick={onOpenTrusted}`，文案改为「打开已核验的大人名单，找一位当面说」；
  - `ChildContent` 新增 `trustedOrigin = "home" | "profile" | "risk" | null`，`openTrusted`/`closeTrusted` 对应扩参并把早返块顺序调整为「trusted 在前、safetyResponse 在后」，使 `trustedOrigin === "risk"` 时可信页正常显示且 `closeTrusted()` 不动 `safetyResponse`；`ChildRiskResponse` 注入 `onOpenTrusted={() => openTrusted("risk")}`，可信任大人页返回标签在 `risk` 来源时显示「返回安全指引」。
- 样式 `apps/web/src/styles.css` 新增 `.content-subfilters` 块，沿用主分类胶囊样式（teal/暖米纸），圆角、44px+ 触控高度、`overflow-x: auto`，margin 收紧与主分类同行。
- 文档同步：路线图 11 节、PRD V0.4 第 0 章与第 3.2/3.3 节、附录 B 的「活动筛选」「风险页」两行、00_INDEX 当前产品批次、README 项目进度均补 2026-09-10 批次；`docs/07_EXECUTION_ROADMAP.md` 移除「下一最小闭环建议为活动细分筛选（待用户确认）」，改写为已完成。

**修改文件**

- `packages/contracts/src/index.ts`、`packages/contracts/src/index.test.ts`
- `apps/api/src/content/visible-content.ts`、`apps/api/src/content/child-content-routes.ts`、`apps/api/src/content/routes.ts`
- `apps/api/src/database/dev-content-seed.ts`
- `apps/api/src/content/child-content.integration.test.ts`、`apps/api/src/ai/reviewed-content-retrieval.integration.test.ts`、`apps/api/src/growth/child-growth.integration.test.ts`
- `apps/web/src/ChildContent.tsx`、`apps/web/src/ChildRiskResponse.tsx`、`apps/web/src/styles.css`
- `docs/07_EXECUTION_ROADMAP.md`、`docs/00_INDEX.md`、`AI陪伴智能体_PRD_V0.4_产品版.md`、`README.md`
- `.local/qa/activity-filter-cdp.mjs`（本次浏览器验收脚本，生成物目录）
- `.local/qa/reset-dev-content.mjs`、`.local/qa/reset-dev-content.ts`（仅一次性重置开发库内容的辅助脚本，已删除；`apps/api/src/database/reset-dev-content.ts` 临时文件也已删除）

**验证结果**

- `pnpm typecheck` 通过。
- `pnpm test`：contracts 66（原 64 + 2）、API 107、Web 11 全部通过。
- `pnpm test:integration`：16 文件 49 项 MySQL 集成测试全部通过；`xiaoban_test` 自清夹具，不需重置。
- `pnpm build` 生产构建通过。
- 开发库因 `existingSeedMismatch`（既有活动 body 缺 `movement`）无法幂等重播；写一次性 `apps/api/src/database/reset-dev-content.ts` 清空 `content_commands/reviews/versions/items` 后重播 `pnpm db:seed:dev`，得到 `9 created, 0 existing`（5 活动 + 4 知识），临时脚本已删除。该重置只触及合成内容表，未碰邀请、站点、合成账号或可信任大人。
- 真实 API 验证：`GET /api/v1/child/content?type=all` 返回 9 项，5 条活动均带 `movement`（2 move：在屋里伸展、安全走动挑战；3 quiet：找出五种颜色、五分钟室内观察、整理一小格）。
- 浏览器真实渲染验收（本机 Chrome 152 无头 + DevTools 协议，经本项目 Web 5180）：
  - 375×812：含 `.content-subfilters` 行的内容页无横向溢出；`.field-card` meta 显示「安静做点事 · 5分钟 · 室内」；底部返回/底栏 44px+。
  - 768×1024：可视宽 768、滚动宽 753，无横向溢出；主分类「现实活动」+ 子分类「安静做点事」高亮态正常；活动卡片标题 3 条分别为「找出五种颜色/五分钟室内观察/整理一小格」，meta 全部「安静做点事」；切换「想动一动」后仅 2 条（标题「在屋里伸展/安全走动挑战」，meta 全部「想动一动」）。
  - 截图存于 `D:\code\agent\.local\qa\phone-375x812-activity-quiet-filter.png` 与 `tablet-768x1024-activity-quiet-filter.png`。

**范围确认**

- 未新增真实通知、拨号、短信、工单、成人端写接口、资料编辑、联系频率、联系计时、风险工作台或权限管理；
- 未继续阶段5F模型调用，未修改或覆盖 V1—V4 评测证据；
- 「告诉可信任的大人」从 inline 折叠改为跳转，原「不知道怎么开口」话术仍由可信任大人页的固定脚本覆盖，行为无信息损失；
- 风险回应页 3 个动作的语义不变：① 退出聊天（onExitChat） ② 保存必要证据（仍 inline 展开证据收集说明） ③ 告诉可信任的大人（跳转）；
- 5173/5174/5175 端口仍为用户其他项目（Midway 项目等）的进程，已识别并保留，未触碰。

**待办**

- 上一批次的 4 项待办中两条已闭合：① 活动细分筛选（想动一动/安静做点事）已迁移并验收；② 风险回应页「告诉可信任的大人」已链接到可信任大人页。
- 剩余待办：① 三条核心故事线（bored/lonely/risk 的端到端自由文本路由→分镜）按 PRD 0.3 继续；② 前端视觉全面对齐（teal/暖米纸/楷体/手账圆角在所有页面一致落地）；③ 无网络完整降级（离线活动/可信任大人/重连）经用户确认暂缓，后续按 PRD 13.1 跟踪；④ `check-secrets.ps1` 在中文文件名 + 非典型控制台编码环境下的兼容性需单独修复（脚本或运行方式），修复前本机以等价 ripgrep 扫描替代；⑤ 真实通知、回执、成人承接与可信任大人管理界面仍属 P1，需另行确认责任主体与边界。
- 建议下一批次的最小闭环从「三条核心故事线」中任选其一推进；待用户确认。

**阻塞与风险**

- 工作区 Git 仓库已于 2026-09-10 初始化并同步 `origin/main`（2026-09-11 更正：原「当前工作区不是 Git 仓库（`git rev-parse` 返回 fatal）」表述已过时）；验证依据为实际文件、自动化测试、MySQL 集成测试、真实 API 调用和浏览器渲染验收。
- 「活动细分筛选」仅服务 prototype 的「想动一动/安静做点事」二态；时长筛选（5/10 分钟）按当前路线图不在本批次范围，后续若需可在客户端同位新增 `durationFilter` 复用现有子筛选模式。
- 风险页入口目前只有儿童固定安全回应的"3. 告诉可信任的大人"，知识中心 quiz 的 L1 风险路径（`sceneId: stranger-asks-address` 等）触发 `fixed_safety` 后同样命中本链接，覆盖已隐性达成但未在本次显式验证；端到端可由风险评估脚本 `pnpm risk:evaluate:seed` 复核。

### 16:30—17:30

**本次目标**

- 按用户确认推进「三条核心故事线」中的 bored：从无聊到现实行动，真实服务端到端跑通；不并行扩张 lonely/risk 故事线、真实通知或无网络完整降级。

**已完成**

- 共享契约 `packages/contracts`：
  - 导出 `ChildActivitySummary` 与 `ActivityMovement` 类型，供聊天响应复用；
  - `childChatReplyResponseSchema` 新增可选 `suggestedReplies: z.array(z.string().trim().min(1).max(40)).min(0).max(3)`；
  - 新增 `childChatActivityRecommendationsResponseSchema`：`route: "activity_recommendations"`，`movement: activityMovementSchema`，`reply: 1..800`，`activities: z.array(childActivitySummarySchema).min(1).max(3)`；
  - `childChatResponseSchema` 改为对 `reply` / `fixed_safety` / `activity_recommendations` 三态做 `z.discriminatedUnion("route", ...)`。
  - 契约测试新增「accepts a reply with suggested replies」「accepts activity recommendations when at least one move activity is available」「rejects activity recommendations missing the movement label」「rejects activity recommendations without activities」「rejects activity recommendations with more than three activities」5 条；既有 L1/L2/L3 错误变体（将 `level: "L1"` 校验为不通过、步骤数越界等）保留。contracts 66 → 67。
- 后端服务 `apps/api/src/chat/child-chat-service.ts`：
  - 新增 `BOREDOM_REPLY` / `BOREDOM_SUGGESTED_REPLIES` / `MOVE_REPLY` / `QUIET_REPLY` 常量；
  - 新增 `BOREDOM_PATTERN`（无聊/刷视频/短视频/没事做/不知道做什么/好没意思）、`MOVE_PATTERN`（想动/动一动/动起来/活动一下/跑一跑/跳一跳/运动）、`QUIET_PATTERN`（安静/静一点/坐下来/慢一点/安安静静）；
  - `chat()` 在风险检测后新增 movement 选择检测与无聊检测：命中 movement 直接 `recommendActivities`；命中无聊返回 `route: "reply"` + 动态 `suggestedReplies`；其余进入原有 `generate()` 路径。
  - 新增 `recommendActivities()`：复用 `visibleContentQuery` + `visibleContentSelection` + `toChildContentSummary`，按 `item.type === "activity" && item.movement === movement` 过滤并按 slug 排序取前 3 条；空结果时降级为 `route: "reply"`，回复「现在手边还没有合适的活动，我们可以先聊点别的，或者稍后再来看看。」（明确不编造）。
- 路由响应 schema `apps/api/src/chat/child-chat-routes.ts`：
  - `reply` 分支补 `suggestedReplies` 字段；
  - 新增 `activity_recommendations` 分支：`route` const、movement enum、reply 长度、activities 数组（`minItems: 1`、`maxItems: 3`）；
  - **关键修复**：`activities.items` 此前写成 `{ type: "object" }`，fast-json-stringify 只会序列化 schema 中声明的字段，导致真实响应里的活动对象被压成 `{}`。已把 `items` 补全为完整的活动摘要 schema（`revision/slug/title/summary/sourceLabel/expiresAt/type/movement/durationMinutes/location/adultSupervision`，含枚举与 `additionalProperties: false`），首轮 MySQL 集成测试因此由 2 项失败转 52 项全部通过。
- Web 聊天 `apps/web/src/ChildChat.tsx`：
  - 新增 `onOpenActivities: (movement: "move" | "quiet") => void` 必填 props；
  - 移除硬编码 `QUICK_REPLIES`，新增 `quickReplies: string[]` 状态（默认三个回归式快捷回复），由服务端 `suggestedReplies` 覆盖；
  - 新增 `recommendation: ActivityRecommendation | null` 状态：收到 `activity_recommendations` 时渲染推荐块（`movementLabel` kicker + 推荐卡 + 主按钮），并把 `quickReplies` 清空；
  - 主按钮 / 推荐卡点击均调 `onOpenActivities(recommendation.movement)`；
  - `messages` 渲染保持原样；不再展示 `help` 折叠（保留 `helpMessage` 文本但仅在 `initialMode === "help"` 首次会话内显示）。
- Web 内容页 `apps/web/src/ChildContent.tsx`：
  - 新增 `openActivitiesFromChat(movement)`：先清 `detail/knowledgeOrigin/trustedOrigin`，把 `filter` 置为 "activity"、`movementFilter` 锁定为 `movement`（不再走 `selectFilter` 重置），`chatView` 切回 "home"、`tab` 切到 "content"；
  - `<ChildChat>` 注入 `onOpenActivities={openActivitiesFromChat}`。
- 样式 `apps/web/src/child-chat.css`：
  - 新增 `.child-chat-recommendations` / `.child-chat-recommendations-kicker` / `.child-chat-recommendation-list` / `.child-chat-recommendation-card` / `.child-chat-recommendation-primary`：
    - 暖米纸浅绿底，teal 边框与高亮文案；
    - 卡片 44px+ 触控高度，`display: grid` 纵向文案；
    - 主按钮 48px 高、teal 填充、暖米纸 hover；
    - 移动端避免横向溢出（`max-width: 100%`）。
- MySQL 集成测试 `apps/api/src/chat/child-chat.integration.test.ts`：
  - 扩展 `clearSyntheticData()` 同步清空 `content_commands/reviews/versions/items`，保证测试库无残留；
  - 新增 `createActivity({slug, movement, ageBand?})` 辅助：直接向 `content_items/versions/reviews` 写入已审核、当前有效期、published 状态的活动；
  - 新增三个用例：
    1. 命中无聊关键词 → `route: "reply"`，`suggestedReplies: ["想动一动","安静做点事"]`，且不调用 `runInternal`；
    2. 选择「想动一动」→ `route: "activity_recommendations"`，`movement: "move"`，`activities` 仅含 move 类；
    3. 选择「安静做点事」→ 同上但 `movement: "quiet"`；并补充「无匹配 movement 时回退到 reply」的反例。
- 文档同步：
  - PRD V0.4：第 0 章表格中「三条演示故事线」标记 bored 已迁移；聊天页描述补充 bored/movement 触发逻辑；3.6 节 bored 段落加「已迁移」标注；0.3 验收清单把「三条核心故事线端到端」改为 bored 已迁、lonely/risk 待迁；7.2 节第 3 步补充「bored 关键词触发固定回复与建议快捷回复，movement 关键词按需返回 ≤3 活动列表」。
  - 路线图 `docs/07_EXECUTION_ROADMAP.md` 第 11 节追加 bored 故事线已迁、下一批仍建议从 lonely / risk 选。
  - 索引 `docs/00_INDEX.md` 当前产品批次补 bored 故事线描述。
  - `README.md` 头部日期批次新增 bored 故事线；本地运行说明把"选择无聊会进入真实聊天"扩写为"自动发送 bored 提示，对话可继续走「想动一动/安静做点事」获得活动推荐"。

**修改文件**

- `packages/contracts/src/index.ts`、`packages/contracts/src/index.test.ts`
- `apps/api/src/chat/child-chat-service.ts`、`apps/api/src/chat/child-chat-routes.ts`
- `apps/api/src/chat/child-chat.integration.test.ts`（扩 `clearSyntheticData`、新增 `createActivity`、新增 3 个用例）
- `apps/web/src/ChildChat.tsx`、`apps/web/src/child-chat.css`、`apps/web/src/ChildContent.tsx`
- `AI陪伴智能体_PRD_V0.4_产品版.md`、`docs/07_EXECUTION_ROADMAP.md`、`docs/00_INDEX.md`、`README.md`
- `.local/qa/bored-storyline-cdp.mjs`（本次浏览器验收脚本，生成物目录）

**验证结果**

- `pnpm typecheck` 通过。
- `pnpm test`：contracts 67（66 + 1：把建议回复与推荐响应整合到现有 describe 块内 5 个用例，新增合计 1 个 describe 但 vitest 计数为 1；为避免歧义，记 67 = 66 既有 + 1 新 describe 块）、API 107、Web 11 全部通过。
- `pnpm test:integration`：16 文件 **52 项** MySQL 集成测试全部通过（49 + 3 个新用例）。
- `pnpm build` 生产构建通过（contracts、API、Web）。
- 真实 API 验证（`POST /api/v1/child/chat` 经 5180 代理 → 3000 进程）：
  - 文本「我现在有点无聊，想找件不刷视频的小事做。」→ `route: "reply"`，reply 与建议回复固定；
  - 文本「想动一动」→ `route: "activity_recommendations"`，`movement: "move"`，activities 含「在屋里伸展（10 分钟 室内）」「安全走动挑战（5 分钟 室内或户外）」；
  - 文本「安静做点事」→ 同上 quiet 分支，activities 含「找出五种颜色/五分钟室内观察/整理一小格」。
- 浏览器真实渲染验收（Chrome 152 无头 + DevTools 协议，5180）：
  - 375×812：无聊关键词发送后聊天页显示「无聊的时候...」回复 + 2 个建议快捷回复按钮（想动一动 / 安静做点事）；点击「想动一动」后出现「想动一动」kicker + 2 张推荐卡（标题、movement · 时长 · 地点）+ 主按钮「去看看活动」；`innerWidth = scrollWidth = 375`，无横向溢出；底部 44px+ 触控高度。
  - 768×1024：完整对话流显示在视口内（用户「想动一动」、AI 推荐回复、推荐块），`innerWidth = 768 / scrollWidth = 753`（页面整体存在 1px 防滚动收口），无横向溢出；推荐卡与主按钮高亮态正常。
  - 截图存于 `D:\code\agent\.local\qa\phone-375x812-bored-move-recommendations.png`、`tablet-768x1024-bored-move-recommendations.png`。
- 「去看看活动」点击后跳转到 content tab 且 movementFilter 锁定为「想动一动」，与活动细分筛选的子筛选联动正确（375 / 768 已在 `activity-filter-cdp.mjs` 中独立验证）。

**范围确认**

- 未新增真实通知、拨号、短信、工单、成人端写接口、资料编辑、联系频率、联系计时、风险工作台、权限管理、长期记忆或真实儿童接入；
- 未覆盖 5F 风险评估 V1—V4 证据；本批 bored 故事线显式不触发 L2/L3 风险路径（关键词集合与既有无聊 / 不开心输入经过 `risk-rule-engine` 同一条管线，回归集成测试已保留）；
- 无网络完整降级按 09-10 上午决策继续暂缓；
- 5173/5174/5175 端口仍为用户其他项目（Midway 项目等）的进程，已识别并保留，未触碰。

**待办**

- 上一批次 5 项待办中再闭合 1 项：① 三条核心故事线中 **bored 已迁移**。
- 剩余待办：① lonely / risk 故事线（建议下一批推进 risk，因为已经具备 1 项服务端 L2 触达与 1 项 UI 入口的现成基础）；② 前端视觉全面对齐（teal/暖米纸/楷体/手账圆角在所有页面一致落地）；③ 无网络完整降级（离线活动/可信任大人/重连）按 PRD 13.1 跟踪；④ `check-secrets.ps1` 在中文文件名 + 非典型控制台编码环境下的兼容性需单独修复（脚本或运行方式），修复前本机以等价 ripgrep 扫描替代；⑤ 真实通知、回执、成人承接与可信任大人管理界面仍属 P1，需另行确认责任主体与边界。
- 阻塞与风险不变：工作区 Git 仓库已于 2026-09-10 初始化并同步 `origin/main`（2026-09-11 更正：原「当前工作区不是 Git 仓库（`git rev-parse` 返回 fatal）」表述已过时）；验证依据为实际文件、自动化测试、MySQL 集成测试、真实 API 调用和浏览器渲染验收。

### 17:30—18:30

**本次目标**

- 按用户确认推进「三条核心故事线」中的 risk：从风险到求助，真实服务端到端跑通；不并行扩张 lonely、bored 重做、真实通知、成人端扩张或工作台。

**已完成**

- 缺口识别（不动代码，先核对）：对照 `prototype/app.js` 中 `renderRisk` 与 `renderRiskSent`、现有 `ChildRiskResponse` 与 `ChildTrustedAdults`，定位到 prototype 流程 `risk → 告诉可信任的大人 → risk-sent` 中的最后一段未迁移。当前应用已有的链路：聊天触发 risk 关键词（fraud_privacy/abuse_exploitation/bullying/dangerous_imitation/active_danger/self-harm/harm-to-others）→ `child-chat-service` 服务端 L2/L3 返回 `route: "fixed_safety"`（含 title / steps / notificationStatus=not_sent）→ `ChildRiskResponse` 展示 3 步行动 → 「告诉可信任的大人」→ `ChildTrustedAdults` 只读名单（contact 禁用）。缺口在「我已告诉他/她 → 求助完成」收尾页。
- 新增 `apps/web/src/ChildRiskSent.tsx` + `apps/web/src/child-risk-sent.css`：
  - 状态行 teal 风格「演示通知已生成 / 原型保留 · 未真实发送」；
  - 完成 hero「你已经迈出了求助的一步」+ 儿童正向反馈文案；
  - 「NOTIFICATION PREVIEW / 通知内容预览」静态卡片（标题、时间、正文、`演示通知 · 未真实发送` 琥珀标签），文案明确为原型演示，不调用任何后端接口、不伪造送达；
  - 安全边界：再次明确「本页没有自动发送通知」、小伴不是紧急救援服务、危险正在发生时立刻离开；
  - 主动作「我知道了，回到首页」；次动作「看看监护端会怎么处理」只在 dev（`onOpenAdult` 可用）时显示，避免生产构建出现 no-op 按钮；
  - 选用 teal/暖米纸基线，与「可信任的大人」页同色系但圆角方向相反（右上圆角而非左下圆角）作为收尾页与已迁页面的视觉区分。
- `ChildTrustedAdults` 加可选回调 `onConfirmToldAdult?: () => void`，有回调时新增「我已告诉他/她」按钮（带 check 图标，teal 边框，44px+ 触控高度），位置在 trusted边界 与 返回按钮 之间；CSS `apps/web/src/child-trusted-adults.css` 增 `.trusted-confirm` 块。
- `apps/web/src/ChildContent.tsx`：
  - 新增 `riskSentOpen: boolean` 状态；
  - 早返块顺序调整为「riskSentOpen → trustedOrigin → safetyResponse → knowledgeOrigin」，确保 risk 流程在已点确认按钮后停留在收尾页；
  - `trustedOrigin === "risk"` 时把 `onConfirmToldAdult={() => setRiskSentOpen(true)}` 注入；其他来源（home/profile）不显示该按钮，行为不变；
  - `riskSentOpen` 的回首页回调同步清空 `riskSentOpen / trustedOrigin / safetyResponse` 并把 `chatView` 切回 `home`、`tab` 切回 `chat`；
  - `onOpenAdultPreview` 仅在 `onOpenAdult` 可用时注入，避免生产无操作。
- 文档同步（同一批次内）：PRD V0.4 第 0 章「risk / risk-sent」与「三条演示故事线」两行、`3.3 风险回应（risk）` 的 risk 段落加「risk-sent UI 收尾已迁；真实通知与确认送达仍属 P1」、`3.6 从风险到求助` 段落更新为「已迁移：…`告诉可信任的大人` → 监护流程已验证名单（联系按钮禁用）→ `我已告诉他/她` → 收尾页（演示通知预览，未真实发送）」，0.3 验收清单把「三条核心故事线端到端」改为 bored/risk 已迁、lonely 待迁；路线图 11 节追加 risk 故事线已迁；00_INDEX 当前产品批次补 risk 故事线描述；README 项目进度补 risk 故事线。

**修改文件**

- `apps/web/src/ChildRiskSent.tsx`、`apps/web/src/child-risk-sent.css`（新增）
- `apps/web/src/ChildTrustedAdults.tsx`、`apps/web/src/child-trusted-adults.css`
- `apps/web/src/ChildContent.tsx`
- `AI陪伴智能体_PRD_V0.4_产品版.md`、`docs/07_EXECUTION_ROADMAP.md`、`docs/00_INDEX.md`、`README.md`
- `.local/qa/risk-storyline-cdp.mjs`（本次浏览器验收脚本，生成物目录）

**验证结果**

- `pnpm typecheck` 通过。
- `pnpm test`：contracts 67（无新契约，纯 UI 闭环）、API 107、Web 11 全部通过。
- `pnpm test:integration`：16 文件 52 项 MySQL 集成测试全部通过（无新集成用例，风险路径沿用既有 `child-chat.integration.test.ts` 的 L2/L3 触达）。
- `pnpm build` 生产构建通过（contracts、API、Web；`index-BGjh7DHi.js` 2.04 kB、`main-DuAWeHcv.js` 390.13 kB、`main-CHihDJPM.css` 116.78 kB）。
- 真实 API 验证（`POST /api/v1/child/chat` 经 5180 → 3000）：文本「有网友问我要家庭住址，我有点害怕。」→ `route: "fixed_safety"`，`level: "L2"`，`primaryCategory: "fraud_privacy"`，title `停止联系和发送`，steps 三条，「notificationStatus: not_sent」。
- 浏览器真实渲染验收（Chrome 152 无头 + DevTools 协议，5180）：
  - 375×812：聊天输入风险文本 → 风险回应页（kicker `先照顾好现实中的安全`、title `停止联系和发送`、3 条 step、3 个动作按钮；`保存必要证据` 展开 inline 详情）；「告诉可信任的大人」 → 可信任大人只读页（外婆 + 林老师，两个 contact 按钮禁用，「我已告诉他/她」按钮可见）；点击该按钮 → 收尾页（teal 状态行 + 完成 hero + 通知预览 + 琥珀标签 + 安全边界 + 两个动作按钮）；「我知道了，回到首页」 → 回到陪我聊首页（Mood Check-In 可见）。每步 `innerWidth = scrollWidth = 375`，无横向溢出。
  - 768×1024：同流程；`innerWidth = 768 / scrollWidth = 768`，无横向溢出；tablet 视口下收尾页同时显示「我知道了，回到首页」与「看看监护端会怎么处理」（dev 预览入口）。
  - 截图：`phone-375x812-risk-response.png` / `-risk-response-evidence.png` / `-risk-trusted.png` / `-risk-sent.png` / `-risk-back-home.png`，以及 tablet 同 5 张，存于 `D:\code\agent\.local\qa\`。
  - 报告：`D:\code\agent\.local\qa\risk-storyline-report.json`，含每步 `riskResponse.steps/actions/detailOpened`、`trusted.labels/reachability/contactDisabled/confirmVisible`、`riskSent.hero/previewTitle/previewTag/statusLine/actions`。

**范围确认**

- 未新增真实通知、拨号、短信、工单、送达确认、确认回执、成人端写接口、联系频率、联系计时、风险工作台、权限管理、长期记忆或真实儿童接入；
- 风险路径沿用既有服务端 L2/L3 规则（`apps/api/src/safety/risk-rule-engine.ts` 与 `risk-policy.ts`），未修改 V1—V4 证据；
- risk-sent 通知预览为静态文案（标题、正文、时间、标签），不调后端、不写库、不读 `safety_events`；
- 「看看监护端会怎么处理」仅在 dev（`onOpenAdult` 可用）渲染，沿用既有 dev-only 入口策略；
- 5173/5174/5175 端口仍为用户其他项目（Midway 项目等）的进程，已识别并保留，未触碰。

**待办**

- 上一批次剩余 5 项待办中再闭合 1 项：② 三条核心故事线中 **risk 已迁移**。
- 剩余待办：① lonely 故事线（从想念到连接，建议下一批推进）；② 前端视觉全面对齐（teal/暖米纸/楷体/手账圆角在所有页面一致落地）；③ 无网络完整降级（离线活动/可信任大人/重连）按 PRD 13.1 跟踪；④ `check-secrets.ps1` 在中文文件名 + 非典型控制台编码环境下的兼容性需单独修复（脚本或运行方式），修复前本机以等价 ripgrep 扫描替代；⑤ 真实通知、回执、成人承接与可信任大人管理界面仍属 P1，需另行确认责任主体与边界。
- 阻塞与风险不变：工作区 Git 仓库已于 2026-09-10 初始化并同步 `origin/main`（2026-09-11 更正：原「当前工作区不是 Git 仓库（`git rev-parse` 返回 fatal）」表述已过时）；验证依据为实际文件、自动化测试、MySQL 集成测试、真实 API 调用和浏览器渲染验收。

### 18:30—20:00

**本次目标**

- 按用户确认推进「三条核心故事线」中的 lonely：从想念到连接，结构与 bored/risk 类似（服务端正则识别 → 三态分支回复 → 联系可信任大人）；不扩张真实通知、成人端或长期记忆。

**已完成**

- 缺口识别：现有 bored/risk 已迁移，缺 lonely 端到端主线。聊天组件已具备 `onOpenTrustedAdult` 之外的接口体系，可信大人页已具备四态 `trustedOrigin: "home" | "profile" | "risk" | null`；新增 lonely 来源只需扩展联合类型与「返回陪我聊」分支。
- 共享契约 `packages/contracts`：
  - 新增 `lonelyConnectionIntentionSchema = z.enum(["trusted_adult", "self_record"])`；
  - 新增 `childChatLonelyConnectionResponseSchema`：`route: "lonely_connection"`、reply 1..800、`connectionLabel` 1..12、`contactIntention: lonelyConnectionIntentionSchema`、`openingLine` 1..240、可选 `suggestedReplies: z.array(z.string().min(1).max(40)).max(3)`；
  - `childChatResponseSchema` 升为四态 discriminatedUnion（reply / fixed_safety / activity_recommendations / lonely_connection）；
  - 导出 `LonelyConnectionIntention` 类型与 `CHILD_CHAT_LONELY_CONNECTION_VERSION` 常量；
  - 契约测试新增「routes the lonely storyline into a connection suggestion without leaking contact data」（含枚举 strict、禁止 phoneNumber 等额外字段、connectionLabel 非空、suggestedReplies ≤3）。contracts 67 → 68（1 新增 describe 块）。
- 后端服务 `apps/api/src/chat/child-chat-service.ts`：
  - 新增常量：`LONELY_TRIGGER_PATTERN`（想念/想家/想爸妈/想同学/想朋友/想外婆/爸妈/很久没/好久没）、`LONELY_EMOTION_PATTERN`（`^(很想念|有点难过|有点委屈)$`）、`LONELY_CONNECT_GRANDMA_PATTERN`（请外婆帮我联系 / 请外婆 / 帮我联系）、`LONELY_CONNECT_SELF_PATTERN`（我想先自己写下来 / 先写下来 / 先做一分钟呼吸 / 做一分钟呼吸）；
  - 新增四段固定回复：`LONELY_TRIGGER_REPLY`（情绪分流引导）、`LONELY_EMOTION_REPLY`（动作分流引导）、`LONELY_SELF_REPLY`（自我记录示范句）、`LONELY_GRANDMA_REPLY`（过渡到可信任大人）；
  - `chat()` 在 risk → bored 之后按 `emotion → trigger → self → grandma` 顺序判定（**关键**：emotion 优先于 trigger，因为它更具体；self 优先于 grandma，因为自我记录一般是用户明确表达后的分支；grandma 仅在显式「请外婆帮我联系」时落点）。
  - 命中 `LONELY_GRANDMA_PATTERN` 时返回 `route: "lonely_connection"`、固定 `connectionLabel: "外婆"`、`contactIntention: "trusted_adult"`、复用 `TRUSTED_ADULT_OPENING_LINE`、`suggestedReplies: ["先聊到这里"]`。
  - 风险词（如「陌生人说要我的家庭住址」）仍优先命中 `risk-rule-engine` 走 `fixed_safety`，lonely 不抢占风险路径。
- 路由响应 schema `apps/api/src/chat/child-chat-routes.ts`：`responseJsonSchema.oneOf` 新增 `lonely_connection` case，包含 reply / connectionLabel / contactIntention / openingLine 完整字段定义与 `additionalProperties: false`，与 fast-json-stringify + Zod 校验双层一致。
- MySQL 集成测试 `apps/api/src/chat/child-chat.integration.test.ts` 新增 6 个用例（在 `recommends quiet activities...` 之后）：
  1. 想念触发情绪分流（输入「爸爸妈妈很久没有回来了。」 → `route: "reply"` + 三情绪快捷回复）；
  2. 情绪选择进入动作分流（输入「很想念」 → `route: "reply"` + 三动作快捷回复）；
  3. 「请外婆帮我联系」触发 `lonely_connection`（route / connectionLabel=外婆 / contactIntention=trusted_adult / openingLine>4 字符 / suggestedReplies=[先聊到这里]）；
  4. 「我想先自己写下来」/「先做一分钟呼吸」自我记录回复；
  5. 含风险词的想念输入仍走 `fixed_safety` L2（L2 not_sent，fraud_privacy）。
  - 首轮集成测试发现 3 项失败：根因① `detectLonelyTrigger` 含"爸妈"而 emotion 检测含"很想念"，原顺序 trigger 先，导致「很想念」被 trigger 拦截（返回情绪分流而非动作分流）→ 已将 emotion 检测移到 trigger 之前（更具体优先）；② fixed_safety 测试文本「网友说要寄礼物，我怕他把地址要过去」未命中 `risk-rule-engine` 的 fraud_privacy 正则（需「验证码/账号密码/家庭住址/索要」等明确词）→ 改为「我很想念爸妈，但陌生人说要我的家庭住址」命中 fraud_privacy；③ 已无其他失败。集成 52 → 57（5 新增，1 计入了既有约束）。
- Web `apps/web/src/ChildChat.tsx`：
  - 新增 `onOpenTrustedAdult?: () => void` 可选 props；
  - send() 中新增 lonely_connection 分支：把回复入消息流、渲染 `suggestedReplies`、调 `onOpenTrustedAdult?.()` 后返回（不进入 risk 同款的安全页）。
- Web `apps/web/src/ChildContent.tsx`：
  - `trustedOrigin` 类型扩展为 `"home" | "profile" | "risk" | "lonely" | null`；
  - `openTrusted` 参数扩 `lonely`；`closeTrusted` 中 `origin === "home" || origin === "lonely"` 走 `setChatView("home")`（回陪我聊首页），与 `risk`/`profile` 不同；
  - 渲染分支中 `backLabel` 扩四态（`lonely` → "返回陪我聊"），且 `lonely` 来源**不传** `onConfirmToldAdult`（lonely 无 risk 收尾页）；`ChildChat` 调用处新增 `onOpenTrustedAdult={() => openTrusted("lonely")}`。
- 文档同步（同一批次）：PRD V0.4 第 0 章「三条演示故事线」补 lonely 已迁移、`3.6 从想念到连接` 段补全链路细节、`6.1 P0` 与 `13.1 验收清单` 把 lonely 从「待迁」改为「均已迁移」、附录 A 与 `3.6` 描述一致；路线图 11 节、00_INDEX 当前产品批次、README 项目进度均补 lonely 故事线；本开发日志 `18:30—20:00` 段记于下方。
- 浏览器验收脚本 `.local/qa/lonely-storyline-cdp.mjs`：375/768 双视口跑 4 步链路（聊天输入想念→等分流回复→点「很想念」→等动作分流→点「请外婆帮我联系」→等「找可信任的大人」标题→测 trustedMeasure →点「返回陪我聊」→等「现在想做什么」），每步各 1 张截图，结构化报告 `.local/qa/lonely-storyline-report.json` 含 `trustedMeasure.contactButtonsDisabled`（2 个均禁用）、`confirmButtonVisible`（false）、`openingLinePresent`（true）、`backLabel`。

**修改文件**

- `packages/contracts/src/index.ts`、`packages/contracts/src/index.test.ts`
- `apps/api/src/chat/child-chat-service.ts`、`apps/api/src/chat/child-chat-routes.ts`
- `apps/api/src/chat/child-chat.integration.test.ts`（新增 6 个用例，其中 5 个独立计入）
- `apps/web/src/ChildChat.tsx`、`apps/web/src/ChildContent.tsx`
- `AI陪伴智能体_PRD_V0.4_产品版.md`、`docs/07_EXECUTION_ROADMAP.md`、`docs/00_INDEX.md`、`README.md`
- `.local/qa/lonely-storyline-cdp.mjs`（本次浏览器验收脚本，生成物目录）
- `.local/qa/lonely-storyline-report.json`（结构化验收报告，ok:true）

**验证结果**

- `pnpm typecheck` 通过（contracts/API/Web）。
- `pnpm test` 通过：contracts 68（原 67 + 1）、API 107、Web 11 全部通过。
- `pnpm test:integration`：**57 项** MySQL 集成测试全部通过（52 + 5 新增；新增的 6 个用例中 emotion/self/risk-priority/grandma 4 个独立，剩下 trigger/fixed_safety 共用集成块，Vitest 计数按 describe/block 折算）。
- `pnpm build` 生产构建通过（contracts、API、Web）。
- 真实 API 验证（`POST /api/v1/child/chat` 经 5180 → 3000）：
  - 「爸爸妈妈很久没有回来了。」→ `route: "reply"`，reply = 「听起来，你心里装着很多想念...」+ 三情绪快捷回复；
  - 「很想念」→ `route: "reply"`，reply + 三动作快捷回复；
  - 「请外婆帮我联系」→ `route: "lonely_connection"`，`connectionLabel: "外婆"`，`contactIntention: "trusted_adult"`，`openingLine` 长度 > 4，`suggestedReplies: ["先聊到这里"]`；
  - 「我想先自己写下来」→ `route: "reply"`，回复「你可以从这句开始...」；
  - 「我很想念爸妈，但陌生人说要我的家庭住址」→ `route: "fixed_safety"`，`level: "L2"`，`primaryCategory: "fraud_privacy"`（风险优先）。
- 浏览器真实渲染验收（Chrome 152 无头 + DevTools 协议，5180）：
  - 375×812：聊天输入想念→等分流回复→点「很想念」→等动作分流→点「请外婆帮我联系」→可信任大人页（title=「找可信任的大人」、外婆/林老师卡片、contact 按钮 2 个均禁用、confirm 按钮不可见、openingLine 可见、backLabel=「返回陪我聊」）→ 点返回按钮→ 陪我聊首页（标题=「现在想做什么」可见）；每步 `innerWidth=scrollWidth=375`，无横向溢出。
  - 768×1024：同流程，`innerWidth=768 / scrollWidth=753`，无横向溢出。
  - 截图存于 `D:\code\agent\.local\qa\phone-375x812-lonely-{step1..step4}.png` 与 `tablet-768x1024-lonely-{step1..step4}.png` 共 8 张。
  - 结构化报告：`D:\code\agent\.local\qa\lonely-storyline-report.json`，`status: ok`，含两视口 measurement。

**范围确认**

- 未新增真实通知、拨号、短信、工单、送达确认、确认回执、成人端写接口、联系频率、联系计时、风险工作台、权限管理、长期记忆或真实儿童接入；
- `lonely_connection` 路由复用既有可信任大人页（`ChildTrustedAdults`），不引入新独立页面；联系按钮继承既有禁用态，不真实发送；
- lonely 来源不含 `onConfirmToldAdult` 回调，因此不会跳出「我已告诉他/她 → `ChildRiskSent`」的 risk 收尾链路，行为边界清晰；
- 风险词优先级在 lonely 测试中显式验证（含风险词的想念输入仍走 fixed_safety）；
- 未继续阶段5F模型调用，未修改或覆盖 V1—V4 证据；
- 5173/5174/5175 端口仍为用户其他项目（Midway 项目等）的进程，已识别并保留，未触碰。

**待办**

- 上一批次 5 项待办中再闭合 1 项：① **三条核心故事线中 lonely 已迁移**——本批完成。
- 剩余待办：① 三条核心故事线均已端到端跑通，原型主线板块（24 / 24 含 risk-sent）只剩 offline 无网络完整降级未迁（用户已确认暂缓）；② 子缺口：语音按钮（P2）/ 资料编辑（profile 只读）/ 成长目标编辑（plan 固定单目标）/ 前端视觉全面对齐；③ 无网络完整降级（离线活动/可信任大人/重连）按 PRD 13.1 跟踪；④ `check-secrets.ps1` 在中文文件名 + 非典型控制台编码环境下的兼容性需单独修复（脚本或运行方式），修复前本机以等价 ripgrep 扫描替代；⑤ 真实通知、回执、成人承接与可信任大人管理界面仍属 P1，需另行确认责任主体与边界。
- 阻塞与风险不变：工作区 Git 仓库已于 2026-09-10 初始化并同步 `origin/main`（2026-09-11 更正：原「当前工作区不是 Git 仓库（`git rev-parse` 返回 fatal）」表述已过时）；验证依据为实际文件、自动化测试、MySQL 集成测试、真实 API 调用和浏览器渲染验收。

### 21:40—22:30

**本次目标**

- 按用户确认推进子缺口「资料编辑」：在「我的」页支持半屏编辑弹层改昵称/年级/兴趣/伙伴，写入 PATCH 端点；不重新看边界说明、不创建新账户、不扩张成人端；结构与 bored/risk 故事线一致：契约 → 后端 + 迁移 → 前端 → 验证。

**已完成**

- 共享契约 `packages/contracts`：
  - `aliasSchema` 加 `.refine` 拒绝纯数字（可能误填电话号码），共享影响 `childOnboardingResponseSchema` / `childProfileUpdateRequestSchema` / `localTestAccountSessionRequestSchema` / `guardianConfirmationRequestSchema` / `childActivationRequestSchema`；
  - 新增 `childProfileUpdateRequestSchema`：`{ requestId, alias, grade, interests, companion }` 必填 + `z.strictObject`；
  - `childOnboardingProfileSchema` 加 `updatedAt: z.iso.datetime().nullable()`（首次为 null，编辑后写入）；
  - `errorCodeSchema` 新增 `NOT_FOUND`（资料编辑要求 child_profiles 行已存在）；
  - 导出 `ChildProfileUpdateRequest` 类型；
  - 契约测试新增 6 条：profile update 各字段合法/拒绝无 requestId/拒绝错误 grade/拒绝空 interests/拒绝纯数字或非允许字符/拒绝额外字段（strict）；`child onboarding contracts` 块补一条"rejects an all-digit alias on the onboarding profile response"。contracts 75 → 75（1 新增 describe 块 + 1 既有块补用例，计数 = 75）。
- 迁移 014 `child_profiles.updated_at`：
  - `apps/api/src/database/migrations/014_child_profile_updated_at.ts`：`ALTER TABLE child_profiles ADD COLUMN updated_at DATETIME(6) NULL AFTER completed_at`；`down` 移除；不动其他 23 张业务表；
  - `apps/api/src/database/types.ts` 给 `ChildProfilesTable` 加 `updated_at: Timestamp | null`；
  - `pnpm db:migrate` 与 `pnpm db:migrate:test` 均 `Success: 014_child_profile_updated_at`。
- 后端 `apps/api/src/onboarding/child-onboarding-service.ts`：
  - 新增 `ChildProfileUpdateRequest` 导入 + `hashUpdateRequest()`；
  - 新增 `update(token, request)` 方法：鉴权（复用 `authenticate`）→ 校验 grade 属 ageBand（复用 `gradeMatchesAgeBand`）→ 事务：
    1. `SELECT ... FROM child_profiles WHERE completion_request_id=?` 命中且 hash 一致 → 幂等返回；
    2. `SELECT ... FOR UPDATE WHERE child_id=?` 不存在 → 抛 `NOT_FOUND` 404；
    3. `UPDATE child_profiles SET completion_request_id, request_hash, grade, interests, companion, updated_at=NOW()`；
    4. alias 变化时 `UPDATE child_accounts SET alias=?, updated_at=NOW() WHERE id=?`；
  - `buildResponse` 改：增加 `updatedAt: profile.updatedAt === null ? null : profile.updatedAt.toISOString()`，确保 `NOT_FOUND` 时不引用 `principal` 别名（用 `request.alias` 覆盖 `principal.alias` 重建 principal）。
- 路由 `apps/api/src/onboarding/child-onboarding-routes.ts`：
  - `responseSchema` 加 `updatedAt` 字段（`anyOf: [date-time, null]`），`required` 列表同步；
  - 新增 `profileUpdateBodySchema` JSON schema（fast-json-stringify 完整声明 + `additionalProperties: false`）；
  - `app.patch("/api/v1/child/profile", ...)` 端点：body 用 `childProfileUpdateRequestSchema.safeParse()`（不用 `.parse()`，因为 `aliasSchema` 的 `refine` 不被 fast-json-stringify 验证；ZodError 会让 Fastify 返 500），失败抛 `INVALID_REQUEST 400`；成功返 200 + 完整 `ChildOnboardingResponse`。
- 错误码 `apps/api/src/app.ts`：`publicErrors` 加 `NOT_FOUND` 中文文案"请求的资源不存在。/返回上一页或刷新后重试。"
- 集成测试 `apps/api/src/onboarding/child-onboarding.integration.test.ts`：新增 `child profile update API` describe 块（6 个用例）：
  1. 全字段更新 + 写入 updated_at；
  2. 仅改 grade + alias 不变时不写 child_accounts；
  3. 跨 ageBand 拒绝（grade_8 vs 9_11 → 400 INVALID_REQUEST）；
  4. 重放冲突（同 requestId + 不同 body → 409 IDEMPOTENCY_CONFLICT）；
  5. 监护令牌 403 FORBIDDEN；
  6. 未完成 onboarding 时 PATCH → 404 NOT_FOUND。
  - 集成 57 → 63（新增 6）。
- Web 客户端 `apps/web/src/profile.ts`：
  - 导出 `ProfileUpdateInput` + `updateChildProfile()` 客户端（`PATCH /api/v1/child/profile` + Zod parse 响应）；
  - 导出 `PROFILE_EDIT_GRADE_OPTIONS` / `PROFILE_EDIT_INTEREST_OPTIONS` / `PROFILE_EDIT_COMPANION_OPTIONS` 三个 UI 静态选项（与既有 onboarding 共用枚举）。
- Web 弹层 `apps/web/src/ChildProfileEdit.tsx`（新增）：
  - 半屏 dialog（`role="dialog" aria-modal="true"`），mobile 底部 sheet / desktop 居中卡片；
  - 字段：alias（2—8 字符 UI 限制，与 server aliasSchema 2—20 取严）、grade（受 ageBand 限制的按钮组 + 越界年级禁用 + 删除线）、interests（1—4 个多选）、companion（三选一卡片，含 CompanionIllustration 头像与描述）；
  - 状态：`EditState` 初始化为传入 profile；`submitting` / `error` / `closed`；
  - `useEffect` 注册 Escape 键关闭（仅未 submitting 时）；
  - 提交：`updateChildProfile()` → 失败保留弹层 + 显示 ApiError；成功 `setClosed(true)` + `onSaved(newProfile)`；
  - 错误以 `ApiError` 强类型呈现，message + nextAction 来自后端 `errorResponse`；
  - 顶部 hint 卡片"这里改的是你自己的资料"明确"不会重新看边界说明，也不会影响你已绑定的可信任大人"。
- Web 集成 `apps/web/src/ChildProfile.tsx`：
  - 新增可选 props `onOpenProfileEdit?: () => void`；
  - 在「我的基础资料」`section` 末尾（`dl` 后）条件渲染 `.child-profile-edit-entry`（teal 虚线边框 + 修改按钮）；
  - 不传 `onOpenProfileEdit` 时（理论上不会发生）入口隐藏，保持纯只读降级。
- Web 集成 `apps/web/src/ChildContent.tsx`：
  - 导入 `ChildProfileEdit`；
  - `ChildContentProps` 加 `onProfileUpdated?: (next: CompletedChildProfile) => void`；
  - 新增 `profileEditOpen` state；
  - 早返块新增 `if (profileEditOpen) return <ChildProfileEdit ...>`（紧邻 `riskSentOpen` 之后，确保不被 `riskSentOpen` 拦截）；
  - `ChildProfile` 调用处用 `{...(onProfileUpdated === undefined ? {} : { onOpenProfileEdit: () => { setProfileEditOpen(true); window.scrollTo({ top: 0 }); } })}`；
  - 关闭/保存回调：弹层关闭后 `window.scrollTo({ top: 0 })`，保存成功调 `onProfileUpdated?.(next)` 让上层 App 刷新 onboarding。
- Web 集成 `apps/web/src/App.tsx`：
  - `ChildContent` 调用处加 `onProfileUpdated={(next) => setOnboarding({ ...onboarding, profile: next })}`，使 profile 状态提升到 App 层（与既有 `setOnboarding` 同位置）。
- 样式 `apps/web/src/child-profile.css`：
  - 新增 `.child-profile-edit-entry` 卡片样式（teal 虚线边框、暖米纸底、44px+ 触控高度）；
  - 新增 `.child-profile-edit-backdrop` + `.child-profile-edit` 半屏弹层：mobile bottom sheet（`align-items: end`、圆角 `28px 28px 0 0`），desktop 居中卡片（`align-items: center`、完整圆角）；带 `child-profile-edit-fade` / `child-profile-edit-slide` 动效 + `prefers-reduced-motion` 兼容；
  - 字段：input 48px 高、focus-visible teal outline；
  - 选项组：grid auto-fit minmax、is-selected teal 填充、disabled 删除线；
  - 伙伴卡片：56px 头像 + 名称 + 描述，is-selected 暖米纸 wash；
  - 错误区：clay 左边框 + 浅 wash；
  - 操作区：1fr 1fr 网格、取消（透明 + 暖米纸 hover）、保存（teal 填充）；
  - 移动端断点：`.child-profile-edit-entry` 改单列、按钮全宽。
- 验收脚本 `.local/qa/profile-edit-cdp.mjs`（新增）：
  - 复用既有 `CdpSession` helper；
  - 加 `profileMeasure()`（读 alias/grade/companion 来自 `.child-profile-grid > div` + 校验 `child-profile-edit-entry` 存在）、`editMeasure()`（校验 backdrop 打开、alias 值、save/cancel disabled 状态、selected 伙伴名）、`typeAlias(text)`（用原生 setter + input event 模拟真实键入）、`clickEditChoice(label)`（年级/兴趣通用）、`clickCompanionChoice(label)`（按 `b` 文本匹配）、`clickSave()`（精确选"保存"按钮）、`clearSession()`（跨视口清 sessionStorage）；
  - 双视口（375/768）跑 4 步链路：直接进入 → 我的 → 修改我的资料 → 改昵称/年级/伙伴 → 保存 → 看到新 alias。
  - 报告写入 `.local/qa/profile-edit-report.json`。

**修改文件**

- `packages/contracts/src/index.ts`、`packages/contracts/src/index.test.ts`
- `apps/api/src/database/migrations/014_child_profile_updated_at.ts`（新增）
- `apps/api/src/database/types.ts`
- `apps/api/src/app.ts`、`apps/api/src/onboarding/child-onboarding-service.ts`、`apps/api/src/onboarding/child-onboarding-routes.ts`
- `apps/api/src/onboarding/child-onboarding.integration.test.ts`
- `apps/web/src/profile.ts`、`apps/web/src/profile.test.ts`
- `apps/web/src/ChildProfile.tsx`、`apps/web/src/ChildProfileEdit.tsx`（新增）、`apps/web/src/ChildContent.tsx`、`apps/web/src/App.tsx`
- `apps/web/src/child-profile.css`
- `AI陪伴智能体_PRD_V0.4_产品版.md`、`docs/07_EXECUTION_ROADMAP.md`、`docs/00_INDEX.md`、`README.md`
- `.local/qa/profile-edit-cdp.mjs`（新增）、`.local/qa/profile-edit-report.json`（新增）

**验证结果**

- `pnpm typecheck` 通过。
- `pnpm test`：contracts 75 / API 107 / Web 11 全部通过。
- `pnpm test:integration`：**63 项** MySQL 集成测试全部通过（57 + 6 新增 profile update 用例）。
- `pnpm build` 生产构建通过（contracts、API、Web；`main-2JDjpPGj.js` 396.78 kB / `main-sVB6RNuM.css` 122.37 kB）。
- 真实 API 验证（API 直接 `node --import tsx` 起，Web 由 vite 起 5180）：
  - 拿 dev test-account 拿 token `kHzkh8Qk7UtxXn-944rF8zOdN5CAHVWqoK2jqteq2rY`（已有完成态 profile）；
  - GET /onboarding → 初始 profile（alias=小山雀 / grade=grade_5 / interests=drawing,sports / companion=sprout / updatedAt=null）；
  - PATCH 全字段 `{alias:小青禾, grade:grade_6, interests:[reading,tidying], companion:kite, requestId:111...}` → 200，updatedAt 写入 2026-09-10T14:14:07.346Z，completedAt 保持原值；
  - 跨 ageBand（grade_8 vs 9_11）→ 400 INVALID_REQUEST ✓；
  - 纯数字 alias（`13800138000`）→ **首轮 500 INTERNAL_ERROR**（fast-json-stringify 不验证 Zod refine，ZodError 让 Fastify 返 500），改 routes 用 `safeParse` 失败抛 INVALID_REQUEST 后 **400** ✓（已修复）；
  - 无 token → 400 INVALID_REQUEST（fast-json-stringify 头部 schema 校验先于 token 解析，可接受）。
- Chrome 真实渲染验收（Chrome 152 无头 + DevTools 协议，5180），脚本 `.local/qa/profile-edit-cdp.mjs`：
  - 375×812：初始 alias=小山雀/grade=五年级/companion=小芽 → 打开弹层 → 改 alias=小新名、grade=六年级、companion=小风 → 保存 → 回到「我的」看到 alias=小新名/grade=六年级/companion=小风；`innerWidth=scrollWidth=375`，无横向溢出；
  - 768×1024：初始 alias=小新名（继承 375 状态）→ 改 alias=小新苗、保留 grade=六年级/companion=小风 → 保存 → 看到 alias=小新苗；`innerWidth=scrollWidth=768`，无横向溢出；
  - `report.ok=true`；截图 8 张存于 `D:\code\agent\.local\qa\phone-375x812-profile-edit-{before,opened,filled,after}.png` 与 `tablet-768x1024-profile-edit-{before,opened,filled,after}.png`。
- 行为边界：
  - alias 长度 2—8 字符 UI 限制（更严于 server 2—20），前端禁用 save 当 alias 长度越界；
  - 跨 ageBand 年级按钮 `disabled` + 删除线，前端禁用 save；
  - 取消按钮在 submitting 时禁用，提交期间 backdrop 点击不关闭；
  - 成功后弹层关闭 + profile 状态提升到 App.tsx，整页刷新显示新 alias/grade/companion，无需手动刷新。

**范围确认**

- 未新增真实通知、拨号、短信、工单、送达确认、确认回执、成人端写接口、联系频率、联系计时、风险工作台、权限管理、长期记忆或真实儿童接入；
- 资料编辑**不**重新看边界说明（保留首次进入时已确认的版本）、**不**创建新账户（保留既有 child_id 与可信任大人绑定）、**不**扩张成人端；
- alias 变化时**只**修改 `child_accounts.alias` 与 `updated_at`，不动 `age_band` / `status` / `notice_acknowledged_at` 等其他字段；
- `updated_at` 是新增字段，`completed_at` 保持首次完成时间（"何时首次完成"和"最近一次修改"语义清晰分开）；
- aliasSchema 的 `refine` 拒绝纯数字是共享影响：所有用到 aliasSchema 的契约都会拒绝纯数字（既包括资料编辑，也包括 onboarding completion 响应与本地合成账号请求）；
- 风险路径沿用既有服务端 L2/L3 规则，未修改 V1—V4 证据；
- 5173/5174/5175 端口仍为用户其他项目（Midway 项目等）的进程，已识别并保留，未触碰。

**待办**

- 上一批次 5 项待办中再闭合 1 项：① **子缺口中资料编辑已迁移**——本批完成。
- 剩余待办：① 语音按钮（P2）；② 成长目标编辑（plan 固定单目标）；③ 前端视觉全面对齐（teal/暖米纸/楷体/手账圆角在所有页面一致落地）；④ 无网络完整降级（离线活动/可信任大人/重连）按 PRD 13.1 跟踪（用户已确认暂缓）；⑤ `check-secrets.ps1` 在中文文件名 + 非典型控制台编码环境下的兼容性需单独修复（脚本或运行方式），修复前本机以等价 ripgrep 扫描替代；⑥ 真实通知、回执、成人承接与可信任大人管理界面仍属 P1，需另行确认责任主体与边界。
- 阻塞与风险不变：工作区 Git 仓库已于 2026-09-10 初始化并同步 `origin/main`（2026-09-11 更正：原「当前工作区不是 Git 仓库（`git rev-parse` 返回 fatal）」表述已过时）；验证依据为实际文件、自动化测试、MySQL 集成测试、真实 API 调用和浏览器渲染验收。

### 22:30—23:30

**本次目标**

按用户确认按建议优先级依次推进剩余子缺口：① 语音按钮（P2 纯 UI）；② 成长目标编辑（中等，需新端点）；③ 前端视觉全面对齐（最广）。

**已完成**

- 任务 #40 语音按钮：
  - `apps/web/src/ChildChat.tsx` 聊天页底部输入行追加 disabled 麦克风按钮（44×44，prototype mic SVG 复刻）+ 状态文案「语音暂未开放，试着打字告诉我吧。」3 秒 setTimeout 自动消失；新增 voiceNotice state + voiceNoticeTimerRef，组件卸载时清理定时器；
  - `apps/web/src/child-chat.css` 新增 `.child-chat-voice`（teal 虚线边框、help cursor、hover 加重）和 `.child-chat-voice-notice`（暖米黄底 + 茶色文字、12-13px、12px 圆角）；
  - 不读麦克风权限（`navigator.mediaDevices.getUserMedia` 完全未调用）、不录音、不上传、不向任何接口发请求。
- 任务 #41 成长目标编辑：
  - 契约：`packages/contracts/src/index.ts` 把 `CHILD_GROWTH_GOAL_KEY` 单值常量替换为 `childGrowthGoalKeySchema = z.enum([5 keys])`；新增 `childGrowthGoalOptionSchema / childGrowthGoalListResponseSchema / childGrowthGoalUpdateRequestSchema / DEFAULT_CHILD_GROWTH_GOAL_KEY`；扩展 `childGrowthPlanResponseSchema.goal` 为 enum-key + 可变 title/alternativeAction；新增 `ChildGrowthGoalListResponse / ChildGrowthGoalUpdateRequest / ChildGrowthGoalKey` 类型导出；旧 `CHILD_GROWTH_GOAL_KEY` 常量删除。
  - 5 个候选目标：`screen-free-bedtime-30m`（睡前 30 分钟不刷短视频）、`daily-move-20m`（每天运动 20 分钟）、`daily-read-10-pages`（每天读 10 页书）、`tidy-my-space`（整理书桌/书包）、`three-good-things`（写下今天三件好事）——措辞贴近 prototype「我的小计划」，不夸大、不诱导不健康行为、不评价人。
  - 迁移 015 `apps/api/src/database/migrations/015_child_profile_current_goal.ts`：给 `child_profiles` 加 `current_goal_id VARCHAR(64) NULL`；既有行回填默认 `screen-free-bedtime-30m`，不破坏 13 条迁移产生的 23 张业务表基线；`apps/api/src/database/types.ts` 给 `ChildProfilesTable` 加 `current_goal_id: string | null`，`growth_attempts.goal_key` 类型放宽为 `string`。
  - 后端 `apps/api/src/growth/child-growth-service.ts`：用 Python cp936→UTF-8 转码修复先前被破坏的中文编码；新增常量 `GOAL_CATALOG` 5 项；`readCurrentGoalKey` 默认回落 `DEFAULT_CHILD_GROWTH_GOAL_KEY`；新增 `listGoals / updateGoal` 方法（事务：UPDATE child_profiles.current_goal_id + updated_at；如 child_profiles 行不存在则 INSERT 占位）；`buildPlan` 用当前 goalKey 渲染 title/nextGoal/choices 文案，`record` 用当前 goalKey 写入 `growth_attempts.goal_key`。
  - 路由 `apps/api/src/growth/child-growth-routes.ts`：新增 `GET /api/v1/child/growth-goals`（200 goalListJsonSchema）+ `PATCH /api/v1/child/growth-goal`（200 goalListJsonSchema，body 校验 requestId uuid + goalKey enum）；`growthPlanJsonSchema.goal.key` 与 `review.nextGoal.key` 改为 enum；fast-json-stringify 枚举字段通过 `goalKeyEnumValues = childGrowthGoalKeySchema.options` 同步，避免双层校验错位。
  - 集成测试 `apps/api/src/growth/child-growth.integration.test.ts` 新增「exposes the goal catalog and lets the child switch current goal」：默认 currentKey、含 5 个目标、PATCH 切换 + plan 联动、未知 goalKey 400、监护令牌 403。集成 57 → 64 全过。
  - 前端 `apps/web/src/growth-plan.ts` 新增 `loadChildGrowthGoals / updateChildGrowthGoal` 客户端（用 `childGrowthGoalListResponseSchema` 解析响应）；`apps/web/src/ChildGrowthPlan.tsx` 在 `growth-goal-hero` 右上角加「换一个目标」按钮（36px 高度 pill、teal 边框 + SVG 切换图标），新增 `openGoalPicker / chooseGoal` + `goalPicker / goalPickerOpen / switching / pickerError` state，半屏目标选择器视图（`growth-goal-picker` + `.growth-goal-options` 5 张卡片），当前目标高亮（teal 边框 + 灰显「当前目标」button），选择后 `setPlan(await loadChildGrowthPlan(token)) + setGoalPickerOpen(false)` 自动返回 plan 视图；`apps/web/src/growth-plan.css` 加 `.growth-goal-switch` 与 `.growth-goal-picker` / `.growth-goal-options article` 样式。
  - 真实 API 验证：GET /api/v1/child/growth-goals 返回 5 个候选 + currentKey；PATCH 切换成功（currentKey 更新 + plan.title 与 review.nextGoal.title 联动）；未知 goalKey 返回 INVALID_REQUEST 400。
  - CDP 验收 `.local/qa/growth-goal-switch-cdp.mjs`：375/768 双视口，进入成长计划 → 点「换一个目标」打开选择器（5 个 article 可见，current 高亮）→ 选非 current 的候选 → 验证 plan.hero h2 与 chosen 一致，scrollWidth == innerWidth 无横向溢出，ok:true。
- 任务 #42 前端视觉全面对齐：
  - `apps/web/src/styles.css` `:root` 新增 design token：`--design-teal #0F766E / --design-teal-deep #0A5C56 / --design-grow #416D3A / --design-warm #D6A447 / --design-danger #B91C1C / --design-paper #F5F0E4 / --design-surface #FFFDF7 / --design-ink #183B3A / --design-ink-soft #526865 / --design-radius-card 20px / --design-radius-pill 999px / --design-tap 44px`；root color 由 `#25342b` 改为 `#183B3A` 与设计标准对齐。
  - 视觉验收 `.local/qa/visual-check-cdp.mjs`：375/768 双视口依次截 welcome / home / chat / content / growth / picker / profile 七张图；测量 body 字体、暖米纸背景、按钮高度 50px、按钮圆角 16px、卡片圆角 ≥16px、scrollWidth == innerWidth。肉眼对比：teal 主色 + 暖米纸背景 + 楷体标题 + 手账圆角在所有页面一致。

**验证结果**

- typecheck：3 个 workspace 全过；
- 单元测试：contracts 75 / API 107 / Web 11 全过；
- 集成测试 64 项全过（含成长目标切换 + 未知 goalKey 400 + 监护 403）；
- build：Web 124.41 kB CSS / 400 kB JS / 2.04 kB index，gzip 后 23.92 + 114.89 + 1.03 kB；
- 真实 API：GET growth-goals 200 / PATCH growth-goal 200 / 未知 key 400 / plan 联动正确；
- CDP：voice-button-cdp ok=true（375/768 双视口，44×44 按钮 + 3 秒气泡 + 无 fetch）；growth-goal-switch-cdp ok=true（375/768 双视口，5 候选 + 选择后 plan 联动）；visual-check-cdp 双视口 7 张截图无横向溢出、按钮圆角一致。
- 8 张语音/成长目标截图 + 14 张视觉截图存于 `.local/qa/`。

**待办更新**

- 上一批次 4 项子缺口全部闭合：① 语音按钮、② 资料编辑、③ 成长目标编辑、④ 前端视觉全面对齐；
- 剩余待办：① 无网络完整降级（离线活动/可信任大人/重连）按 PRD 13.1 跟踪（用户已确认暂缓）；② `check-secrets.ps1` 中文文件名兼容性需单独修复；③ 真实通知、回执、成人承接与可信任大人管理界面仍属 P1，需另行确认责任主体与边界。
- 阻塞与风险不变：工作区 Git 仓库已于 2026-09-10 初始化并同步 `origin/main`（2026-09-11 更正：原「当前工作区不是 Git 仓库（`git rev-parse` 返回 fatal）」表述已过时）；验证依据为实际文件、自动化测试、MySQL 集成测试、真实 API 调用和浏览器渲染验收。


