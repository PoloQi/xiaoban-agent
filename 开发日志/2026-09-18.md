# 开发日志 2026-09-18

> 项目：小伴AI陪伴智能体
> 当前阶段：产品开发轨道 / 阶段6（通知、成人端与风险工作台）；5F/儿童试点安全评测按2026-09-16决定暂停而非通过；6A.10后仅SLA桌面演练门槛按本地合成演练勾选，其余退出门槛未达成，未进入阶段7

## 记录

<!-- dev-log:2026-09-18:phase6-integrate-verify -->
### 14:20 · 阶段六完成状况核查与集成分支复跑/合入

**摘要**

- 核查确认阶段6未达退出门槛（SLA桌面演练等未完成），未进入阶段7；经用户选择，先复跑验证再合入集成分支：在`codex/phase6-integrate-20260917`工作树完成全量复跑，并经用户明确授权重建干净`xiaoban_test`后用标准迁移器验证000→019与019/018回滚重放；本地`main`已对齐`origin/main`（df60cb6，PR #3）。

**已完成**

- 阶段状态核查：6A.1—6A.9与6B.1均已在集成分支合并完成（6A.9风险工作台API/UI仅在集成分支，主工作树原先不可见）；阶段6五条退出门槛均未正式勾选，明确缺口为风险SLA桌面演练、通知降级接真实工单数据源的验收、数据权利真实导出/物理删除（保留策略评审，须确认）和阶段6总体验收；真实渠道/真实回执属暂停确认项。
- 集成复跑（工作树`.worktrees/phase6-integrate-20260917`，提交e2cf2e3）：MySQL集成24文件95/95；契约82/82；API离线19文件119/119；Web 6文件14/14；三工作区typecheck通过；API与Web生产构建通过（Web 138 modules）。
- 秘密扫描：系统PATH无`rg`，`scripts/check-secrets.ps1`无法原样运行（本机工具缺口，非代码问题）；按该脚本同一排除目录、扩展名集合与四条正则在PowerShell等价复现扫描，结果通过。
- 经用户授权（仅`xiaoban_test`，不触碰`xiaoban_dev`与`.local/mysql-data`）：迁移账号`DROP DATABASE`+重建空`xiaoban_test`（utf8mb4/utf8mb4_0900_ai_ci），标准迁移器`migrate.ts --target=test`从000到019全部Success；标准迁移器依次回滚019、回滚018后重放至019成功；干净库重跑全量MySQL集成24文件95/95。共享库“019已登记、018缺登记”污染状态已消除。
- 本地`main`快进合并集成分支并对齐`origin/main`（df60cb6，含PR #1与PR #3合并提交）；未推送新提交。
- 文档同步：docs/08追加2026-09-18重建与标准迁移验证记录（标注xiaoban_dev未迁移）；docs/07阶段6状态追加合并、复跑与干净迁移验证事实，不勾选退出门槛。

**修改文件**

- docs/08_LOCAL_MYSQL_DEVELOPMENT.md：追加2026-09-18解决记录。
- docs/07_EXECUTION_ROADMAP.md：阶段6状态行追加合并与验证事实。
- 开发日志/2026-09-18.md：新增本记录。
- 数据库：仅`xiaoban_test`被授权drop/recreate并迁移到019；`xiaoban_dev`未执行任何迁移；未改任何代码文件。

**验证**

- `node --env-file=D:/code/agent/.local/mysql-secrets/database.env ./node_modules/vitest/vitest.mjs run --config vitest.integration.config.ts`（apps/api，集成工作树）：污染库与重建后干净库两次均为24文件95/95通过。
- `pnpm typecheck`：contracts/api/web三工作区通过。
- `pnpm test`：contracts 82/82、web 14/14、api离线19文件119/119通过。
- `pnpm build`：contracts、api、web构建通过（Web 138 modules）。
- 标准迁移器：干净库000→019成功；019 down、018 down、018/019 up成功（Kysely Migrator，非直接调用迁移函数）。
- 秘密扫描：官方脚本因缺`rg`未能原样运行，已用同规则PowerShell等价扫描通过；不得记为官方脚本通过。
- 未发起外部网络请求；未调用模型；未读取/打印任何秘密值；未覆盖V1—V7评测证据。

**待办**

- 下一最小切片建议：6A.10 风险SLA桌面演练（确定性本地演练+记录，无网络、无真实通知），直接补阶段6退出门槛。
- `xiaoban_dev`需在常规开发流程中由`pnpm db:migrate`补齐018/019（本次未授权，未执行）。
- 通知降级组件接真实工单读取数据源（failed/timed_out/escalated）并做阶段级验收；数据权利真实导出文件、物理删除/保留策略评审（须用户确认）；生产造单写入`child_id`；阶段6总体验收。
- 本机安装/配置`rg`到PATH后恢复`pnpm check:secrets`原样运行；评估是否保留随PR合入的`TASK_BRIEF.md`（Agent A分支任务书，无敏感信息）。

**阻塞与风险**

- 阶段6仍未达退出门槛，不得进入阶段7；进入阶段7封闭Alpha仍需伦理/合规/值守与通知链路准备及用户明确确认。
- 所有通知/回执仍为本地合成状态（`synthetic:true`、`networkCallMade:false`），不代表真实送达或真实确认；真实渠道、真实回执、真实通知属暂停确认项。
- 5F仍暂停而非通过（V7停在307/500），V1—V7证据只读保留。

<!-- dev-log:2026-09-18:6A10-sla-tabletop -->
### 16:50 · 6A.10 风险SLA桌面演练（切片1/1）

**摘要**

- 在无网络、仅合成数据边界内新增确定性`RiskSlaTabletopDrillRunner`：按PRD V0.4 §9建议SLA对合成工单做可机器判定的桌面走查，within_sla剧本（L2/L3）全部检查点通过并闭环到closed，breach剧本备通道两次失败升级为escalated且检查点如实判负；阶段6“风险SLA桌面演练通过”退出门槛仅就本地合成演练勾选，真实人员/书面SLA演练仍属阶段7。

**已完成**

- 新增`apps/api/src/tickets/risk-sla-tabletop-drill-runner.ts`：每次运行在固定锚点时间（2026-09-18T00:00:00Z）新建一张synthetic工单，注入虚拟时钟，复用`RiskNotificationAttemptRunner`/`RiskLocalReceiptRunner`/`RiskOutboxClaimWorker`/`RiskTicketStore`，不复制退避SQL与状态机；不注册API、不启动worker/定时器、无新增迁移或契约枚举。
- within_sla：L2走双通道attempted（15/16秒）→delivered（60/90秒）→viewed（4/5分钟）→acknowledged（12/13分钟）→resolve（“虚构处置：”）→close（14分01秒），检查点双通道送达≤5分钟（实测90秒）、双通道确认≤15分钟（实测780秒）；L3走attempted（10/11秒）→delivered（30/31秒）→viewed（100/101秒）→acknowledged（200/201秒）→`escalate_for_immediate_human_review`（210秒）→resolve/close（360/361秒），检查点“立即送达”演练代理阈值≤60秒（实测31秒）、人工复核≤5分钟（实测210秒）。
- breach：固定占用in_app租约，备通道首次failed/attempts=1，退避窗口内claimed:false，到期二次尝试timed_out/attempts=2，工单escalated；送达/确认（L3另含人工复核）检查点输出`measuredMs:null/pass:false`，`slaMet:false`，不显示任何成功。
- 非法requestId/workerId/level（非L2/L3）/scenario/leaseDurationMs（1000—30000外）与多余字段一律`RISK_SLA_TABLETOP_DRILL_INVALID`拒绝，且在建单前失败、不留工单。
- 同步docs/00、docs/02、docs/03、docs/07（新增6A.10条目、勾选SLA桌面演练退出门槛并注明本地合成边界）。

**修改文件**

- apps/api/src/tickets/risk-sla-tabletop-drill-runner.ts、risk-sla-tabletop-drill-runner.integration.test.ts：新增。
- docs/00_INDEX.md、docs/02_TECHNICAL_ARCHITECTURE.md、docs/03_SAFETY_PRIVACY_STANDARD.md、docs/07_EXECUTION_ROADMAP.md：追加6A.10。
- 开发日志/2026-09-18.md：追加本记录。

**验证**

- 聚焦MySQL集成（vitest.integration单文件）：1文件4/4通过（L2按时、L3按时+人工复核、L2违约升级、非法请求不建单）。
- 全量MySQL集成：25文件99/99通过（新增4项；此前95/95）。说明：首次全量曾有控制台2文件5项失败，根因为主仓库contracts `dist`为6A.9合并前旧产物（缺控制台schema），重建`@xiaoban/contracts`后全绿，非本切片代码缺陷。
- `pnpm --filter @xiaoban/contracts test`：82/82通过；`pnpm test`：contracts 82/82、web 14/14、api离线19文件119/119通过。
- `pnpm typecheck`：三工作区通过；`pnpm build`：contracts/api/web通过。
- 未发起外部网络请求；未调用模型；未读取/打印秘密值；未覆盖V1—V7证据；无新增迁移、无契约schema变更。

**待办**

- 阶段6剩余退出门槛：通知降级组件接真实工单读取数据源（failed/timed_out/escalated）并做阶段级验收；“未回执不显示成功”端到端验收；数据权利删除/审计链路阶段级验收（真实导出、物理删除/保留策略评审须单独确认）；阶段6总体验收。
- 生产造单写入`child_id`；`xiaoban_dev`待`pnpm db:migrate`补齐018/019。
- 本机安装`rg`后恢复`pnpm check:secrets`原样运行。

**阻塞与风险**

- 60秒“立即送达”仅为本地演练代理阈值，不是生产SLA承诺；PRD V0.4 §9 SLA仍须合作机构书面确认，真实人员值守演练属阶段7，未完成。
- 演练全部为本地合成状态（`simulated:true`、`networkCallMade:false`），不代表真实送达/确认或真实值守介入；真实渠道与真实通知须单独确认。
- 阶段6仍未整体验收，未进入阶段7；5F仍暂停而非通过。
<!-- dev-log:2026-09-18:6A11-notice-real-source -->
### 17:25 · 6A.10提交 + 6A.11 通知降级接真实工单数据源（切片1/1）

**摘要**

- 先按用户指示提交6A.10（commit c8f9e63，8文件，未推送；docs/15、agent.md、PRD、.claude/属并行用户故事工作未纳入）；随后完成6A.11：把6A.7通知降级纯函数接入6A.9风险工作台工单详情这一真实（合成工单持久化）数据源——failed/timed_out/escalated如实驱动“暂时没能送达”，未回执绝不显示成功；阶段6“通知故障降级通过”退出门槛仅就合成工单数据源勾选。

**已完成**

- `apps/web/src/notice-status.ts`新增`noticeInputFromRiskConsoleDetail`：从工单详情映射`ticketStatus`与按通道顺序的`channelStatuses`，逻辑零分支、仅做形状映射，判定仍全部走6A.7既有`resolveNoticeStatus`。
- `apps/web/src/RiskConsole.tsx`：`NotificationState`在边界说明下渲染`NoticeStatusBanner audience="guardian"`，引入`notice-status.css`；工单escalated或任一通道failed/timed_out→“暂时没能送达”（引导当面联系），open/not_sent与单通道acknowledged→“还没有收到确认”，仅工单acknowledged/resolved/closed且双通道均acknowledged→“本地演练回执已记录（不代表真实渠道送达）”。
- `notice-status.test.ts`新增3项（fixture按`RiskConsoleTicketDetail`形状构造）：通道顺序映射；open/not_sent、breach形（escalated+timed_out）、failed、单通道确认均不显示成功且禁用词为0；acknowledged/resolved/closed+双通道acknowledged才显示local_acknowledged并保留“不代表真实”。
- 文档同步：docs/07新增6A.11条目并勾选“通知故障降级通过”（注明仅合成工单数据源）；docs/00、docs/02、docs/03追加6A.11边界。

**修改文件**

- apps/web/src/notice-status.ts、notice-status.test.ts、RiskConsole.tsx：新增映射函数/接线/测试（无API、契约、迁移、CSS规则变更）。
- docs/00_INDEX.md、02_TECHNICAL_ARCHITECTURE.md、03_SAFETY_PRIVACY_STANDARD.md、07_EXECUTION_ROADMAP.md、开发日志/2026-09-18.md：追加6A.11。

**验证**

- Web：6文件17/17通过（6A.10后为14，新增3）。
- 全量离线：契约82/82、API离线19文件119/119、Web 17/17通过；三工作区typecheck通过；Web生产构建通过（415.51 kB / gzip 119.05 kB）。
- 全量MySQL集成回归：25文件99/99通过（本切片未改API/迁移，回归确认无影响）。
- 未发起外部网络请求；未调用模型；未读取/打印秘密值；未覆盖V1—V7证据。

**待办**

- 6A.11改动提交：用户本次指示“按建议提交改动，并执行建议切片”，6A.10已单独提交（c8f9e63）；6A.11按同一“切片完成即提交”节奏一并提交，推送仍须用户另行指示。
- “未回执不显示成功”端到端总验收（儿童端risk-sent/监护主页合成预览/工作台三处联合走查）；真实渠道与真实回执、退避常驻调度器、真实导出文件与物理删除/保留策略评审（须单独确认）、生产造单写入child_id、阶段6总体验收。
- xiaoban_dev待`pnpm db:migrate`补018/019；本机安装rg恢复`pnpm check:secrets`；并行的用户故事工作（docs/15、agent.md、PRD一行）未提交，归属用户决定。

**阻塞与风险**

- 勾选仅限合成工单数据源：真实渠道故障处理、真实值守通知仍属暂停确认项；banner一切成功文案仍带“本地演练/不代表真实”限定。
- 阶段6五條退出门槛已勾2条（SLA桌面演练、通知故障降级），其余3条（未回执不显示成功端到端、成人无法查看普通完整聊天的阶段验收、删除和审计链路）未勾，未进入阶段7；5F仍暂停而非通过。
- 执行环境备注：提交期间权限分类器多次间歇性超时，通过拆分小命令完成，未绕过任何审批。
<!-- dev-log:2026-09-18:6A12-no-success-before-ack -->
### 17:45 · 6A.11/6A.10推送 + 6A.12“未回执不显示成功”三处端到端走查（切片1/1）

**摘要**

- 先按用户指示`git push origin main`，df60cb6..71ca830（6A.10 c8f9e63、6A.11 71ca830）已推送；随后完成6A.12：以“真实响应经Zod契约parse→前端映射→降级判定”的数据链测试，机器锁定儿童risk-sent、监护主页alerts、风险工作台三处用户触点在未回执时均不显示成功；阶段6“未回执不显示成功”退出门槛按合成链路勾选（已勾3/5），未进入阶段7。

**已完成**

- 推送：origin/main更新至71ca830（github.com:PoloQi/xiaoban-agent）。
- 处①儿童risk-sent：`notice-status.ts`导出共享常量`syntheticNotSentNoticeInput`（`{notificationStatus:"not_sent"} as const satisfies NoticeStatusInput`），`ChildRiskSent.tsx`引用常量替代内联字面量，banner始终为“还没有收到确认”+当面引导。
- 处②监护主页alerts：走查测试用完整dashboard响应经`guardianDashboardResponseSchema.parse`，证明alert的`notificationStatus`只能是not_sent、`acknowledgementStatus`只能是unavailable，并断言契约拒绝`delivered`取值——前端在数据层收不到成功信号；AlertDetail维持数据驱动（`{notificationStatus: alert.notificationStatus}`）。
- 处③风险工作台：经`riskConsoleTicketDetailSchema.parse`的open/not_sent、escalated+timed_out、waiting+单通道acknowledged三种工单均断言safeToShowSuccess=false且5个禁用成功词（已送达/已查看/已确认/发送成功/通知成功）为0；仅acknowledged/resolved/closed+双通道acknowledged才为local_acknowledged且文案保留“不代表真实”。
- 全仓grep用户侧文案：“正在帮助/正在处理”仅出现在否定句中，无违规成功暗示。
- 文档同步：docs/07新增6A.12条目并勾选“未回执不显示成功”；docs/00、docs/02、docs/03追加边界。

**修改文件**

- apps/web/src/notice-status.ts：新增`syntheticNotSentNoticeInput`常量。
- apps/web/src/ChildRiskSent.tsx：引用常量（+1 import，1处替换）。
- apps/web/src/notice-status-walkthrough.test.ts：新增走查4用例。
- docs/00_INDEX.md、02_TECHNICAL_ARCHITECTURE.md、03_SAFETY_PRIVACY_STANDARD.md、07_EXECUTION_ROADMAP.md、开发日志/2026-09-18.md：追加6A.12。
- 无API、契约、迁移、CSS变更。

**验证**

- Web：7文件21/21通过（6文件17→新增1文件4项）。
- 全量离线：契约82/82、API离线19文件119/119通过；三工作区typecheck通过；Web生产构建通过（415.52 kB / gzip 119.03 kB）。
- 全量MySQL集成回归：25文件99/99通过。
- 未发起外部网络请求（git push除外，系用户明确指示）；未调用模型；未读取/打印秘密值；未覆盖V1—V7证据。

**待办**

- 提交并推送6A.12（按用户“推送改动，并按建议切片执行”的同一节奏）。
- 阶段6剩余退出门槛：成人无法查看普通完整聊天的阶段级验收复核、数据权利删除和审计链路（真实导出文件、物理删除/保留策略评审须单独确认）；阶段6总体验收；生产造单写入child_id；退避常驻调度器（若阶段6要求）。
- xiaoban_dev待`pnpm db:migrate`补018/019；本机安装rg恢复`pnpm check:secrets`；并行用户故事工作（docs/15、agent.md、PRD一行）仍未提交，归属用户决定。

**阻塞与风险**

- 勾选仅限合成链路：真实渠道回执接入后，本走查必须扩展真实回执场景重跑；当前任何成功文案均带“本地演练/不代表真实”限定。
- 阶段6五条退出门槛已勾3条，其余2条未勾，未进入阶段7；5F仍暂停而非通过；进入阶段7仍须伦理/合规/值守准备与用户明确确认。
