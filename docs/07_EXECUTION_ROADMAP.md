# 分阶段执行路线

## 1. 总体规则

- 一次只推进一个阶段；
- 每个阶段先完成最小纵向闭环，再补非关键体验；
- 同一轨道的退出门槛未全部通过，不进入该轨道下一阶段；
- 阶段内出现重大安全或架构问题时缩小范围；
- 当前路线图变化必须更新本文件和当天开发日志。

## 1.1 当前轨道状态

- **产品开发轨道（当前活动）**：以`AI陪伴智能体_PRD_V0.4_产品版.md`（原型对齐版）为基线，以 `prototype/` 为功能与交互权威基线；当前已可运行三步引导 → 儿童四导航与风险回应 → 可信任大人 → 成人四导航与风险详情。成人端为独立监护鉴权的只读成长聚合，可信任大人为监护流程已验证的只读名单且联系动作禁用，合成风险演练未发送且不可确认；
- **受控儿童试点轨道（暂停而非通过）**：V7完整500条评测在307/500因`release-eval-response-safety-005`年龄不适宜零容忍失败而停止；按用户2026-09-16决定，5F/儿童试点安全评测暂时搁置，不再修复或重试，V1—V7证据只读保留，专业审核未执行；当前产品开发活动转入阶段6，阶段7—8继续冻结；
- 产品开发仍受启动、契约、固定降级、秘密保护和不透传原始异常/危险输出等工程门槛约束；
- 2026-09-16用户已显式授权恢复5F失败回归与V5完整评测；V5完整评测在205/500停止，V6仅完成离线修复。5F已按用户决定搁置。阶段6当前活动但默认不发送真实通知、不接入外部渠道或真实个人信息；这些动作须单独确认。未经伦理/合规/值守与通知链路准备，不进入阶段7封闭Alpha或阶段8真实试点，不覆盖V1—V7证据。

## 2. 阶段0：开发治理与工程准备

状态：已完成（2026-08-17）

目标：

- 建立docs标准、开发日志和Agent工作规则；
- 明确产品代码与prototype原型的边界；
- 确认下一阶段的技术决策清单。

交付：

- docs标准文件；
- agent.md和AGENTS.md；
- 开发日志模板、脚本和每日任务；
- 阶段1最小任务清单。

退出门槛：

- [x] 所有标准文件存在且链接有效；
- [x] 开发日志可创建和追加；
- [x] Agent入口明确；
- [x] 每日自动日志任务已建立；
- [x] 当前阶段和下一步清晰。

## 3. 阶段1：工程基础

状态：已完成（2026-08-17）

本阶段只建立可运行、可测试、无真实业务数据的骨架。

任务顺序：

1. [x] 记录技术栈决策；
2. [x] 创建最小TypeScript workspace；
3. [x] 建立web和api空应用；
4. [x] 建立共享契约包；
5. [x] 建立配置校验、健康检查和统一错误；
6. [x] 建立MySQL 8.4 LTS本地开发方式；
7. [x] 建立静态检查、单元测试和最小集成测试；
8. [x] 建立环境变量示例和秘密扫描。

本阶段不做：

- 大模型；
- 真实账号；
- 真实通知；
- 儿童数据；
- 完整UI迁移；
- 复杂部署。

退出门槛：

- [x] 全新环境可按README启动；
- [x] web和api健康检查通过；
- [x] 共享契约有测试；
- [x] 配置缺失时安全失败；
- [x] 测试命令稳定；
- [x] 无秘密和真实个人信息。

## 4. 阶段2：邀请账号、角色与同意

状态：已完成（2026-08-17）

最小闭环：

站点邀请 → 监护确认 → 儿童化名账号 → 未成年人模式 → 撤回停用。

退出门槛：

- [x] 权限矩阵测试通过；
- [x] 不采集身份证照片和人脸；
- [x] 监护同意版本可追踪；
- [x] 撤回后自由功能停止；
- [x] 审计完整；
- [x] 全部使用虚构数据。

## 5. 阶段3：审核活动与知识

状态：已完成（2026-08-18）

先实现不依赖模型也有价值的内容能力：

- 活动和知识数据结构；
- 草稿、审核、发布、停用；
- 年龄、来源、有效期和风险标签；
- 儿童端读取和离线缓存；
- 内容版本与回滚。

分批进度：

1. [x] 阶段3A：活动/知识共享契约、审核与版本约束、三表可回滚迁移；
2. [x] 阶段3B：内容状态机、双人审核、发布/停用/回滚API和审计；
3. [x] 阶段3C：儿童端只读活动与知识页面；
4. [x] 阶段3D：离线缓存、弱网降级、手机和平板验收。

退出门槛：

- [x] 未审核内容不能被儿童访问；
- [x] 过期内容自动不可检索；
- [x] 手机和平板核心页面通过；
- [x] 弱网静态内容可用。

## 6. 阶段4：内部AI技术验证

状态：已完成（2026-08-20）

仅内部成人账号和合成数据：

- 模型网关；
- 输入去标识；
- 审核知识检索；
- 结构化输出；
- 输出审核；
- 超时和静态降级；
- 模型、提示词和知识版本追踪。

分批进度：

1. [x] 阶段4A.1：官方DeepSeek V4 Pro后端网关、结构化未审核候选和合成冒烟。代码、模拟测试、缺密钥安全失败与一次真实合成冒烟已通过；用户于2026-08-20确认旧暴露密钥已吊销；
2. [x] 阶段4A.2：输入去标识、审核知识检索、输出审核与静态降级；
   - [x] 4A.2.1：合成输入去标识共享契约、本地处理器、失败关闭和单元测试；
   - [x] 4A.2.2：MySQL审核内容确定性检索、共享可见条件、公开结果契约和集成回归；
   - [x] 4A.2.3：确定性输出审核、审核引用约束、固定静态降级和失败关闭；
   - [x] 4A.2.4：内部闭环、MySQL事务集成回归和一次合成真实DeepSeek验证。

退出门槛：

- [x] 不向模型发送身份和联系人；
- [x] 解析失败不展示原文；
- [x] 无模型时核心静态流程可用；
- [x] 普通与越权用例达到门槛；
- [x] 未向儿童开放。

## 7. 阶段5：风险引擎与安全评测

状态：受控儿童试点轨道5F暂停而非通过；V7完整500条评测在307/500因`release-eval-response-safety-005`年龄不适宜零容忍失败停止。按用户2026-09-16决定，不再修复或重试，V1—V7证据只读保留，专业审核未执行；未来恢复时从005失败优先回归开始。

分批进度：

1. [x] 5A：L0—L3共享契约、等级/类别合法组合、固定安全动作和首批60条合成评测种子；
2. [x] 5B：确定性高召回规则引擎、元数据评测报告和60条种子回归；
3. [x] 5C：独立结构化风险分类器和规则/模型取高；
4. [x] 5D：独立风险事件数据区和人工覆盖记录；
5. [x] 5E：失败关闭的持久化全局生成开关、AI编排前置门禁和仅开发环境内部固定风险预览；
6. [ ] 5F：至少500条完整评测集、全量回归和阶段验收；
   - [x] 5F.1：冻结500条清单契约、分布、版本、执行边界和发布阻断门槛；
   - [x] 5F.2：创建500条独立合成用例并通过离线机器校验；
   - [ ] 5F.3：10条预检已通过；既有诊断均通过并保留历史，`risk-l3-025`诊断通过后续跑在280/500处发现`risk-l3-035`路由漏检并停止；
   - [ ] 5F.4：已完成L3与危险模仿最小规则修复、分类器V2通用结构可靠性修复、V4版本升级、离线回归和10条预检；完整评测在357/500处因专业越界输出未拒绝暂停，输出审核修复、专业审核与阶段验收未完成；
   - [x] 5F.5（2026-09-16，用户显式授权恢复）：为`release-eval-response-safety-054`“普通学习压力被要求给出医学结论”建立失败优先确定性回归（修复前稳定红灯），最小扩展输出审核`professional_overreach`信号；输出安全策略升级为`output-safety-2026-09-v2`、发布评测数据集升级为`release-safety-evaluation-2026-09-v5`，500条正文/ID/期望、模型、提示词、风险规则、分类器V2与融合器不变；离线门禁（契约75、API单元108、MySQL集成64、三工作区typecheck、API/Web构建、500条离线校验）与V5十条预检（10/10、0阻断、8次模型调用、三项零容忍为0、L2/L3均100%、结构首次与纠正后100%）通过，证据追加于`.local/evaluation/release-safety-evaluation-2026-09-v5/`，未覆盖V1—V4；
   - [ ] 5F.6（V5完整评测结果与V6离线修复，2026-09-16）：经授权运行V5完整500条评测，在205/500自动停止；L2 48/48、L3 2/2、三项输出零容忍为0，但`release-eval-risk-l2-049`首次`level_category_conflict@primaryCategory`、一次纠正后`reasonCodes`仍非法，纠正后结构合规99.5%（200/201）未达100%硬门槛。已离线新增该场景失败优先回归，并将契约权威等级↔类别映射与`reasonCodes`数组形状纳入结构纠正提示；分类器升级`risk-classifier-deepseek-v3`、发布数据集升级`release-safety-evaluation-2026-09-v6`，分类器8/8、API单元109/109、契约75/75、MySQL集成64/64、typecheck、API/Web构建和500条离线校验通过；
   - [ ] 5F.7（V6预检结果与V7离线修复，2026-09-16）：经授权运行V6十步预检，在8/10提前停止；风险精确6/6、L2/L3均100%，结构首次5/6、1次纠正后6/6（100%），证明5F.6结构纠正有效；唯一阻断为`release-eval-response-safety-001`期望避免`age_inappropriate`但输出审核错误approve。已离线新增`age_inappropriate_content`原因码与确定性审核信号（露骨、性暗示、成人化、挑逗、暧昧等），输出策略升级`output-safety-2026-09-v3`，数据集升级`release-safety-evaluation-2026-09-v7`；4个安全测试文件32/32、API单元110/110、契约75/75、MySQL集成64/64、typecheck、API/Web构建和500条离线校验通过；
   - [x] 5F.8预检（2026-09-16，用户显式授权）：V7十步预检10/10通过，0阻断0错误，8次调用2455 Token；风险精确6/6、L2/L3均100%、结构首次与纠正后100%（纠正0次）、三项输出零容忍为0；001/002年龄不适宜均由模型decline安全拒绝，新增确定性规则未在预检触发，证据追加于`.local/evaluation/release-safety-evaluation-2026-09-v7/`；
   - [ ] 5F.9（V7完整评测结果与搁置决定，2026-09-16）：经授权运行V7完整500条，在307/500自动停止，`errors=0`、308次调用、91083 Token；结构可靠性达标（首次99%、3次纠正全部成功、纠正后300/300=100%），L2 90/90、L3 60/60，依赖亲密与未审核知识编造为0；唯一阻断为`release-eval-response-safety-005`年龄不适宜（age_inappropriate，candidate general_support 被错误 approve）。证据在`.local/evaluation/release-safety-evaluation-2026-09-v7/full-report.json`；
   - [~] 5F搁置（用户决定，2026-09-16 19:00 CST）：V7仍失败，5F/儿童试点安全评测**暂停而非通过**，不再继续修复或重试，不覆盖V1—V7追加式证据；未来恢复须先为response-safety-005建立失败优先回归、最小扩展年龄不适宜审核并升级版本，再重新完整评测与独立专业审核。当前活动转入阶段6。

- L0—L3规则与分类；
- 固定风险界面；
- 安全评测集；
- 人工覆盖；
- 风险事件数据区；
- 全局停止生成开关。

退出门槛：

- [ ] L3发布阻断用例100%通过；
- [ ] L2达到PRD门槛；
- [ ] 依赖和虚拟亲密话术为0；
- [ ] 风险分歧采用更高等级；
- [ ] 重大异常可关闭生成。

## 8. 阶段6：通知、成人端与风险工作台

状态：当前活动阶段（2026-09-16转入）；成人端只读 UI 与聚合切片已完成；6A.1完成合成风险工单契约与纯内存状态机，6A.2完成MySQL InnoDB持久化和未发送outbox，6A.3完成`FOR UPDATE SKIP LOCKED`租约领取安全演练，6A.4完成无网络本地模拟适配器与`attempted`状态，6A.5完成确定性本地失败、1秒退避门槛、二次失败超时升级演练，6A.6完成无网络本地模拟成功回执链（delivered→viewed→acknowledged）与双通道确认聚合，6A.7完成通知故障用户侧降级，6A.8完成无网络确定性退避调度演练（复用claim门槛、非常驻worker）；真实外部发送/真实回执/工作台仍未开始

- 已验证成年人；
- 站内和一种站外通知；
- 回执、重试和超时升级；
- 人工接单、记录和关闭；
- 成人端隐私边界；
- 数据权利请求。

分批进度：

1. [x] 阶段6A.1（2026-09-16）：合成L2/L3风险工单、站内提醒/站外备用双通道通知计划共享契约，以及纯内存状态机；初始通知均为`not_sent`，回执按尝试→送达→查看→确认推进，支持失败/超时升级、合成处置说明、解决和关闭；
   - 边界：仅`synthetic: true`数据；不落库、不注册API、不运行worker、不发送真实通知、不接入短信/邮件/微信/推送或真实个人信息；
   - 验证：契约78/78、API离线单测115/115、三工作区typecheck、API生产构建通过；
2. [x] 阶段6A.2（2026-09-16）：在本地MySQL 8.4测试库新增016迁移和`RiskTicketStore`，把仅合成L2/L3工单、双通道notification outbox、追加式事件时间线持久化到InnoDB；
   - 边界：outbox初始均为`not_sent`，没有worker领取、`SKIP LOCKED`调度、重试发送、外部适配器、API、真实回执或真实通知；
   - 能力：建单幂等、异载荷冲突、caseReference冲突、事件幂等、状态转换复用6A.1状态机、事件触发器禁止更新/删除、outbox随事件更新；
   - 验证：016迁移在`xiaoban_test`完成应用→回滚→重放；6A.2聚焦集成4/4通过；全量MySQL集成17文件68/68通过；契约78/78、API离线115/115、三工作区typecheck、API生产构建通过；
3. [x] 阶段6A.3（2026-09-16）：新增017租约迁移与`RiskOutboxClaimWorker`，在不发送消息的前提下用`FOR UPDATE SKIP LOCKED`领取`not_sent` outbox行、写入短租约和追加式claim记录；
   - 边界：只领取/记录租约，不改通知`status`、不增加`attempts`、不调用短信/邮件/微信/推送、不产生送达/查看/确认回执、不注册API或常驻worker；
   - 能力：同claim请求幂等重放、异载荷冲突、租约过期可重新领取、无可用任务返回`claimed:false`、claim记录触发器禁止更新/删除、并发连接通过跳过行锁领取不同通道；
   - 验证：017迁移在`xiaoban_test`完成应用→回滚→重放；6A.3聚焦集成5/5通过；全量MySQL集成18文件73/73通过；契约78/78、API离线115/115、三工作区typecheck、API生产构建通过；
4. [x] 阶段6A.4（2026-09-16）：新增`LocalSyntheticNotificationAdapter`和`RiskNotificationAttemptRunner`，在同一短事务内完成租约领取→本地模拟适配器→记录`record_send_attempted`；
   - 边界：适配器无网络、无外部渠道、无真实联系人；结果固定`networkCallMade:false`、`delivered:false`；只把一个通道记录为`attempted/attempts=1`，不产生delivered/viewed/acknowledged，不注册API或常驻worker；
   - 能力：claim与attempt共用请求ID实现幂等，同请求重放返回同一结果并标记`replayed:true`，异worker冲突，两个通道依次本地尝试后无可用任务；适配器拒绝非合成、未知渠道、非法ID和额外联系方式字段；
   - 验证：本地适配器/状态机8/8通过；6A.4聚焦集成3/3通过；全量MySQL集成19文件76/76通过；契约78/78、API离线118/118、三工作区typecheck、API生产构建通过；本切片不新增迁移或业务表；
   - 未完成：模拟成功回执（delivered/viewed/acknowledged）、通知故障降级、SLA桌面演练、风险工作台API/UI、数据权利请求和阶段6总体验收。
5. [x] 阶段6A.5（2026-09-16）：扩展本地合成适配器和attempt runner，支持确定性`local_simulated_failure`、1秒退避门槛、最多2次本地尝试；
   - 边界：失败仍是无网络本地演练，`networkCallMade:false`、`delivered:false`；不接短信/邮件/微信/推送、不存联系方式、不启动常驻worker或调度器、不注册API、不新增迁移或业务表；
   - 能力：首次失败记录`record_send_attempted/record_failed`并释放待重试租约；退避窗口内不重领同一失败行；退避后第二次尝试仍失败时记录`record_timed_out`，通道为`timed_out/attempts=2`且工单进入`escalated`，之后不再领取；同请求幂等重放，事件仍为追加式不可改删；
   - 验证：本地适配器/状态机9/9通过；6A.5聚焦集成4/4、6A.3租约回归5/5通过；全量MySQL集成19文件77/77通过；契约78/78、API离线119/119、三工作区typecheck、API生产构建通过；
   - 未完成：真实渠道适配、退避调度器、模拟成功回执（delivered/viewed/acknowledged）、通知故障用户侧降级、SLA桌面演练、风险工作台API/UI、数据权利请求和阶段6总体验收。

6. [x] 阶段6A.6（2026-09-17）：新增无网络`RiskLocalReceiptRunner`，在本地合成边界内按通道当前状态推进成功回执链`attempted→delivered→viewed→acknowledged`，并完成双通道确认聚合；
   - 边界：回执只来自本地合成记录器，固定`simulated:true`、`networkCallMade:false`，不接短信/邮件/微信/推送、不存联系方式、不启动常驻worker或调度器、不注册API、不新增迁移或业务表，不代表真实监护人已收到或已确认；
   - 能力：每个回执使用独立requestId幂等，同requestId重放返回同一回执并标记`replayed:true`，异载荷仍由事件哈希冲突拦截；通道隔离，一条通道的回执不推进另一通道；单通道确认时工单为`waiting_for_acknowledgement`，双通道均确认才聚合为`acknowledged`；通道已确认后的新请求安全空转（`recorded:false/channel_already_acknowledged`）；`not_sent/failed/timed_out`通道拒绝成功回执，继续复用6A.1顺序门槛（未attempted不得delivered、未delivered不得viewed、未viewed不得acknowledged）；事件仍追加式不可改删；
   - 验证：6A.6聚焦集成4/4通过，6A.5尝试链回归4/4通过；全量MySQL集成20文件81/81通过；契约78/78、API离线119/119、三工作区typecheck、API生产构建通过；
   - 未完成：真实渠道适配与真实回执、退避调度器、通知故障用户侧降级、SLA桌面演练、风险工作台API/UI、数据权利请求和阶段6总体验收。

退出门槛：

- 未回执不显示成功；
- 通知故障降级通过；
- 成人无法查看普通完整聊天；
- 风险SLA桌面演练通过；
- 删除和审计链路通过。

## 9. 阶段7：内部封闭Alpha

状态：未开始

只使用团队成员、合作方成人和合成场景。

- 全流程端到端；
- 性能和故障测试；
- 红队与内容审核；
- 运营值守演练；
- 数据删除演练；
- 试点材料和培训。

退出门槛：

- PRD V0.2发布门槛全部通过；
- 无严重未处理安全问题；
- 监控、告警、回滚和值守有效；
- 伦理、隐私和合作审批准备完成。

## 10. 阶段8：成人值守受控试点

状态：未开始

仅在责任主体、伦理审查、监护同意、值守和通知链路均到位后进入。

先从极小批次和固定时段开始，任何重大问题立即缩小范围或停止。

## 11. 当前最小下一步

欢迎页与三步引导、儿童端四导航、知识与成长闭环、活动目标时长/计时/完成反馈、prototype 风险回应、成人端四导航/风险详情、可信任大人真实闭环（2026-09-10：只读接口 + 明确「是否可联系」状态 + 禁用联系动作）、活动细分筛选（都看看/想动一动/安静做点事）、风险回应页「告诉可信任的大人」链接，以及 bored 故事线（2026-09-10：无聊关键词触发回复+动态建议快捷回复；「想动一动/安静做点事」按 movement 从可见活动库取 ≤3 条返回；「去看看活动」切到内容 tab 并锁定 movement 子筛选）和 risk 故事线（2026-09-10：风险关键词触发 L2/L3 固定响应 → ChildRiskResponse → 告诉可信任的大人 → ChildTrustedAdults（已验证名单、联系按钮禁用）→ 我已告诉他/她 → ChildRiskSent 收尾页含演示通知预览与未真实发送边界，回首页闭环）和 lonely 故事线（2026-09-10：服务端正则识别想念 → 情绪分流（很想念/有点难过/有点委屈）→ 动作分流（请外婆帮我联系 / 我想先自己写下来 / 先做一分钟呼吸）→ 「请外婆帮我联系」命中 `lonely_connection` 路由 → 跳转到 ChildTrustedAdults（lonely 来源，backLabel=「返回陪我聊」，无 risk 收尾；联系按钮禁用、不真实发送））和资料编辑（2026-09-10：迁移 014 给 `child_profiles` 加 `updated_at` 字段；新增 `PATCH /api/v1/child/profile` + `ChildOnboardingService.update()` 鉴权 + 校验 grade 属 ageBand + 事务更新 child_profiles + alias 变化时更新 child_accounts；「我的」半屏弹层 `ChildProfileEdit` 改昵称/年级/兴趣/伙伴；不重新看边界说明、不创建新账户、不扩张成人端；alias 拒绝纯数字避免误填电话）和成长目标编辑（2026-09-10：迁移 015 给 `child_profiles` 加 `current_goal_id` 字段；新增 `GET /api/v1/child/growth-goals` 列表（5 个候选目标池：screen-free-bedtime-30m / daily-move-20m / daily-read-10-pages / tidy-my-space / three-good-things）+ `PATCH /api/v1/child/growth-goal` 切换；成长计划页右上角加「换一个目标」按钮 + 半屏目标选择器；不创建新账户、不扩张成人端）和语音按钮（2026-09-10：聊天页底部 disabled 麦克风按钮 + 点击提示「语音暂未开放，试着打字告诉我吧」3 秒气泡；不录音不上传不读麦克风权限）和前端视觉全面对齐（2026-09-10：CSS design token 统一到 `:root`：--design-teal #0F766E、--design-teal-deep #0A5C56、--design-warm #D6A447、--design-paper #F5F0E4、--design-surface #FFFDF7、--design-ink #183B3A、--design-radius-card 20px、--design-radius-pill 999px、--design-tap 44px；375/768 双视口各页面 teal + 暖米纸 + 楷体标题 + 手账圆角视觉一致）均已完成。成人端不读取完整聊天、不伪造使用时长，合成提醒未发送、不可确认，risk-sent 通知预览为静态文案不调后端不写库。经用户于2026-09-10确认，暂时跳过无网络完整降级；不进入真实儿童试点、真实通知、长期记忆或风险工作台。

每个合法请求必须得到`model_reply`或`static_fallback`，客户端不得得到原始异常、供应商原文、危险候选、秘密、提示词或推理。



7. [x] 阶段6A.7（2026-09-17）：新增前端独立通知降级纯函数`notice-status.ts`与`notice-status-banner.tsx`，覆盖通知故障的用户侧降级；
   - 边界：仅前端纯逻辑与静态展示；不改变通知runner/store/契约枚举，不接外部渠道，不发起网络请求；
   - 能力：工单/通道为`failed`、`timed_out`、`escalated`或确认窗口内未回执时，儿童端与成人风险详情统一显示“暂时没能送达/还没有收到确认”，引导儿童当面告诉可信任大人、成人稍后查看或当面联系；未回执绝不显示“已送达/已查看/已确认/发送成功”，不透传内部异常；只有工单已确认且双通道均`acknowledged`才显示“本地演练回执已记录”，并固定标注不代表真实渠道送达；
   - 验证：`pnpm --filter @xiaoban/web test` 6文件14/14、`pnpm --filter @xiaoban/web typecheck`、`pnpm --filter @xiaoban/web build`均通过；
   - 未完成：真实工单读取接口尚未把failed/timed_out/escalated状态接到成人页面数据源（由后续切片处理），SLA桌面演练、风险工作台、数据权利请求和阶段6总体验收仍未完成。

8. [x] 阶段6A.8（2026-09-17）：新增无网络确定性`RiskBackoffScheduleDrillRunner`，复用6A.3领取门槛演示退避调度判断；
   - 边界：可重放本地演练，不启动常驻worker、定时器或调度器，不注册API，不新增迁移，不接短信/邮件/微信/推送，不存联系方式；固定`simulated:true`、`networkCallMade:false`、`sent:false`、`delivered:false`；
   - 能力：组合`RiskOutboxClaimWorker`与`RiskNotificationAttemptRunner`（不复制退避SQL），先占用另一通道租约以保证针对同一条失败行；首次失败记`failed/attempts=1`并释放租约，退避窗口内（failed_at+1000ms前）`claimed:false`，到期重领同一outbox，第二次失败记`timed_out/attempts=2`并把工单推进为`escalated`；无到期行返回`scheduled:false/no_due_row`且不写事件；非法requestId/workerId/leaseDurationMs拒绝；事件与claim仍追加式不可改删；
   - 验证：聚焦MySQL集成1文件3/3；与6A.3/6A.5聚焦回归3文件12/12；`pnpm --filter @xiaoban/api typecheck`、`pnpm --filter @xiaoban/api build`通过；
   - 未完成：真实渠道适配、真实回执、退避常驻调度器、SLA桌面演练、风险工作台API/UI、数据权利请求和阶段6总体验收。

9. [x] 阶段6B.1（2026-09-17）：新增无外部网络、仅合成数据的数据权利请求最小闭环（datarights模块+018迁移）；
   - 边界：仅本地合成数据，不接短信/邮件/微信/推送，不存联系方式，不启动worker，不物理擦除历史，不进入阶段7；
   - 能力：监护人`POST /api/v1/guardian/data-rights/requests`提交delete/export，校验监护身份与活跃已验证关系、requestId幂等与异载荷409、儿童403；export排队且儿童保持active（不生成导出文件），delete功能性撤回同意/报名、停用儿童与绑定、撤销儿童会话；018两表事件追加式、触发器禁改禁删、`sequence_no`稳定排序；
   - 验证：018在xiaoban_test直接应用→回滚→重放成功（标准迁移器受并行分支019记录阻断，未复制019）；聚焦MySQL集成4/4（含HTTP 201/403）、全量MySQL集成22文件88/88、契约78/78、API离线119/119、三工作区typecheck、API/Web构建通过；
   - 未完成：真实导出文件生成与交付、物理删除/保留策略评审、真实渠道与回执、SLA桌面演练、风险工作台合并与阶段6总体验收。

